# 技能使用规则

本项目已安装以下工作流技能(位于 `.claude/skills/`),你必须在匹配场景**主动调用**,无需等待用户发斜杠命令:

| 技能 | 触发场景 |
|------|----------|
| `brainstorming` | 创建新功能、组件、新增行为或修改行为**之前**——先把想法理清成设计与需求 |
| `writing-plans` | 拿到需求/规格后、动手写代码**之前**——先产出多步实施计划 |
| `test-driven-development` | 实现任何功能或修 bug **之前**——先写测试 |
| `systematic-debugging` | 遇到任何 bug、测试失败或异常行为时,在提出修复方案**之前**——系统化定位根因 |

## 严格遵守

- 在适用场景必须**严格按技能定义的流程执行**,把它们当作硬性流程,而不是可选参考。
- TDD:写实现代码前必须先有一个失败的测试(Red → Green → Refactor)。
- Debugging:先定位并确认根因,再提出修复,不做盲目的增量试错。
- Brainstorming / Writing-plans:涉及多步骤或不熟悉的改动时,先设计、出计划,再编码。
- 这条规则优先于"对小改动直接动手"的默认行为。仅当改动是单行/明显的琐碎修正时,可跳过对应技能。

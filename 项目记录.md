## 2026-08-07 object_classify 学习器模式：5 ID 选项卡 + 空闲零推理 + 点击锁定 + KEY 全屏框记录

### 需求（用户确认）
1. 单通道化后 object_classify 每 2 帧跑全流水线（1 次 det + 最多 5 次 rec + 每框 gc.collect），检测帧 300ms+ 而显示帧 20ms，抖动"好卡"；object_detect 的 postprocess 8400 候选纯 Python 循环逐个 ulab 标量索引过阈也拖慢每检测帧。
2. 物体分类改为学习器：最多学习 5 个 ID（底栏 ID1~ID5 选项卡，点哪个学哪个）；运行不推理（空闲态零推理，预览丝滑）；屏幕点物体 → 按 KEY → 全屏框记录；学习后锁定态持续识别。

### 方案（用户确认的契约）
- 空闲态(_locked_feature=None)：零 NPU 推理，纯预览 + 绿十字，30fps 丝滑。
- 点预览区命中框 → 单次 det+1rec 锁定（_click_lock + extract_feature）；点空白/锁定丢失回空闲。
- 锁定态每 DET_INTERVAL=3 帧降频推理，select_lock_index 特征跟踪（已学显示 ID%d / 未学 LOCK）。
- K2 → register_at 注册锁定特征到 _selected_id 槽 → _record_flash 全屏黄框闪烁确认（约 0.4s）。

### 关键实现
- `core/object_ai.py`：postprocess 阈值筛选 confs/inds 批量转 Python list 再循环，消 8400 次 ulab 标量索引（object_detect/classify 共用提速）。
- `core/object_classify_ai.py`：FeatureExtractionApp.config_preprocess 去掉每框 gc.collect（移入 run/extract_feature 入口各一次）；新增 extract_feature(det_box, img_np) 单框提特征（点击响应快）。
- `core/object_classify_db.py`：MAX_SLOTS=5（学习上限，用户确认）+ register_at(feature, slot_id) 指定槽注册（越界返回 None，覆盖已有）。
- `scripts/object_classify/app.py`：on_frame 状态机（空闲零推理/点击锁定/锁定态降频/K2 注册）；_build_ui 5 个 ID 选项卡（选中按槽色高亮）+ _on_tab_clicked/_refresh_tabs；_record_flash 全屏框；DET_INTERVAL=3；code-simplifier 抽 _ai_input/_to_disp_boxes 公共 helper 消除重复。

### 验证
- 更新/新增 host 测试：test_object_classify_db（25→5 槽轮转 + register_at 4 项 + MAX_SLOTS=5）、test_object_ai（postprocess 向量化）、test_object_classify_detect_ast（DET_INTERVAL=3 / 5 选项卡 / 全屏框 / extract_feature / gc 收敛 / register_at 等 10 项）。Red→Green。
- 全量回归 56/60（4 历史基线失败不变：face_register/template/object_db/tag_db，与本改动无关）；py_compile 通过；code-simplifier 优化后复测全绿。
- ⚠️ 板端需真机确认：空闲态预览丝滑度（30fps）、点击锁定响应（单次 ~60ms 推理）、KEY 注册全屏框闪烁、锁定态识别显示已学 ID。
- **v2 交互调整（2026-08-07 用户再次确认）**：① 底栏 ID1~ID5 选项卡选中高亮统一绿色（0x00FF00），不再按槽色；② 去掉点击锁定（删 _click_lock/_pending_click/select_lock_index 锁定跟踪，预览区不再可点）；③ 按 KEY 直接学习画面中心物体（中央十字对准后按 K2 → 单次 det + 提含中心 (320,240) 的框特征 → register_at 注册到选中 ID → 全屏黄框闪烁确认，中心无物体时长蜂鸣提示）；④ 识别模式（DB 非空时每 DET_INTERVAL=3 帧降频推理）命中已学 ID 的物体不画物体框——屏幕四边画全局框 + 各命中物体中心标 ID%d + 中央十字架移动到最佳匹配物体中心，未命中十字架回中央，未学物体不画不报。
- v2 契约测试更新：test_has_recognition_match（database_search 匹配 + 无锁定标识符）、test_learn_center_picks_center_box（_learn_center + pick_box_at_point + 320/240）、test_tab_highlight_green（0x00FF00 + 无 BOX_COLORS）、test_learn_center_exists。Red→Green；全量回归 56/60（4 历史基线失败不变）；py_compile 通过。
- **v3 UI 细节（2026-08-07 用户确认）**：① 底栏 5 个 ID 选项卡宽度按剩余宽度分摊（`tab_w = (DISPLAY_SIZE[0] - 56 - 2 - 4*gap) // MAX_ID_TABS`，gap=4），刚好铺满底部栏（list 图标右侧到右边缘）；② 识别时命中 ID 拼接到**全屏框左上角**（`" ".join(ids)` 画在 (8,6)），不再标在物体中心/十字架里；③ **十字架固定屏幕中央不动**（不再移动到物体），命中/未命中/无 DB 均画中央十字。
- v3 契约测试：test_tabs_fill_bottom_bar（`// MAX_ID_TABS` 宽度分摊）、test_recognition_id_at_frame_corner（`" ".join` 左上角拼接）。Red→Green；全量回归 56/60（4 历史基线失败不变）；py_compile 通过。

## 2026-08-06 学习 ID 画框颜色扩展（4 → 25 色，学习 ID 不用白色）

### 需求
学习 ID 上限放开到 25 后，画框颜色表仍是 4 色，超出 4 的槽位 `BOX_COLORS.get(slot, BOX_UNKNOWN)` 回退白色——学习 ID 的框不能用白色。

### 修复
- 新增共享模块 `core/box_colors.py`：25 槽颜色表（1~4 保持历史色 绿/蓝/橙/紫 兼容既有习惯；5~25 取 HSV 色环均匀 21 等分 S=0.95 V=0.85）+ `BOX_UNKNOWN=0xFFFFFF`（仅未学习目标 id=0 白框）+ `box_color(slot)` helper。
- 8 处本地 4 色定义（body/color/gesture/object_classify/object_detect 脚本 + core/face_ai.py + core/body_ai.py；face_detect 走 face_ai.draw_result）统一改为 `from core.box_colors import BOX_COLORS, BOX_UNKNOWN`，消除重复。

### 验证
- 新增 `tests/test_box_colors.py`（6 项真单测）：25 色全覆盖 1~25、无白色、互异、1~4 历史色保持、box_color 超界回退白。Red→Green。
- 相关回归 23 项全绿；全量 55 PASS / 4 既有基线失败（同前，与本改动无关）；9 个改动文件 py_compile 全过。
- ⚠️ 板端需真机确认：第 5 个起的学习 ID 框有独立颜色（不再是白色）。

## 2026-08-06 标签识别记忆 + 快捷切换 + 已学习标记 + 上限 25 + 名称帧

### 需求
1. tag_detect 带记忆选择 AprilTag/二维码：每次进入按上次选择启动。
2. 在 0xFF 模式切换命令链路上新增 0x14=二维码、0x15=AprilTag，其它模式（含主菜单）可直接切。
3. 协议 conf 字节 bit7 = 已学习标记（learned），未学习目标以 id=0 + learned=0 上报，主机可区分。
4. 学习 ID 上限与上报上限统一 25（7 个 DB 轮转 4→25，脚本槽列表化）。
5. 去掉 6 模式底栏"已学习 x/x"标签（UI + i18n）。
6. 名称帧（类型 0x0E）上传识别名称/内容（tag/object/object_classify/gesture）。

### 方案（用户确认的契约）
- mode 码：0x04=标签识别（记忆）、0x14=二维码、0x15=AprilTag。
- learned 编码：conf 字节 bit7（conf 0~100 恒 <128 不冲突，保持 10B/组）；主机 `conf=byte&0x7F`、`learned=byte>>7`。
- 名称帧 0x0E：`[src_type][id,len,name_utf8...]` ≤250B，无名称模式不发。
- 槽位上限统一 25（删 MAX_REG_SLOTS），7 个 DB（face/object/color/gesture/body/object_classify/tag）轮转 4→25。

### 关键实现
- `core/tag_mode.py`（新建）：`.tag_fn` 标记读写，os.stat 预检查（坑#18）；tag_detect 启动读记忆、卡片切换置 dirty 主循环 task_handler 前落盘（LVGL 事件回调内不写文件，坑#2）。
- `comm/host_api.py`：`LEARNED_FLAG=0x80`、`MODE_TO_TAG_FN={0x14:"qr",0x15:"april"}`（0x14/0x15 同时加进 MODE_TO_CATEGORY→tag_detect）、切换回调升级 `(category, option=None)`、`TYPE_NAME=0x0E` + `send_name_data`（零分配复用缓冲）、tick 支持 names、上限统一 25。
- `main.py`：`_on_remote_switch(category, option=None)` option 非 None 先写 .tag_fn 再切脚本。
- 6 个注册类脚本：槽构建从固定 4 槽 `slots[slot-1]` 改列表化 `slots.append`；已注册 conf|=LEARNED_FLAG；face/object/color 未学习目标上报 id=0；tag/object/gesture/object_classify 收集 names 发名称帧；删 count_label/_refresh_count/registered i18n（face/object/color/gesture/body/object_classify）。
- object_classify 无类别体系，已注册目标名称用 "obj<槽号>" 占位标识。

### 验证
- 新增 `tests/test_tag_mode.py`（9 项真单测）；`tests/test_tag_detect_app.py`（记忆启动/dirty 写回/名称帧 AST）。
- 更新：test_host_api_switch（MODE_TO_TAG_FN/LEARNED_FLAG/TYPE_NAME/0x14/0x15 分发/option）、test_host_api_order（上限 25/conf bit7/名称帧编码）、test_main_remote_switch_ast、test_face_detect_template、test_object_detect_app、test_color_detect_ast、test_gesture_detect_ast、test_body_detect_ast、test_object_classify_detect_ast、6 个 DB round_robin 测试（4→25）、test_i18n_object、test_face_detect_i18n（registered 断言→断言不存在）。
- 相关回归全绿（tag_mode/tag_scan/tag_detect_app/host_api_switch/host_api_order/main_remote_switch_ast/framework/face/object/color/gesture/body/object_classify DB 与 AST/i18n）。
- 已知既有基线失败（与本改动无关）：test_face_register / test_template / test_object_db、test_tag_db 的 flush_to_disk 缺 path 参数（测试无参调用）等。
- ⚠️ 板端需真机验证：记忆启动、0x14/0x15 快捷切换、learned 标记、>4 目标上报、名称帧（二维码 payload 中文/网址）。

## 2026-08-01 主菜单吸附提速（缩短松手等待 + 让动画时长参数真正生效）

### 现象
连续滑动已丝滑，但松手后吸附偏慢，体验拖沓。

### 根因
1. `SCROLL_SNAP_DELAY = 150ms`：松手后要干等 150ms 才开始吸附动画。
2. **`SCROLL_SNAP_TIME = 250` 从未生效**：代码里没有 `set_anim_time` 调用，吸附动画实际用 LVGL 默认时长（约 200ms）。两段延迟叠加（150+200ms），参数调了也白调。

### 修复
- `SCROLL_SNAP_DELAY` 150 → 90ms（去抖不依赖这个长等待：连续滑动时 SCROLL 事件会清 pending，窗口只是松手瞬间兜底）。
- `SCROLL_SNAP_TIME` 250 → 160ms，且 `show()` 里 `self._scroll.set_anim_time(SCROLL_SNAP_TIME)` 显式应用（try/except 防御绑定缺失退回默认）。

### 验证
- `tests/test_main_menu_scroll_smooth.py` 新增 2 项契约：`test_snap_delay_is_short`（delay ≤ 100ms）+ `test_snap_anim_time_actually_applied`（show() 必须 set_anim_time 应用常量）。Red → Green。
- 相关回归 + 全量回归（除 4 个既有基线失败）全绿；`py_compile` 通过；code-simplifier 后复测全绿。
- ⚠️ 板端手感需真机确认：部署 `ui/main_menu.py` 后滑动试吸附快慢；仍慢可再降 `SCROLL_SNAP_DELAY`（最小 60ms）或 `SCROLL_SNAP_TIME`（最小 120ms），过小会失去动画过渡的平滑感。

## 2026-08-01 主菜单连续滑动不丝滑修复（延迟吸附 + 手势优先）

### 现象
主菜单单次滚动正常；手指连续滑动时怪怪的、不丝滑（不跟手/抖动/卡顿）。

### 根因
1. **松手即吸附动画与下一手势竞争**：`_on_scroll_end` 每次触发都立即 `_snap_to_nearest()` → `scroll_to_y(..., lv.ANIM.ON)` 启动 250ms 吸附动画。连续滑动时，上一手势的吸附动画还没结束，新手指已按下拖动 → LVGL scroll 动画与手指竞争滚动位置；吸附动画期间 SCROLL 事件高频触发，Python 回调每帧做多次 LVGL style 写入，拖慢 `task_handler` → 不跟手、抖动。
2. **`SCROLL_SNAP_DELAY = 150` 常量定义了却从未使用**（`_scroll_end_timer` 字段也是死的）——原设计意图就是"松手后延迟吸附防连续滑动打架"，但实现没接上。
3. `_scroll_to` 无距离短路：目标与当前几乎没移动时也启动动画，加剧抖动。

### 修复（手势优先 + 延迟吸附）
- `_on_scroll_end` 不再立即吸附，改为 `_schedule_snap()`：记录 `_snap_pending_at = ticks_ms() + SCROLL_SNAP_DELAY`。
- 新增 `tick()`：由 `main.py` run_menu 主循环（`lv.task_handler()` 后）调用，到期才执行 `_snap_to_nearest()`。期间若用户继续操作，pending 被取消，吸附不打扰手势。
- 新增 `_on_pressed`（注册 `lv.EVENT.PRESSED`）：手指按下即清 pending + `_cancel_scroll_anim()`（`scroll_to_y(当前值, lv.ANIM.OFF)` 终止进行中的 scroll 动画），手势完全接管。
- `_on_scroll` 中清 pending：`_cancel_scroll_anim` 终止动画会再触发 SCROLL_END 重设 pending，此处清掉避免拖动中被 tick 抢走吸附。
- `_scroll_to` 距离短路：`|目标-当前| <= 2px` 时用 `lv.ANIM.OFF` 直接落位，不启动 0 位移动画。
- 顺带清理死字段 `_scroll_end_timer`、死状态 `_is_scrolling`（只写不读）。

### 验证
- 新增 `tests/test_main_menu_scroll_smooth.py`（9 项 AST 契约）：Red 全 FAIL → Green 全 PASS。
- 相关回归全绿：`test_main_menu_scroll_smooth` / `test_main_menu_memory_ast`（手动 runner）/ `test_main_menu_cutover_ast` / `test_main_menu_runtime_ast` / `test_framework` / `test_main_remote_switch_ast`；`py_compile` 全过。
- 全量回归：除 4 个既有基线失败文件（face_register/template/object_db/tag_db）外全部 PASS。
- code-simplifier 审查后复测全绿。
- ⚠️ 板端手感（跟手/吸附节奏）需在真机验证：`ui/main_menu.py` + `main.py` 部署后连续滑动确认。

## 2026-08-01 color_detect 主循环 NameError 修复（缺 global 声明）

### 现象
板端运行 color_detect 脚本后主循环直接崩溃：
```
File "/sdcard/CamerAi/scripts/color_detect/app.py", line 753, in run
NameError: local variable referenced before assignment
```

### 根因
`run()` 主循环对模块级变量 `_pending_clear_flush` 有赋值（`_pending_clear_flush = False`，清库后写盘请求复位），但 `run()` 的 `global` 语句只声明了 `_RUNTIME, _color_db`，漏了 `_pending_clear_flush`。Python 语义：函数内只要对某名字赋值，整个函数作用域内该名字即视为局部变量，于是第 753 行 `if _pending_clear_flush:` 读取时触发 NameError。与 face_detect 曾踩的 `_pending_clear_flush` 缺 `global` 坑（记忆#face_detect 连续返工）完全同型。

### 修复
`run()` 的 `global` 声明补上 `_pending_clear_flush`。

### 验证
- 新增 host 侧 AST 契约测试 `tests/test_color_detect_ast.py::test_run_declares_pending_clear_flush_global`（与 `test_face_detect_template.py` 同款）：Red（修复前 FAIL）→ Green（修复后 PASS）。
- `test_color_detect_ast.py` + `test_color_db.py` + `test_color_db_persist.py` + `test_face_detect_template.py` 全绿；`py_compile` 全过。
- 全量回归：4 个测试文件失败为既有基线（`test_face_register.py`、`test_template.py` 涉及 main.py face_detect 特判/_template 分支，`test_object_db.py`/`test_tag_db.py` 为 `from core import` 路径问题），stash 前后一致，与本改动无关。

### 教训
任何在主循环内读写模块级标志的脚本函数，必须把该标志列入 `global` 声明。AST 契约测试（函数源码内 regex 断言 `global` 语句含目标名）是唯一能静态拦住此类 NameError 的门禁，新增脚本需同步补齐同类测试。

## 2026-07-31 全项目性能与健壮性优化（修复 + UI 流畅度）

### 修复（CRITICAL）
- **`core/face_db.py`**：`_load_next_slot` 加 `os.stat` 预检查（坑#18，open ENOENT 污染 FATFS）。
- **`core/config_manager.py`**：`app.json`/`categories.json` 读取前加 `os.stat` 预检查。
- **`core/lang.py`**：i18n JSON 读取前加 `os.stat` 预检查（补 import os）。
- **5 个 AI 脚本**（face_detect/body_detect/gesture_detect/object_detect/object_classify）：`_init_ai()` 加整体 try/except，kmodel 加载失败则 `_ai_ready=False`，`on_frame` 跳过推理不崩溃。

### 优化（UI 流畅度）
- **`scripts/camera/app.py`**：拍照白闪改为预建对象池（show/hide），避免每次拍照创建/删除 `lv.obj` 的坑#10 风险。
- **`main.py`**：`run_menu` 主循环加 try/except 打印上下文，不影响外层 `BaseException` 兜底。
- **5 个 AI 脚本主循环**：加每 300 帧 `gc.mem_free()` 监控日志，用于观测 FULL render 模式下长期内存趋势。

### 验证
- 10 文件 `py_compile` 全过。
- 9 个 host 回归测试全绿（`test_face_detect_template` 1 项预存基线 FAIL 不变——`flush_to_disk` 在 on_frame 内是已知设计，注册即写，在 task_handler 前安全窗口）。
- 坏机器部署验证：AI 脚本全部正常打开运行，主机通讯正常，camera 拍照白闪正常。

### 现象
两台硬件完全相同的 K230D 摄像头，一台能与主机通讯、一台不能。坏机器板端 `[HostAPI] tick category=main_menu msg_type=0x01 slots=none` 打印正常（固件"看起来活着"），但主机收不到任何帧；`[HostAPI] RX` 诊断打印从不出现。

### 同场：AI 脚本（face/body/gesture/object/object_classify）全部打不开
跟进发现坏机器所有 AI 类脚本点击后报 `ImportError: can't import name ScopedTiming`，traceback 指向 `core/face_ai.py:14` 的 `from libs.Utils import ScopedTiming`。

**根因**：坏机器固件 v1.2.2 的 `libs.Utils` 没有 `ScopedTiming`（`ScopedTiming in Utils: False`），`ScopedTiming` 在 `libs.PipeLine` 里（`ScopedTiming in PipeLine: True`）。好机器固件（2025-09）的 `libs.Utils` 包含 `ScopedTiming`。前者是官方 demo 规范写法（`from libs.PipeLine import ScopedTiming`），后者是固件附加的额外导出。代码写的是 `from libs.Utils import ScopedTiming`，在新固件上"运气好能通"，在旧固件上直接报错。

**修复**：5 个 `core/*_ai.py` 和 3 个 `test_face_baseline*.py` 的 import 行改为 `from libs.PipeLine import ScopedTiming`（`letterbox_pad_param` 仍在 `libs.Utils`，不动）。本次修复与 UART any() 修复都是**同一台坏机器旧固件 API 差异**引发的系列问题。

**教训**：项目代码依赖的固件库 API（`libs.Utils.ScopedTiming`、`machine.UART.any`）因固件版本而异，部署前应在目标板上执行 `hasattr(libs.Utils, 'ScopedTiming')` 等预检。两步修复合起来让坏机器同时恢复「主机通讯」和「AI 脚本运行」。

**验收（2026-07-31）**：✅ 坏机器部署改版 `core/*_ai.py` 后，人脸识别、物体识别、手势识别、人体识别、物体分类全部可正常打开运行，验收通过。

### 现象
两台硬件完全相同的 K230D 摄像头，一台能与主机通讯、一台不能。坏机器板端 `[HostAPI] tick category=main_menu msg_type=0x01 slots=none` 打印正常（固件"看起来活着"），但主机收不到任何帧；`[HostAPI] RX` 诊断打印从不出现。

### 定位过程（systematic-debugging）
1. 加 RX 原始字节插桩（v1 用 `raw.hex()`）→ 部署到好机器后连 tick 打印都消失 → 撤回为 v2（逐字节 `%02x` + 诊断段 try/except 隔离），断电重启后好机器恢复。**教训：调试插桩绝不能依赖可能缺失的 API，且必须 try/except 隔离，否则诊断代码杀死业务主循环**。
2. 坏机器：tick 有 + RX 无 → 嫌疑收窄到 UART1 收链路或固件。
3. 写 `tools/uart_rx_probe.py` 环回探针（不依赖 main.py/协议栈，直接 read 打印 + 回显）。
4. **关键证据**：坏机器探针报 `'UART' object has no attribute 'any'`。固件为 `MicroPython v1.2.2 (2025-02-22); k230d_canmv_atk_dnk230d`，其 `machine.UART` **没有 `any()`**，但有 `read/readinto/readline/write`。
5. 探测 `read()` 行为：无数据时立即返回 `None`（非阻塞），且首次 `read(1)` 直接读到 `b'Z'`（= 0x5A 协议帧头）——**主机在发、物理链路通、数据一直进得来**。

### 根因
`comm/host_api.py:poll_handshake` 第一行依赖 `self._uart.any()`。坏机器固件无 `any()` → 每次抛 `AttributeError` 被 `except Exception` 吞掉 → **永不读数据 → 不应答握手 → `_last_handshake_ms` 恒为 0 → `tick()` 的静默期判断挡死 `send_id_data` → 一个字节都不发**。tick 打印与握手状态无关（每 30 帧无条件打），所以"固件活着"是假象，协议栈静默死亡。好机器能通 = 好机器固件有 `any()`（**两台机器 CanMV 固件版本不同**，"硬件一样"≠"固件一样"）。

**固件版本实锤（2026-07-31 板端实测）**：
- 好机器：`CanMV 0295137e6(based on Micropython e00a144) on 2025-09-20`（新版固件，UART 有 any()）
- 坏机器：`MicroPython v1.2.2 on 2025-02-22`（旧版固件，UART 无 any()）
两台 K230D 硬件相同但刷的 CanMV 固件不同。根治方向：坏机器升级到好机器同款（或更新）固件；软件兼容层（_read_uart）已让旧固件无需刷机即可通讯。

### 修复
- `comm/host_api.py`：新增 `HostAPI._read_uart()` 跨固件兼容读取——有 `any()` 走 `any()` 读可用字节；无 `any()`（捕获 AttributeError）直接 `read(256)` 轮询（实测非阻塞、无数据返回 None）。`poll_handshake` 改用 `_read_uart()`。
- `tools/uart_rx_probe.py`：主循环同样弃用 `any()`，改 `read()` 轮询，跨固件可跑。

### 验证
- host 回归全绿：`test_host_api` / `test_host_api_switch` / `test_host_tick_wiring`。
- 双路径行为验证：无 `any()` fake UART（模拟坏机器固件）与有 `any()` fake UART（模拟好机器）喂真实握手帧 → 均正确应答，应答帧头 `5aa7970f`。
- 待板端验收：坏机器部署新 `comm/host_api.py` → 重启 → 主机应收握手应答 + 周期数据帧。

### 后续（2026-07-31 验收完成）
- ✅ 坏机器部署修复版 `comm/host_api.py` 后与主机通讯恢复，验收通过。
- ✅ 好机器可正常运行 `tools/uart_rx_probe.py`；探针保留在 tools/ 供后续排查复用。
- ✅ 临时 RX 调试插桩（`_debug_rx_print`）已从 `comm/host_api.py` 移除（保留 `_read_uart` 正式兼容层），host 回归全绿。
- 可选根治：坏机器刷成好机器同款固件（CanMV 0295137e6，2025-09-20）或更新版，统一两台固件版本。

## 2026-07-01 主机→摄像头脚本切换指令 — 实施完成(TDD,仅 K230 侧)

按计划 `docs/superpowers/plans/2026-07-01-host-to-camera-switch.md` 7 任务全部 TDD(Red→Green→commit)完成。

部署文件(2 源 + 2 测试):
- `comm/host_api.py` — 命令帧常量(`TYPE_MODE_SWITCH=0xFF`/`MODE_SWITCH_SRC/DST`/`MODE_SWITCH_FRAME_LEN=8`)、`MODE_TO_CATEGORY` 反向映射(0x01→None 即回菜单,0x02~0x13→对应 category)、`register_switch_handler(cb)`、纯函数 `_parse_switch_frame(buf,off)`(校验前缀 5A 97 A7 01 FF + chk + 尾 A5,返回 (mode,next_off) 或 None)、`poll_handshake` 扫描循环重写(每个 0x5A 位置先试握手常量 18B 再试命令帧 8B,命中且 mode 在表中→调 `_switch_handler(category)`+消费帧;半帧保留尾部;未知 mode 丢弃打印)。
- `main.py` — 模块级 `_on_remote_switch(category)`(None→`_clear_next_script()`+reset,否则→`_write_next_script(category)`+reset,复用菜单点击路径);`run_menu()` 与 `run_script()` 启动时 `runtime.host.register_switch_handler(_on_remote_switch)`。
- `tests/test_host_api_switch.py` — 10 项全绿(映射值/常量/解析 valid/bad_chk/bad_tail/bad_prefix/注册存在/分发 category/半帧不触发/未知mode不触发/0x01→None)。注:host 侧 exec host_api.py 须往 `sys.modules` 注 fake `machine`/`time`(因 `from machine import UART` 走 import 系统非 exec 命名空间)。
- `tests/test_main_remote_switch_ast.py` — 4 项 AST 全绿(注册点/_on_remote_switch 定义/write+reset 分支/clear+reset 分支)。

数据流: 主机 `changer_camer_mode(port,mode)` → UART1 TX `5A 97 A7 01 FF <mode> <chk> A5` → K230 `poll_handshake` 解析 → `MODE_TO_CATEGORY[mode]` → `_switch_handler(category)` → main.py `_on_remote_switch` → 写/清 `.next_script` + `machine.reset()` → 进新脚本或回菜单(热启动跳 LOGO)。

回归: test_host_api(15)/test_image_classify_ast(7) 全绿,握手 rfind/substring/non-short-circuit 既有契约不破;4 文件 `py_compile` 通过。

待板端验收: 摄像头跑任意脚本 → 主机 PikaScript `changer_camer_mode(4, 0x10)` → 摄像头 reset 进手势;`0x01` → 回菜单;各 mode(0x02~0x13)切对应脚本;坏帧(校验和错/尾错)不切不崩;切后主机仍收新脚本协议帧(mode 字段正确)。

## 2026-07-01 主机→摄像头脚本切换指令 — 设计确认(仅 K230 侧)

新功能:K230 接收主机协议帧命令,切换到任意脚本模式(或回主菜单)。机制 = reset:写 `.next_script` + `machine.reset()`,与菜单点击路径一致。spec: `docs/superpowers/specs/2026-07-01-host-to-camera-switch-design.md`(commit 31f1f5f)。

现状(已探明,不需改):
- 主机 `_camer_changer_camer_mode(port,mode)`(`E:\LBS-NEW-AI\python\pikascript-lib\camer\_camer.c:7`)已实现并绑定 PikaScript `changer_camer_mode`,发帧 `5A 97 A7 01 FF <mode> <chk> A5`(chk=sum[0..5]&0xFF),mode=主机 CAMER_MODE 枚举(与 K230 协议类型码一致)。本轮主机不动。
- K230 切脚本 = reset:`_write_next_script(cat)`+`machine.reset()`(`main.py:125-132`),重启 main() 读 `.next_script` 直接进新脚本。`_clear_next_script()` 回菜单。
- K230 `HostAPI.poll_handshake` 已把 UART1 输入读进 `_rx_buf` 按 0x5A 对齐扫描,只匹配握手常量、不校验和;`register_handler` 预留模式已存在。

设计(方案 A — HostAPI 解析+回调,main.py 执行):
1. `comm/host_api.py`:常量 `TYPE_MODE_SWITCH=0xFF`;`MODE_TO_CATEGORY` 反向映射(0x01→None 即回菜单, 0x02→camera … 0x13→image_classify);`register_switch_handler(cb)`;纯函数 `_parse_switch_frame(buf,off)`(校验前缀/len/chk/尾A5,返回 mode 或 None);`poll_handshake` 扫描循环扩展——每个 0x5A 位置除握手外也试 `_parse_switch_frame`,命中且 mode 在表中→调 `_switch_handler(category)`+消费帧;校验错/尾错/未知mode→丢弃+打印;半帧→等拼接。
2. `main.py`:启动注册 `runtime.host.register_switch_handler(_on_remote_switch)`;`_on_remote_switch(cat)`:None→`_clear_next_script()`+reset,否则→`_write_next_script(cat)`+reset(复用现成路径)。
3. `tests/test_host_api_switch.py`:纯 Python 测映射/解析/校验/回调/半帧/未知mode + AST 契约 + 自跑。
4. `tests/test_main_remote_switch_ast.py`:AST 守护 main.py 注册点 + `_on_remote_switch` 含 write/clear+reset。
数据流: changer_camer_mode→5A 97 A7 01 FF <mode>→poll_handshake→MODE_TO_CATEGORY→_switch_handler→main.py 写.next_script+reset→进新脚本(热启动跳LOGO)。验收: 主机调 changer_camer_mode(4,0x10)→进手势;0x01→回菜单;坏帧不切不崩。

非目标: 主机任何改动、进程内切换、settings切换、切换确认回执。

## 2026-07-01 图像分类(image_classify)预览脚手架 — 设计确认

新增 `image_classify` 脚本,本轮只输出单路摄像源预览(chn0 VGA RGB888),其余 AI/识别暂定(留空),不做持久化(无 DB/JSON)。spec 已写: `docs/superpowers/specs/2026-07-01-image-classify-preview-design.md`(commit 010c652)。

已就位基建: categories.json(order 11)、i18n(category.image_classify/_desc zh+en)、host_api `TYPE_IMAGE_CLASSIFY=0x13`、`resource/icons/image_classify_icon/` 目录(空)。

待补齐(实施计划待 writing-plans 产出):
1. `scripts/image_classify/__init__.py` + `app.py` — 以 `_template/app.py` 为骨架 + road_detect 的 `_DETECTION_ENABLED=False` 模式;单线程主循环 snapshot→on_frame(try/except)→show_image(OSD1)→host_tick(None);顶栏(返回钮用 `get_back_icon()` + i18n 标题)+ 透明预览 + 空底栏。修复模板 `_on_back` 引用未定义 `_RUNTIME` 的隐患(run 里设 `global _RUNTIME`)。
2. `comm/host_api.py` `CATEGORY_TYPE` 增 `"image_classify": TYPE_IMAGE_CLASSIFY` —— **关键**:缺它 host_tick 回退 0x01 主菜单,协议发错。
3. `core/app_runtime.py` `_channels_for` 增 `elif "image_classify": pass`(显式单 chn0,镜像 road_detect)。
4. `tests/test_image_classify_ast.py` — host 侧 AST 契约(镜像 test_gesture_detect_ast):CATEGORY_TYPE 含 image_classify、app 有 run/host_tick、`_DETECTION_ENABLED = False` 字面量。

无新图标资产(返回钮走共享 settings_icon/back.png)。数据流: host_tick(None)→0x13 40B 全零→主机 mode=19。

## 2026-07-01 图像分类(image_classify)预览脚手架 — 实施完成(TDD)

按计划 `docs/superpowers/plans/2026-07-01-image-classify-preview.md` 8 任务全部 TDD(Red→Green→commit)完成。

部署文件(4 源 + 1 测试):
- `scripts/image_classify/__init__.py`(包标记)
- `scripts/image_classify/app.py` — `_template` 骨架 + road_detect 的 `_DETECTION_ENABLED=False` 预览模式;顶栏(`get_back_icon()` + i18n `category.image_classify`)+ 透明预览 + 空底栏;单线程主循环 snapshot→on_frame(try/except 隔离)→show_image(OSD1)→task_handler;on_frame 预览分支每帧 `host_tick(None)`;`run()` 设 `global _RUNTIME`(修模板 `_on_back` 引用未定义 `_RUNTIME` 的隐患)。
- `comm/host_api.py` — `CATEGORY_TYPE` 增 `"image_classify": TYPE_IMAGE_CLASSIFY`(关键,防 host_tick 回退 0x01)。
- `core/app_runtime.py` — `_channels_for` 增 `elif "image_classify": pass`(显式单 chn0)。
- `tests/test_image_classify_ast.py` — 7 项 host 侧 AST 契约全绿(CATEGORY_TYPE 映射 / _channels_for 不 append AI 通道 / app 有 run / host_tick / `_DETECTION_ENABLED = False` / get_back_icon+i18n / `global _RUNTIME`)。

踩坑(实施中):Task 5 的 `app_runtime._channels_for` 注释里写了 "append chn2" 字样,被 `test_channels_for_image_classify_is_single_chn0` 的 `assert "append" not in after`(image_classify 后 200 字符窗口)误判命中 —— 注释改 "附加 chn2" 规避。教训:AST 文本契约的 200 字符窗口会扫到注释词,注释勿含被断言的关键词。

回归: test_host_api / test_app_runtime_object 全绿;5 文件 `py_compile` 通过。

待板端验收: 进"图像分类"菜单 → 单路 chn0 预览 → 主机收 `{"port":4,"camer":{"mode":19,...}}`(0x13=19);点返回蜂鸣+回菜单无残留。后续 AI 阶段只翻 `_DETECTION_ENABLED=True` + `_channels_for` 附加 chn2 + on_frame 填 slots,脚手架结构不变。

## 2026-06-30 主菜单 GC 后卡死真根因定位（坑#18 变体）+ 修复

### 根因（彻底定位）

主菜单滚动/挂机 GC 后卡死的真根因:**开机 `main()` 调 `_read_next_script()` → `open("/sdcard/CamerAi/.next_script")` → 文件不存在抛 ENOENT 异常 → 污染 K230 FATFS/全局状态 → 后续 Display/MediaManager/LVGL 在 GC 后撞上污染状态卡死。**

这是**坑#18 的变体**(坑#18 原是 AI 线程内 open ENOENT 污染)。probe 当 main.py 直跑不死,正是因为它**不读 `.next_script`、没有这次 open 异常**。

### 定位过程（systematic-debugging 收敛二分）

1. DurUI probe 二分(2026-06-29)排除 MainMenu UI,确认显示栈相关。
2. init_menu 切 DurUI 栈(DIRECT+无sensor+无OSD2+铺黑+flush不清缓冲)+ probe-style touch + 去 color_format → 有画面但仍 GC 后死。
3. host_tick/BootSplash/touch 单变量排除 → 仍死。
4. PROBE_DIRECT:main.py 直接调 probe.main() → 仍死;但 probe 当 main.py 直跑不死。
5. 最小启动器二分:`main.py = 两行 import probe+probe.main()` → **不死**。
6. 逐步加回:模块级 import → 不死;main()函数+try/except → 不死;reset 框架函数定义 → 不死;`_read_next_script()`+分支 → **死**。
7. `_read_next_script` 不 open(直接 return None)→ 不死;用 os.stat 预检查 → 不死。

→ 锁定 `_read_next_script` 的 open ENOENT 污染。

### 修复

`main.py:_read_next_script` 改 os.stat 预检查(同坑#18 解法):文件不存在直接 return None,不触发 open 异常。

```python
def _read_next_script():
    try:
        os.stat(NEXT_SCRIPT_PATH)
    except Exception:
        return None
    try:
        with open(NEXT_SCRIPT_PATH, "r") as f:
            cid = f.read().strip()
        return cid if cid else None
    except Exception:
        return None
```

### 顺带保留的改进（实验中验证无害）

- `core/app_runtime.py`:`init_menu` 切 DurUI 显示栈(`_init_menu_display_and_media` 无 osd_num + DIRECT + opaque_bg 铺黑 + flush 主菜单分支不清缓冲 + `_init_menu_touch` probe 风格 + `_lvgl_init` opaque_bg 时不设 color_format)。脚本模式 `init_app` 保持 FULL+OSD2 不变。
- `ui/main_menu.py`:滚动视觉 DurUI 风格(`apply_scroll_visual` 距离驱动 transform_zoom/opa/pos,无 Python lv.anim_t)+ style/event callback 保活 + 诊断开关默认关。

### 验证

- AST 契约 `tests/test_main_menu_cutover_ast.py` 6 PASS(含 `test_read_next_script_uses_stat_precheck` 固化根因修复 + `test_main_has_no_probe_experiment_switches` 防实验开关残留)。
- 板端:probe_launcher 二分确认 os.stat 预检查后 GC 不死。
- 待板端验证:完整 main.py 正常流程(BootSplash + host_tick + init_menu DurUI 栈)GC 后不死 + 进脚本/返回主菜单正常。

### 板端验证通过（2026-06-30）

用户确认:完整正常流程(BootSplash + host_tick + DurUI 栈 + os.stat 修复 + 诊断开)下,主动 GC 后**不死机**,完美修复。根因(open ENOENT 污染)+ 修复(os.stat 预检查)闭环。诊断已关回 `MENU_DIAG_MEM=False`。记忆 `camerai-k230-pitfalls.md` 坑#18 根因B 已扩展记录主菜单根因,坑#20 因果链标记闭合。

### 部署清单（本轮）

- `main.py`(`_read_next_script` os.stat 修复 + 清理所有 PROBE 实验开关 + 恢复正常 reset 框架)
- `core/app_runtime.py`(init_menu DurUI 栈)
- `ui/main_menu.py`(滚动视觉 + 保活 + 诊断关)
- `main_menu_durui_probe.py`(留作参照,可不部署)

### 教训

- 主菜单 GC 卡死排查耗时极长,因元凶(open ENOENT)与症状(GC 后卡)分离太远,且显示栈相关假设(sensor/OSD2/DIRECT/flush)全是烟雾弹。最终靠"最小启动器逐步加回"的收敛二分才定位。
- 坑#18 已知 open ENOENT 污染,但未推广到所有开机期 open 调用。`_read_next_script` 是开机第一个 open,应优先做 os.stat 预检查。
- 记忆 `camerai-k230-pitfalls.md` 坑#18 应扩展:所有运行期/开机期 open 不存在文件都会污染,须 os.stat 预检查。



- 实验：新建 `main_menu_durui_probe.py`，逐行照搬 DurUI 显示栈（LCD: Display.ST7701 不传 osd_num + MediaManager + DIRECT 双缓冲铺不透明黑 + flush_cb 单层 show_image 不指定 layer/不清缓冲；Touch TOUCH(0)+indev；buzzer PWM0 pin60；3ms 主循环），复用现有 `ui.main_menu.MainMenu` 卡片 UI。板端把 probe 当 main.py 跑（开头加 sys.path.insert /sdcard/CamerAi 解决 import core/ui）。
- 结果（用户板端确认）：菜单有画面 + seq=5 `proactive gc end mem=3615424` 后 **GC 不卡**，seq=6/7 mem 稳定在 3.5M 不再单调下降。`[probe-diag] seq=6 mem=3607648 / seq=7 mem=3599616`。
- 二分结论 A：**显示栈是元凶**。`app_runtime.init_menu` 的显示链路（配 sensor + osd_num=2 + FULL + flush 清非活跃缓冲）与 GC 冲突；DurUI 栈（无 sensor + 无 OSD2 + DIRECT + flush 不清缓冲）稳定。MainMenu UI、滚动动画、Python 对象保活均无关。
- 设计/计划：`docs/superpowers/specs/2026-06-29-main-menu-durui-probe-design.md`、`docs/superpowers/plans/2026-06-29-main-menu-durui-probe.md`。TDD Task1（6 AST 契约 RED，commit 9903698）+ Task2（probe 实现 GREEN，commit 9f4acc5）+ Task3（板端二分）。
- 下一步：把 `app_runtime.init_menu` 显示链路正式切换为 DurUI 栈（probe 已验证可行的 LCD/lvgl_init/flush_cb 三件套），脚本模式 init_app 保持 FULL+OSD2 不动。需新开 spec/plan。



- 触发：主菜单 mem 持续下降，滚动时下降更快，耗尽后 GC/死机；DurUI 对照可自动 GC 回收且主动 `gc.collect()` 不死。
- 设计/计划：
  - `docs/superpowers/specs/2026-06-29-main-menu-scroll-memory-stability-design.md`
  - `docs/superpowers/plans/2026-06-29-main-menu-scroll-memory-stability.md`
- 当前改动：
  - `tests/test_main_menu_memory_ast.py` 新增源码契约测试，先 RED：4 项失败，命中缺 `apply_scroll_visual`、`_on_scroll` 仍走旧 `_update_snap`、源码仍有 `_animate_geometry` / `set_custom_exec_cb`。
  - `ui/main_menu.py` 改为 DurUI 风格距离驱动滚动视觉：`_on_scroll -> _apply_scroll_visuals`，每张卡 `apply_scroll_visual(t)` 只更新已有对象的 `transform_zoom` / `opa` / `pos`，删除滚动几何 `_animate_geometry` 与 `set_custom_exec_cb`。
  - 点击反馈 `press_animation()` 也改为瞬时反馈，避免残留 Python 自定义动画回调触发源码契约。
  - 新增 `MENU_DIAG_MEM=False`、`MENU_DIAG_FORCE_GC_AT_SEQ=0` 诊断开关，默认关闭；可板端临时开启打印 mem。
- 验证：
  - `pytest` 未安装：`pytest ...` 与 `python -m pytest ...` 均不可用。
  - 用简易 Python runner 执行 `tests/test_main_menu_memory_ast.py`：先 4 FAIL（RED），实现后 4 PASS（GREEN）。
  - `python -m compileall ui/main_menu.py tests/test_main_menu_memory_ast.py` PASS。
- 追加根因假设与修复 B：style 保活后用户板端仍在 `proactive gc end` 后卡死，style 假设排除。对照 DurUI：`self._scroll_cb = self._on_scroll_any` 后再 `add_event`，而本项目直接 `add_event(self._on_scroll/...)`，`_CardSlot` 点击回调也直接传 bound method。高风险为 LVGL C 侧持有 Python callback 指针但 Python bound-method 未保活，GC 后下一次事件分发访问悬空 callback。TDD 新增 `test_main_menu_keeps_lvgl_event_callbacks_alive`，先 RED，修复为 `self._scroll_cb/self._scroll_end_cb/self._click_cb` 保活后 GREEN。验证：简易 runner 4 PASS；`python -m compileall main.py ui/main_menu.py tests/test_main_menu_memory_ast.py` PASS。
- 追加根因假设与修复 C（架构对齐 DurUI）：callback 保活后用户板端仍在 `proactive gc end mem=3548896` 后卡死，连续 3+ 局部修复失败，质疑架构。判定主菜单显示链路与 DurUI 不一致：本项目主菜单配 sensor + `osd_num=2` + FULL + flush 清缓冲；DurUI 稳定版是 LCD-only（无 sensor + 无 OSD2 + DIRECT + flush 不清缓冲）。
  - `core/app_runtime.py`：新增 `_init_menu_display_and_media()`（Display.init 不传 osd_num=2）；`init_menu()` 不再 `_config_sensor`、改用 `lv.DISP_RENDER_MODE.DIRECT`；`_flush_cb()` 加 `category_id=="main_menu"` 分支：`show_image(draw_buf)` 不指定 layer、不清缓冲，脚本分支保留 OSD2+清缓冲。`init_app()`（脚本模式）不动。
  - 新增 `tests/test_main_menu_runtime_ast.py` 3 项契约，先 RED 后 GREEN。
  - 验证：简易 runner 跑 `test_main_menu_runtime_ast.py`+`test_main_menu_memory_ast.py` 共 7 PASS；`python -m compileall main.py core/app_runtime.py ui/main_menu.py tests/test_main_menu_runtime_ast.py tests/test_main_menu_memory_ast.py` PASS。触摸 `hw/touch.py` 独立 I2C 不依赖 sensor，主菜单无 sensor 安全。
  - 本轮部署：`core/app_runtime.py`（核心）+ `main.py` + `ui/main_menu.py`。待板端验证 `gc end` 后能否继续滚动。


## 2026-06-18 face_detect 最小基线增量恢复 Step1-2 + 颜色根因（坑#17）

- 文件：`scripts/face_detect/app.py`
  - Step 1：加绿色十字架（两条相交线，中心 320,240，arm=20）。
  - Step 2：`draw_result` 增 `recognition_results` 参数 + `BOX_COLORS`(4 ID)+`BOX_UNKNOWN` 白 + `draw_string_advanced` ID 标签。
  - **根因修复**：`draw_line`/`draw_rectangle` 在 Sensor.RGB888 上 color 元组按 **(A,B,G,R)** 解释（非 (R,G,B,A)）。新增 `_draw_color(hex)` → `(0xFF,B,G,R)`。绿色 `(0,255,0,255)` 曾显示紫色（G 落 B 位）。
- 文件：`tests/test_face_detect.py`
  - 新增 `test_draw_color_uses_abgr_tuple_order`（host stub 板端模块，验证 `_draw_color` 输出）。
  - 调整十字架测试为 2 线相交。
- 记忆：`camerai-k230-pitfalls.md` 加坑#17（draw 颜色 ABGR）。

验证：
- 已执行：`python tests/test_face_detect.py` → 8/8 PASS。
- 板端：Step1/2 流畅不卡；颜色修复待板端确认（绿色十字+白色框）。

修复 crash：`CROSSHAIR_COLOR` 曾是 tuple `(0,255,0,255)` 但被 `_draw_color()` 当 hex 做 `>>` → `TypeError: unsupported types for __rshift__: 'tuple','int'`（fc=1 第一帧崩）。改为 hex `0x44CC44` 统一走 `_draw_color`。
颜色通道顺序结论（(A,B,G,R)）重新论证：官方 main2.py `(255,0,0,255)` 画红框（红=意图），(R,G,B,A) 与 (A,B,G,R) 对此例都给红，无法区分；但本板 `(0,255,0,255)` 绿→紫，仅 (A,B,G,R) 成立（R=255,B=255=品红）。结论保留。

下一步：
- 板端确认颜色正确后 → Step 3 face_db 读取。

## 2026-06-18 face_detect Step 3 face_db 读取

- 文件：`scripts/face_detect/app.py`
  - 新增全局 `db_features = {}`。
  - `face_det_thread` 启动期（`config_preprocess` 后、`while running` 前）调 `face_db.init_features()` 读入特征库到内存。仅读不推理。
  - 与 anchors 同位置（snapshot 循环前的文件 I/O 安全窗口）。
- 文件：`tests/test_face_detect.py`
  - 放宽 `test_face_detect_baseline_has_no_camerai_ui_dependencies`：允许 `core.face_db`（Step 3 sanctioned）。
  - 新增 `test_face_detect_loads_face_db_in_ai_thread`（验证 import + init_features 在 while 前）。

验证：
- 已执行：`python tests/test_face_detect.py` → 9/9 PASS。
- 板端：Step 3 初版（init_features 放 AI 线程）**一运行就卡**，与历史概率卡死同症状。

修复（坑#18）：`face_db.init_features()` 从 AI 线程移到 `run()` 主线程、`_thread.start_new_thread` 之前。根因：AI 线程 start 后主线程立即进 task_handler DMA 循环，AI 线程内文件 I/O 与 display DMA flush 竞争死锁（坑#2 跨线程变体）。AI 线程内零文件 I/O。
- 测试改为 `test_face_detect_loads_face_db_in_main_thread_before_ai`（验证 init 在 run() 主线程、start_new_thread 前，且不在 face_det_thread）。
- 记忆：坑#18 写入 `camerai-k230-pitfalls.md`。

下一步：
- 板端确认 Step 3（主线程版）不卡 → Step 4 face_reg 模型加载（只 load 不 run，同样须主线程）。

补充：Step 3 主线程版仍 fc~30 渐进卡。对照实验（注释 init_features 调用）不卡 → 锁定 init_features 内部。根因 B：`open()` 对不存在文件抛 ENOENT 异常污染 K230 FATFS/DMA 状态 → AI 线程每帧累积 fc~30 卡死。改用 `os.listdir(dir)` 一次列目录、文件不在则跳过（0 face 零 open），板端不卡。MicroPython 无 `os.path`。
- 文件：`core/face_db.py` `init_features` 改 listdir 预检查。
- 记忆：坑#18 扩展为双约束（根因A 线程时机 + 根因B open 异常污染）。
- 待验证：有注册人脸时 open().read() 是否也卡（Step 5/7 复测）。

## 2026-06-18 face_detect Step 4 face_reg kmodel 加载（只 load 不 run）

- 决策（用户）：用标准 `face_recognition.kmodel`（128 维，对齐官方 main2.py），弃 mobile（512 维）。
- 文件：`core/face_db.py` — `EXPECTED_BYTES` 512*4 → 128*4（512 字节）。
- 文件：`scripts/face_detect/app.py`
  - 新增 `FaceRegistrationApp(AIBase)`：构造即加载 kmodel 到 NPU。`config_preprocess` 留 NotImplementedError（Step 5 填 umeyama+affine），`postprocess` 返回 results[0][0]。
  - 全局 `face_reg = None`。
  - `run()` 主线程、face_db 后、AI 线程前：构造 `FaceRegistrationApp`（坑#18）。只 load 不 run。gc.collect 后打印 mem。
- 文件：`tests/test_face_detect.py` — 新增 3 测试。
- 验证：`python tests/test_face_detect.py` → 12/12 PASS。
- 板端待验证。**关键风险**：backup 历史记载"2 kmodel 占 NPU 池致 fc~35 卡"。官方 main2.py 单线程 2 kmodel 稳定。本架构双线程是否复现 fc~35 卡 = Step 4 板端核心。
- 下一步：板端跑 fc=50+ → 不卡则 Step 4 通过；卡则定位 NPU 池/双线程冲突。

**Step 4 修订（kmodel 选型反转）**：板端首次用标准 `face_recognition.kmodel`（44.1MB）→ `AIBase.__init__` 死锁（停在 `calling AIBase.__init__...`）。根因：LVGL 双线程后 `mem_free` 仅 3.7MB，装不下 44MB。改回 `face_recognition_mobile.kmodel`（2.65MB，512 维）→ face_db `EXPECTED_BYTES` 改回 512*4=2048。官方 main2.py 能用标准版是因无 LVGL。
- 文件：`scripts/face_detect/app.py` reg_kmodel → mobile；`core/face_db.py` EXPECTED_BYTES → 512*4。
- 测试：`test_face_registration_app_class_exists_and_loads_mobile_kmodel` + `test_face_db_uses_512_dim_features`。
- 记忆：坑#19（LVGL 双线程须 mobile kmodel）。
- 下一步：板端验证 mobile 加载通过（`AIBase.__init__ done` + `face_reg kmodel loaded`）后跑 fc=50+。

## 2026-06-18 face_detect Step 5 face_reg.run 识别（每帧最大人脸）

- spec：`docs/superpowers/specs/2026-06-18-face-detect-step5-recognition-design.md`
- 决策（用户）：每帧只识别最大人脸（按 w*h 面积）；阈值 0.75（官方默认）；识别在 AI 线程（NPU 同线程）。
- 文件：`scripts/face_detect/app.py`
  - `FaceDetectionApp.postprocess`：恢复返回 `(boxes, landms)`（原只返回 boxes 丢了 landms，对齐官方 main2.py:81）。
  - `FaceRegistrationApp`：实现 `config_preprocess`（umeyama+affine，从 backup 移植 `_get_affine_matrix`/`_image_umeyama_112`/`_svd22`）+ `umeyama_args_112` 常量。删 NotImplementedError。
  - 新增模块级 `database_search(feature, db_features, threshold)`：余弦比对，空库/低阈值/坏特征→None。
  - `face_det_thread`：解包 `det_boxes, landms = face_det.run`；选最大脸 `max_i`；`face_reg.config_preprocess(landms[max_i])` + `face_reg.run` + `database_search`；组装 `recognition_results=[(max_i, matched_id)]`；try/except 保护。
  - `draw_result`：改用 `rec_map = {det_index: matched_id}` 查表（原按 det 顺序索引，不适用"只识别最大脸"）。
  - 顶部 `import math`（_svd22 用）。
- 文件：`tests/test_face_detect.py` — 新增 5 测试（postprocess 两值、config_preprocess 已实现、database_search 空库 None、face_det_thread 识别最大脸、draw_result rec_map）+ `_load_app_module` host stub 辅助。
- 验证：`python tests/test_face_detect.py` → ALL PASS。
- 板端待验证：跑 fc=50+，0 face 时全白框不卡；total took 是否仍 18-20ms 量级（每帧多 1 次 face_reg.run）。
- 下一步：板端通过 → Step 6 UART 上送。注册人脸（Step 7）后实测阈值调优。

## 2026-06-16 人脸识别启动卡在 FaceReg AIBase 初始化

- 文件：`scripts/face_detect/app.py`
  - 将人脸识别模型从 `/sdcard/examples/kmodel/face_recognition.kmodel` 切换为 `/sdcard/examples/kmodel/face_recognition_mobile.kmodel`，用于验证 46MB 大模型在 LVGL 运行后加载导致卡死的假设。
- 文件：`hw/lcd.py`
  - 恢复启动期声明 chn2 AI 推理通道：`Sensor.XGA` + `Sensor.RGB888` + `self.ai_chn = CAM_CHN_ID_2`。
- 文件：`tests/test_face_detect.py`
  - 新增 AST 回归测试，要求 FaceDetect 使用 `face_recognition_mobile.kmodel`，并禁止运行期加载 46MB `face_recognition.kmodel`。

验证：

- 已执行：`python tests/test_face_detect.py`
- 结果：全部通过（8/8 PASS）。

下一步：

- 上板启动人脸识别 APP，确认日志是否能继续输出到 `[FaceReg] AIBase.__init__ done` 和 `[FaceDetect] D — face_reg model loaded`。
- 若 mobile 模型可启动，再观察首帧 AI 推理和人脸框绘制是否正常。

## 2026-06-16 11:46:35 人脸识别 mobile 模型仍卡住后的二次定位

- 现象：上板日志显示已切到 `/sdcard/examples/kmodel/face_recognition_mobile.kmodel`，但仍停在 `[FaceReg] calling AIBase.__init__...`。
- 判断：46MB 大模型不是唯一根因；更可能是第一个模型加载后立即 `config_preprocess()` 构建 AI2D 预处理缓冲，占用/碎片化 KPU/AI2D 内存池，导致第二个 kmodel 的 `load_kmodel()` 卡住。
- 文件：`tests/test_face_detect.py`
  - 新增 AST 回归测试 `test_face_detect_loads_both_kmodels_before_ai2d_build`，要求先加载两个 kmodel，再构建 `face_det.config_preprocess()`。
  - RED 验证：执行 `python tests/test_face_detect.py`，该测试按预期失败。
- 文件：`scripts/face_detect/app.py`
  - 调整 `_init_ai_models()` 初始化顺序：`FaceDetApp` 只先加载 kmodel；随后加载 `FaceRegistrationApp`；两个 kmodel 都加载成功后，再调用 `self._face_det.config_preprocess()`。
  - 保留诊断日志，新增 `A — face_det kmodel loaded`、`D — face_reg model loaded`、`E — face_det preprocess built` 分段。

验证：

- 已执行：`python tests/test_face_detect.py`
- 结果：全部通过（9/9 PASS）。

下一步：

- 上板启动人脸识别 APP，确认是否能输出到 `[FaceReg] AIBase.__init__ done`、`[FaceDetect] D — face_reg model loaded` 和 `[FaceDetect] E — face_det preprocess built`。
- 若仍卡在第二个 `AIBase.__init__`，则说明问题不是模型大小或 AI2D 预处理缓冲顺序，而是运行期加载第二个 kmodel 与当前显示/LVGL/MediaManager 生命周期冲突，需要验证“启动期预加载/延迟进入 LVGL 前加载”的架构方案。

## 2026-06-16 11:49:47 人脸识别卡在 face_det 后的三次定位

- 现象：调整为先加载两个 kmodel 后，上板日志停在 `[FaceDetect] A — face_det kmodel loaded`，没有继续到加载 face_reg。
- 判断：新卡点位于 `A` 后的运行期 `gc.collect()`；这与 K230 坑 #10（UI/LVGL 运行期禁止 `gc.collect()`，可能触发 LVGL 终结器/DMA 死锁）一致。
- 文件：`tests/test_face_detect.py`
  - 新增回归测试 `test_face_detect_ai_init_does_not_collect_gc_at_runtime`，禁止 `_init_ai_models()` 内调用 `gc.collect()`。
  - RED 验证：执行 `python tests/test_face_detect.py`，该测试按预期失败。
- 文件：`scripts/face_detect/app.py`
  - 删除 `_init_ai_models()` 中 `gc.collect()` 和未使用的 `import gc`。
  - 保留诊断日志，替换为 `[FaceDetect] B — skip runtime gc before face_reg load`，用于确认是否越过原卡点。

验证：

- 已执行：`python tests/test_face_detect.py`
- 结果：全部通过（10/10 PASS）。

下一步：

- 上板启动人脸识别 APP，确认是否能从 `[FaceDetect] A — face_det kmodel loaded` 继续到 `[FaceDetect] B — skip runtime gc before face_reg load`、`[FaceReg] AIBase.__init__ done`。
- 若仍卡在 face_reg 的 `AIBase.__init__`，再继续验证运行期第二个 kmodel 加载与 LVGL/Display/MediaManager 生命周期冲突。

## 2026-06-16 11:56:12 人脸识别启动后无画面 / snapshot chn0 failed(3)

- 现象：模型已能完整加载并进入 `on_enter: done`，随后每帧报 `AI inference error: sensor(0) snapshot chn(0) failed(3)`，屏幕无相机画面。
- 根因：`FaceDetectApp.on_enter()` 没有像 Camera APP 一样调用 `lcd.ensure_sensor_running()`，导致共享 Sensor 未启动取流；`on_frame()` 随后直接 `sensor.snapshot()` / `get_ai_frame()`，所以 chn0 snapshot 失败。同时透明预览区需要先清 LVGL 双缓冲，避免 OSD2 旧像素挡住 OSD1 画面。
- 文件：`tests/test_face_detect.py`
  - 新增回归测试 `test_face_detect_on_enter_starts_shared_sensor`，要求 `FaceDetect.on_enter()` 调用 `clear_framebuffers()` 和 `ensure_sensor_running()`。
  - RED 验证：执行 `python tests/test_face_detect.py`，该测试按预期失败。
- 文件：`scripts/face_detect/app.py`
  - 在 `on_enter()` 中，`_init_db()` / `_init_ai_models()` 前调用 `self.ctx.lcd.clear_framebuffers()` 和 `self.ctx.lcd.ensure_sensor_running()`。
  - 新增日志 `[FaceDetect] shared sensor running`，用于确认取流启动。

验证：

- 已执行：`python tests/test_face_detect.py`
- 结果：全部通过（11/11 PASS）。

下一步：

- 上板启动人脸识别 APP，确认日志出现 `[FaceDetect] shared sensor running` 后不再持续报 `snapshot chn(0) failed(3)`。
- 若仍无画面但不报 snapshot，则继续定位 OSD1/OSD2 透明层或 AI chn2 帧格式问题。

## 2026-06-16 人脸识别有画面后运行一段时间卡住 — 双 snapshot 死锁

- 现象：on_enter 成功后画面正常出现，人脸框也已渲染，但运行一会后画面卡死。
- 根因：`FaceDetectApp.on_frame()` 每帧调用两次 `sensor.snapshot()`：先 chn0 取预览帧推 OSD1，再 `get_ai_frame()` 取 chn2 做 AI 推理。两个通道共用同一个 sensor 捕获引擎，在同一帧内两次 snapshot 会导致 buffer 竞争，运行一段时间后缓冲区耗尽 → 永久死锁。
- 正解（参考官方 face_det demo）：单帧双用——一次 snapshot（chn0/VGA）同时用于 OSD1 预览和 AI 推理。AI2D 将 VGA 输入 resize 到模型分辨率。
- 文件：`tests/test_face_detect.py`
  - 新增回归测试 `test_face_detect_on_frame_uses_single_snapshot`，禁止 `on_frame()` 调用 `get_ai_frame()`（避免第二次 snapshot）。
  - RED 验证：该测试按预期失败。
- 文件：`scripts/face_detect/app.py`
  - 重写 `on_frame()`：一次 `sensor.snapshot()` 后先推送 OSD1 预览，再将同一帧转 CHW numpy 喂 AI 推理。删除了原来的 `get_ai_frame()` 调用和独立的预览/AI 分支。
  - 新增静态方法 `_img_to_chw(img)`：将 snapshot Image (RGB888) 转为 (1,3,H,W) CHW numpy，替代 `get_ai_frame()` 的 chn2 snapshot 逻辑。

验证：

- 已执行：`python tests/test_face_detect.py`
- 结果：全部通过（12/12 PASS）。

下一步：

- 上板启动人脸识别 APP，确认长时间运行不再卡死。
- 观察帧率和人脸框更新是否正常。
- 若仍卡死，再排查 AI 推理耗时是否阻塞主循环导致看门狗/硬件复位。

## 2026-06-16 人脸识别仍运行一会卡死 — 缺每帧 GC（原生缓冲池耗尽）

- 现象：单 snapshot 改完后，画面/人脸框正常，但运行一会仍卡死。
- 根因（系统化调试 + 官方 demo 比对确认）：`on_frame()` 整个推理循环**从不 `gc.collect()`**。每帧分配大量 NPU/AI2D/numpy 原生缓冲——`img.to_rgb888()`+`to_numpy_ref()`+`.copy()`（~920KB/帧）、`face_det.run()`、每张脸 `face_reg.config_preprocess()` 内的 `ai2d.build()`（每次重建 AI2D 工作区）、`face_reg.run()`。这些原生内存对 MicroPython GC 不可见，自动 GC 不触发 → 池累积耗尽 → 下次 `run()`/`snapshot()` 永久阻塞。官方所有 AI demo 循环结构均为 `get_frame → run → draw → gc.collect()`（main2.py:386、人脸/手掌/人体检测 demo 一致），每帧 GC 是 K230 NPU 推理硬性模式。相机 APP 不卡是因为它只 snapshot+show_image，零 NPU/AI2D 分配。
- 耦合约束（坑 #10）：原 `_update_face_boxes()` 每帧 create/delete 人脸框 LVGL 对象。若直接加每帧 GC，会触发这些对象的 pending 终结器碰 DMA 死锁。官方 demo 用 `image.Image` 画 OSD，无 LVGL churn 才能安全每帧 GC。
- 正解（一次解决两问题）：人脸框改预建对象池。
- 文件：`tests/test_face_detect.py`
  - 新增 `test_face_detect_on_frame_collects_gc_each_frame`：要求 `on_frame` 每帧 `gc.collect()`。
  - 新增 `test_face_detect_update_boxes_does_not_churn_lvgl_objects`：禁止 `_update_face_boxes` create/delete LVGL 对象。
  - 新增 `test_face_detect_has_prebuilt_face_box_pool`：要求 `_build_face_box_pool()` 且 `_build_ui` 调用它。
  - RED 验证：3 个新测试按预期失败。
- 文件：`scripts/face_detect/app.py`
  - 新增 `_build_face_box_pool()`：UI 构建期一次性预建 4 组（矩形+ID标签），默认隐藏。`_build_ui` 调用。
  - 重写 `_update_face_boxes()`：仅复用池对象 reposition/set_color/hide，绝不 create/delete。
  - `on_frame()` 末尾加 `_gc.collect()`（`_gc` 已在设备导入块）。
  - `__init__` 的 `_face_boxes` 列表换成 `_box_pool`；`_destroy_ui` 改删池中 rect（连带删子 label）。

验证：

- 已执行：`python tests/test_face_detect.py`
- 结果：全部通过（15/15 PASS）。

下一步：

- 上板启动人脸识别 APP，确认长时间连续运行不再卡死（重点：放置多张脸、长时间观察）。
- 串口可观察 `gc.mem_free()` 是否稳定不再单调下降。
- 若仍卡死，排查 AI 推理单帧耗时是否过长触发看门狗，或 `ai2d.build()` 每帧每脸重建是否需进一步缓存。
---

## 2026-06-17 — 新增官方风格基线测试脚本 test_face_baseline.py

- 时间：2026-06-17
- 文件：`test_face_baseline.py`（新建，项目根目录）
- 内容：systematic-debugging 基线对照脚本。逐字照搬官方
  `examples/21-AI-With-Others/ai_lvgl.py` 的稳定双线程结构（face_det_thread
  + 主线程 `time.sleep_ms(lv.task_handler())` + 每帧 `gc.collect()` + flush_cb
  `time.sleep(0.01)`），仅适配 640×480 屏（官方 800×480）。不加任何 CamerAi
  附加逻辑（无 UI/注册/UART/预热/对象池/跨帧持久 view）。
- 背景：face_detect 双线程改造后，gc.collect() 仍偶发卡死（首个 NPU 帧 pre-gc
  打印、post-gc 不打印；概率性，有时第 7 帧卡有时跑过）。已验证排除：
  show_image（双线程+flush sleep 解决）、冷启动硬锁（预热 6 帧解决）、
  跨帧持久 view（去 _current_frame_data 降低概率未根治）、UART 引脚冲突
  （关 UART 仍卡）。在自有代码上反复打补丁多轮未根治，改用官方稳定基线
  增量对照法。
- 用法：复位板子后直接运行本脚本（不跑 main.py，显示栈只 init 一次）。
  - 稳定跑几百帧 → 板子+官方结构 OK，CamerAi 卡死在附加逻辑，逐项加回定位
  - 也卡 → 板子/sensor 配置问题
- 验证：主机端 `python -c "import ast; ast.parse(open('test_face_baseline.py',encoding='utf-8').read()); print('OK')"` → AST OK。板端待用户运行验证。

- 修正（2026-06-17）：`ALIGN_UP` 不在 `libs.Utils`（官方靠 `from libs.PipeLine import *` 间接获得，PipeLine 内部用但未定义，实来自 media.*）。基线脚本改为内联定义 `ALIGN_UP`，import 行去掉它。`letterbox_pad_param`/`ScopedTiming` 仍在 `libs.Utils`。CanMV IDE 直接运行报 `ImportError: can't import name ALIGN_UP` 已修。AST OK。

- 2026-06-17 补充：板端验证 test_face_baseline.py（官方裸跑结构）**流畅稳定**。但同结构通过 CamerAi 主菜单架构启动（face_detect `_ai_loop` 改基线最小版）→ fc=720 卡死，之后断电/RESET 都卡 fc=1（暖机无关）。说明问题在 CamerAi 架构（lcd Sensor 配置/常驻 Display/MediaManager）与官方裸跑的差异，不在 AI 线程代码本身。
- 新增 `test_face_baseline_camerai_sensor.py`：基线裸跑结构但用 CamerAi lcd.py 的 Sensor 配置（`Sensor(1280,960)` + 3通道 chn0 VGA/chn1 SXGAM/chn2 XGA 1024×768 RGBP888），隔离"Sensor 配置"变量。若裸跑也卡 → Sensor 配置问题；若稳定 → CamerAi 常驻架构问题。AST OK，板端待验证。
- 同步：face_detect `_ai_loop` 临时改为基线最小版（照搬 face_det_thread，去附加逻辑），补 `CAM_CHN_ID_0` 导入。验证后恢复。

- 2026-06-17 补充：用户确认"第一次进 face_detect 就卡"（非反复进出累积）+ 裸跑 test_face_baseline_camerai_sensor.py fc=3060 稳定 → 排除 Sensor 配置与脚本切换累积，锁定 **main.py 启动期污染**（主菜单 UI/图标预读/字体加载消耗或碎片化 RAM，进 face_detect 后可用连续内存不足致 NPU+gc 卡死）。两脚本加 gc.mem_free() 打印对比：face_detect on_enter vs 裸跑 init done。AST OK。

- 2026-06-17 补充：内存对比数据——裸跑 mem_free=3875744(稳定)，face_detect on_enter mem_free=3028768(卡死)，差~850KB。850KB 主要是图标 bytes(icon_cache + menu._icon_cache)受坑#2 保护必须常驻(运行期不能文件I/O)，不可释放。同进程内腾内存行不通(图标数据不能清)。on_enter 加 gc.collect 后再测 mem_free，区分"内存量不足"vs"碎片"。AST OK。

- 2026-06-17 补充：gc 后 mem_free=3465504（回升~430KB），仍卡 fc=1。**排除内存量不足**（3.46MB 够 face_det）。锁定 main.py 主循环差异：CamerAi 固定 sleep_ms(5) 让 LVGL flush 高频在途 vs 裸跑 time.sleep_ms(lv.task_handler()) 用返回值（空闲 sleep 久，flush 低频）。改 main.py 主循环对齐官方（sleep 用 task_handler 返回值）+ 恢复 script_runner 握手 poll。AST OK。

- 2026-06-17 补充：main.py 主循环改 time.sleep_ms(task_handler) 仍卡 fc=1，排除主循环 sleep。新增 test_face_baseline_with_menu_sim.py：在裸跑稳定脚本基础上，init 后模拟 CamerAi 主菜单期（占~800KB + 建9个LVGL对象 + 跑3秒task_handler + hide不删），再启 AI 线程。区分"主菜单污染"vs"lcd 常驻栈复用"。AST OK。

- 2026-06-17 决定性发现：test_face_baseline_with_menu_sim.py 卡在 task_handler 循环（建9个LVGL对象后跑 time.sleep_ms(lv.task_handler()) 卡死）。不带主菜单模拟的裸跑 sensor.run()+空屏 task_handler 能跑 fc=3060。**根因锁定：sensor 取流 DMA 与 LVGL task_handler flush DMA 争用——当 LVGL 有非空内容(对象)要 flush 时卡死，空屏 flush 不卡。** CamerAi 主菜单期 sensor 没 run 所以不冲突；face_detect 期 sensor.run + 顶/底栏 LVGL 对象 → flush 卡。改模拟脚本：LVGL 对象建后立刻 HIDDEN(不 flush 显示)，验证"flush 非空内容"是否致卡。AST OK。

- 2026-06-17 补充：LVGL 对象全 HIDDEN 仍卡 task_handler → 排除"flush 非空内容"。新假设：sensor.run() 后长时间不 snapshot，缓冲池(buffer_num 4)被产帧填满 → sensor DMA 卡 → task_handler 撞上。解释 CamerAi face_detect 卡：on_enter ensure_sensor_running(sensor.run) → _init_ai_models 加载2个kmodel耗时数秒 → 首次snapshot前缓冲满 → 卡。裸跑 camerai_sensor: sensor.run→sleep100ms→立刻snapshot，不卡。with_menu_sim 加 warmup snapshots(消费8帧)后再跑 task_handler 验证。AST OK。

## 2026-06-18 Task 1.1 — 新建 core/app_runtime.py 公共 init 模块（reset 框架基座）

- 背景：CamerAi 从同进程脚本切换改为 reset 切换架构。本 Task 新建公共 init 模块 +
  契约测试，是 reset 框架阶段 1 骨架的第一块。后续 main.py 启动器按 .next_script
  决定调 init_menu（主菜单）或 init_app(category_id)（脚本模式），脚本退出调 cleanup。
- 文件：`tests/test_framework.py`（新建）
  - reset 框架契约测试：`test_app_runtime_class_exists`（AppRuntime 有
    init_menu/init_app/cleanup）、`test_app_runtime_init_app_takes_category`
    （init_app 接 category_id 参数）。
  - 含 `__main__` runner（照搬 test_face_detect.py 模式：遍历 test_*，try/except 打
    PASS/FAIL，末尾 ALL PASS / N FAILED + sys.exit），可直接 `python tests/test_framework.py` 跑。
- 文件：`core/app_runtime.py`（新建，逐字照搬计划）
  - `AppRuntime` 类：每进程独立 init。封装 Display/MediaManager/sensor(按 category 配
    通道)/LVGL/host/字体/图标。
  - `init_menu(fpioa)`：主菜单模式，chn0 VGA/RGB888，预读 settings/camera/face 图标。
  - `init_app(category_id, fpioa)`：脚本模式，按 category 加 chn2(face_detect XGA
    RGBP888 planar)/chn1(camera SXGAM RGB565)，末尾 `sensor.run()` 紧贴脚本主循环
    （消费者就绪后才 run，避免缓冲满 DMA 卡死——对齐坑#17）。
  - `cleanup()`：显式 stop sensor / del draw_buf / lv.deinit / display.deinit /
    MediaManager.deinit（reset 会清，但显式调用对齐坑#14/15 每进程独立 init/deinit）。
  - `_flush_cb` 对齐官方 ai_lvgl.py disp_drv_flush_cb（末尾 `time.sleep(0.01)`）。
  - K230 约束：坑#2（首次 task_handler 前完成文件 I/O：字体/图标/kmodel）、坑#15
    （sensor 通道须 MediaManager.init() 前声明——_config_sensor 在 _init_display_and_media 前）。

验证：

- 已执行：`python -c "import ast; ast.parse(open('core/app_runtime.py', encoding='utf-8').read()); print('OK')"` → OK。
- 已执行：`python tests/test_framework.py` → ALL PASS（2/2 PASS）。
- TDD：先建 test_framework.py → 跑确认红（app_runtime.py 不存在，2 FAILED）→ 建 app_runtime.py → 跑确认绿。

下一步：

- Task 1.2：重写 main.py 为 reset 启动器（读 .next_script，主菜单/脚本模式分流，点卡片
  写 .next_script + machine.reset()），追加 main.py 契约测试。
- 板端待 Task 1.2 完成后部署 app_runtime.py + main.py 验收主菜单。

## 2026-06-18 Task 1.2 — main.py 重写为 reset 切换启动器

- 背景：Task 1.1 完成后，本 Task 把 main.py 从旧"同进程 ScriptRunner 常驻"入口重写为
  reset 切换架构的启动器（永不被覆写）。读 `.next_script` 标记决定跑主菜单还是目标脚本，
  点卡片写 `.next_script` + `machine.reset()`，脚本进程独立 `init_app` + `run(runtime)`
  + 清标记 + reset 回主菜单。对齐官方综合例程 DemoScriptRunner（machine.reset 切换），
  用 `.next_script` 标记替代覆写 main.py（防变砖）。
- 文件：`tests/test_framework.py`（追加 2 个契约测试，runner 之前）
  - `test_main_reads_next_script`：main.py 含 `next_script` + `machine.reset`（或
    `reset()`）。
  - `test_main_has_launch_writer`：main.py 含 `next_script` + `wb`/`write`（点卡片写标记）。
- 文件：`main.py`（整体替换，照搬计划代码）
  - `_read_next_script` / `_write_next_script(category_id)` / `_clear_next_script`：标记
    文件 `/sdcard/CamerAi/.next_script` 的读写删。
  - `_load_script(category_id)`：按 config 取 category 的 script 名，`__import__` 动态
    加载 `scripts.<name>.app` 模块，要求含 `run` 入口。
  - `run_menu()`：AppRuntime.init_menu → MainMenu（卡片回调写 .next_script + reset）→
    主循环 `time.sleep_ms(task_handler)`（空闲 sleep 久，flush 低频，对齐官方/坑#17）。
  - `run_script(category_id)`：AppRuntime.init_app(category_id) → mod.run(runtime) →
    `_clear_next_script` → `machine.reset()` 回主菜单。
  - `main()`：读 .next_script 分流（有标记 → 脚本模式；无 → 主菜单模式）。
  - 顶层 try/except：fatal 时 print_exception + 删 .next_script（防错误标记循环 reset）。
  - import machine 用简化形式（顶部 `import machine` + 调用处 `machine.reset()`），
    而非计划 try/except `from machine import machine`（MicroPython 的 machine 模块无
    machine 属性，try/except 形式会 fallback 到 `import machine`，等价但绕）。

验证：

- 已执行：`python -c "import ast; ast.parse(open('main.py', encoding='utf-8').read()); print('OK')"` → OK。
- 已执行：`python tests/test_framework.py` → ALL PASS（4/4 PASS）。
- TDD：先追加 2 个 main 契约测试 → 跑确认红（test_main_* 2 FAILED，test_app_runtime_*
  2 PASS）→ 重写 main.py → 跑确认绿。
- IDE 警告"无法解析导入 machine"是预期的（machine 是 MicroPython 硬件模块，Windows
  主机无此包），不影响 AST 与契约测试。

下一步：

- Task 2.1：face_detect 迁移为 `run(runtime)` 独立脚本（删 BaseScript，ctx→rt 映射）。
- 板端验收（Task 1.2）：部署 main.py + app_runtime.py 上电看主菜单正常显示。

- 2026-06-18 定位进展：face_detect reset 架构下仍偶发卡 show_image(OSD1)。隐藏顶/底栏 LVGL + 关 _draw_overlay/UART/_prev_img 后跑到 fc~24-35 仍卡（比 fc~27 晚但未根治）。新假设：Buzzer(PWM0) 硬件与 NPU/DMA/sensor 冲突。app_runtime 临时不创建 Buzzer（self.buzzer=None），main.py beep 加 None 守卫。验证 PWM 是否元凶。AST OK。

## 2026-06-18 face_detect Step 7 K2 注册人脸 + 可复用 ID 管理（TDD 全绿）

- spec：`docs/superpowers/specs/2026-06-18-face-detect-step7-register-design.md`
- plan：`docs/superpowers/plans/2026-06-18-face-detect-step7-register.md`
- 背景：Step 5 识别链路板端验证通过（0 face 全白框不卡），本步接 K2 注册人脸 + 抽可复用 id_registry 模块 + 收尾临时定位（face_detect 走完整 init_app + buzzer 接回）。
- TDD：先建 `tests/test_face_register.py`（16 测试）→ RED 全 FAIL → 逐 Task 实现转绿。
- 文件：`core/face_db.py`
  - 加 `_next_slot` 指针（1-4 循环）+ `_NEXT_SLOT_PATH=/data/fac_db/.next_slot`。
  - `_load_next_slot`/`_save_next_slot`：指针持久化（init_features 内读、register 内写，与读/写 .bin 同安全窗口）。
  - `register(feature)`：空 slot 优先填（不动指针）；4 满后轮转覆盖 _next_slot +1（方法 B）；写内存后当场 `flush_to_disk()` + `_save_next_slot()`（试法1）。
  - `clear()`：清内存 + 删 .bin + 删 .next_slot + 指针回 1（clear_disk 只删 .bin 保留，clear 供后续清除按钮）。
- 文件：`core/id_registry.py`（新建，可复用注册控制器）
  - `IdRegistry`：K2=GPIO0 输入，主线程 `poll_k2()` 软件边沿（松开→按下置 pending 一次，防按住连触发）+ 2 秒超时；AI 线程 `try_register(feature, buzzer)` 消费 pending → `face_db.register` + 蜂鸣（成功 80ms/失败 200ms）+ 清 pending。不绑死 AI 模型，后续手势/物体脚本复用。
- 文件：`scripts/face_detect/app.py`
  - 全局加 `id_registry`/`_rt`（`_rt` 暴露 runtime.buzzer 给模块级 face_det_thread）。
  - `run()`：AI 线程启动前 `IdRegistry(runtime.fpioa, pin=0)`；主循环加 `id_registry.poll_k2()`。
  - `face_det_thread`：识别段 database_search 后 `id_registry.try_register(feature, _rt.buzzer)`（复用 Step 5 特征，零额外 NPU）。
- 文件：`main.py`：删"face_detect 跳过 init_app"分支，所有 category 统一 `runtime.init_app(category_id, fpioa)`。
- 文件：`core/app_runtime.py`：删 init_app 中"face_detect 跳过 touch/fonts/services"分支；`_init_services` 恢复 Buzzer 创建（取消注释，`self.buzzer = Buzzer(...)` + set_enabled）。
- 验证：`python tests/test_face_register.py` → ALL PASS（16/16）；`python tests/test_face_detect.py` → ALL PASS；`python tests/test_framework.py` → ALL PASS。5 文件 AST 全 OK。
- 提交：6 commit（face_db register+clear / id_registry / face_detect 接入 / main 收尾 / app_runtime 收尾+Buzzer）。
- 板端待验收（核心验证点）：
  1. 进 face_detect 不卡（init_app 收尾 + buzzer 接回不引入卡死）→ 排除/确认 PWM0 元凶。
  2. 对脸按 K2：蜂鸣 80ms + 当场彩色框+ID（注册成功，内存即生效）。
  3. **试法1 核心验证**：register 当场 flush_to_disk 在 AI 线程运行期执行，验证运行期写盘是否与 display DMA flush 竞争卡死（坑#2 延伸）。卡则退化为试法2（主线程 task_handler 间隙 flush，本步不实现留 fallback）。
  4. 注册 4 张脸后第 5 次覆盖 id1（轮转 B）；退出→reset→再进，已注册人脸仍识别（持久化生效）。
- 下一步：板端验收通过 → Step 6 UART 上送；若试法1 卡 → 实现试法2 fallback。

## 2026-06-18 Step 7 收尾回退——face_detect double-init sensor 崩溃修复

- 现象：部署 Step 7 后上板进 face_detect 报 `OSError: sensor(2) is already inited`（栈：run_script → app.py:411 run → app.py:329 media_init → Sensor()）。
- 根因（systematic-debugging Phase 1-2）：Task 5 删"face_detect 跳过 init_app"分支 + Task 6 恢复 buzzer 后，`runtime.init_app` 内 `_config_sensor` 已 `Sensor()+reset()+run()`，紧接着 `face_detect.run()` 的 `media_init()` 又 `Sensor()+reset()` → Sensor 不容忍二次 init（Display.init 双调只警告，Sensor/MediaManager 直接抛）。`Display.init` 的 "Already run Display.init()" 警告印证双 init。
- 架构冲突：Step 7 "收尾"假设 init_app 与 face_detect self-init media 能共存——不能。face_detect 是 reset `run(runtime)` 裸跑风格自包含基线（`run()` 自己 media_init+lvgl_init），而 init_app 把 media 初始化和 services(buzzer) 绑一起。原"跳过 init_app"正是为避免这套双 init（记忆 camerai-face-detect-stable-config 已记）。
- 测试盲点：host 测试是 AST 字符串断言（`runtime.init_app(category_id, fpioa)` 存在），不模拟运行期 double-init，未捕获。
- 修复（选项1，用户确认）：face_detect **继续 self-init 稳定配置**，main.py 对 face_detect 跳过 init_app 但补 `runtime.fpioa = fpioa`（IdRegistry 配 K2 GPIO0 用）；buzzer=None，id_registry 静默守卫。其他脚本仍走 init_app。buzzer 接回作为单独隔离实验留后。
- 文件：`main.py`（face_detect 跳过 init_app + 设 fpioa）、`tests/test_face_register.py`（`test_main_face_detect_skips_init_app_sets_fpioa` 替代原 no-skip 测试）。
- 注：app_runtime.py 的 init_app 收尾（删 face_detect 跳过 touch/fonts/services 分支 + 恢复 Buzzer）**保留**——因 face_detect 现在根本不调 init_app，该分支不影响 face_detect 运行；恢复的 Buzzer 仅在其他脚本迁移后生效，face_detect 路径 buzzer 仍 None。若后续 buzzer 单独实验接回 face_detect，再按 §11 验证 PWM0 是否元凶。
- 验证：`tests/test_face_register.py` ALL PASS（16）+ test_face_detect + test_framework 全绿；main/app_runtime AST OK。提交 02d382e。
- 板端待验收：进 face_detect 不卡 + K2 注册彩色框+ID + 试法1 当场 flush 不卡（buzzer 仍 None，无蜂鸣反馈）。

## 2026-06-23 通用脚本模板——基础框架稳定（face_detect 卡死根因结构根除）

- 背景：face_detect fc~20-35 卡死反复发作（Step 5/7 多轮），06-18 的"稳定"靠砍 buzzer/touch 堆出来根因未除。决定搁置 face_detect，先做稳定的基础框架模板，作为后续 AI 脚本复制起点。
- 卡死根因定位：face_detect `DISP_RENDER_MODE.FULL` + **双线程**（AI 线程每帧 show_image(OSD1) + 主线程每帧 FULL 整屏刷 OSD2）= 双写者抢 display DMA。卡点乱跑（init 中段/init 末尾/fc~20）= 资源竞争/时序死锁，非确定性 bug。
- 模板核心结构（scripts/_template/app.py）：
  - `run(runtime)` 单线程主循环：snapshot → on_frame(try/except) → show_image(OSD1) → task_handler，串行。display 只有一个写者（主循环），结构上消除双写者竞争。
  - `on_frame(img)` AI 钩子（默认空），异常 try/except 隔离不杀循环 → 基础框架不被 AI 影响。
  - 顶栏（返回钮+硬编码标题"基础框架"）+ 空底栏 + 透明预览区。触摸返回：回调设 exit_flag → 主循环退出 → _destroy_ui → main.py cleanup+reset 回菜单。
- 框架改动：
  - `app_runtime._lvgl_init(render_mode=FULL)` 加参数；`init_app` 按 ui_mode 选（stream→PARTIAL，menu/page→FULL）。
  - `_channels_for` 加 `_template` 单通道 pass。
  - `main.py run_script`：非 face_detect 脚本 mod.run 返回后调 `runtime.cleanup()`（跳过 face_detect）。
  - `config/categories.json` + `scripts/_template/manifest.json` 注册 _template category。
- 板端验收 6/6 通过：
  1. 能进（连5次硬断电点进都成功）✅
  2. **画面稳：fc 持续涨不卡**（对比 face_detect fc~20，数量级提升）✅ —— 单线程结构从根上根除卡死
  3. 触摸返回稳定（连5次进出不卡）✅
  4. 退出干净（不 wedged，再进不需硬断电）✅
  5. PARTIAL/FULL 渲染都正常（板端实测单线程下两者都稳——render mode 非关键，单线程才是关键）✅
  6. **AI 异常隔离**：on_frame raise Exception，串口持续打印 error 但画面继续刷新、fc 继续涨 ✅
- 设计文档：docs/superpowers/specs/2026-06-23-script-template-design.md
- 实施计划：docs/superpowers/plans/2026-06-23-script-template.md（Task 1-8 TDD 完成，Task 9 验收通过）
- 关键结论：**单线程是消除卡死的关键，render mode 非关键**。后续 AI 脚本若在 on_frame 内起 AI 线程会重新引入双写者竞争，届时需协调。
- face_detect 现状：app.py 回退 Step 5 纯净版搁置；main.py 保留 face_detect 特殊分支（跳过 init_app + 不调 cleanup）。
- 旧孤儿：camera/settings app.py 是旧 BaseScript 架构无 run() 入口，进不去，需按模板 run(runtime) 范式改造（待开新设计）。

## 2026-06-23 git push 修复——HTTPS 被重置，切 SSH 推送

- 现象：`git push` 报 `fatal: unable to access 'https://github.com/MrHuang-0110/LBS-CAMER.git/': Recv failure: Connection was reset`。
- 根因（systematic-debugging）：HTTPS 直连 github.com 被 GFW RST 重置；本地未开代理（7890/7891/1080/10809/10808/2080 全 closed）；但 SSH 22 端口通且 key 已认证（`ssh -T git@github.com` 返回 `Hi MrHuang-0110!`），SSH over 443 也通。
- 修复：
  1. `git remote set-url origin git@github.com:MrHuang-0110/LBS-CAMER.git`（改走 SSH）→ `git push origin main` 成功 `2a094d6..6c87a4e`。
  2. 新建 `C:\Users\24160\.ssh\config`，配 `github-443` 别名（Hostname ssh.github.com / Port 443）作为 22 端口被封时的后备通道——默认不影响当前 22 通道，仅后备。
- 验证：push 成功（6c87a4e 已在 origin/main）；`ssh -T git@github.com` 与 `-p 443 git@ssh.github.com` 均认证通过。
- 下一步：日常 push/pull 直接走 SSH 即可；若某天 SSH 22 被封，`git remote set-url origin git@github-443:MrHuang-0110/LBS-CAMER.git` 切后备；恢复 HTTPS 用 `git remote set-url origin https://github.com/MrHuang-0110/LBS-CAMER.git`。

## 2026-06-23 face_detect Phase 1 单线程模板迁移设计

- 设计文档：`docs/superpowers/specs/2026-06-23-face-detect-template-phase1-design.md`
- 背景：settings/camera 已按 reset 框架验收；face_detect 仍是旧的裸跑双线程特例。历史 fc~20-35 卡死根因是 AI 线程 show_image(OSD1) + 主线程 task_handler 刷 OSD2 的双写者 display DMA 竞争。模板 `run(runtime)` 单线程主循环已板端验证能结构性规避该类卡死。
- 用户确认范围：本轮只做 Phase 1（模板单线程 + 人脸检测白框），不混入 ID 注册、特征匹配、清除记录；Phase 2-4 待 Phase 1 板端稳定后另开设计。
- 用户确认模块结构：选 A，抽 `core/face_ai.py` 保存复用 AI 类/辅助函数，`scripts/face_detect/app.py` 只负责 UI + run/on_frame 编排。
- 设计要点：
  - `main.py` 删除 `face_detect` 跳过 `runtime.init_app`/`cleanup` 的裸跑特例，统一走 reset 框架 init/cleanup。
  - `core/app_runtime.py` 保留 face_detect 的 chn0(VGA/RGB888 预览) + chn2(XGA/RGBP888 NPU 输入)，不加 chn1。
  - `scripts/face_detect/app.py` 复制 `_template`：主循环串行 `snapshot chn0 → on_frame → Display.show_image(OSD1) → lv.task_handler()`。
  - `on_frame(img)` 内取 chn2 输入、跑 `FaceDetectionApp`、把白框画到 chn0 预览帧；不得启动线程、不得 `Display.show_image`、不得文件 I/O、不得创建/删除 LVGL 对象。
  - anchors/kmodel 在 `run()` 首次 `lv.task_handler()` 前加载，遵守 K230 坑#2 安全窗口。
- 验证计划：新增 host-side AST 契约测试（face_detect 模板化、无 `_thread`/自 init、chn2 输入、on_frame 不 show_image；framework 删除 face_detect 特例；face_ai 保留 ABGR 颜色转换与 `FaceDetectionApp`）。板端验收看启动、白框检测、fc 2-5 分钟不卡、UI 不消失、返回/反复进出稳定。
- 当前状态：仅完成并提交设计文档；尚未实施。下一步按 `writing-plans` 生成 TDD 实施计划。

## 2026-06-23 face_detect Phase 1 单线程模板迁移实现（host 验证）

- spec：`docs/superpowers/specs/2026-06-23-face-detect-template-phase1-design.md`
- plan：`docs/superpowers/plans/2026-06-23-face-detect-template-phase1.md`
- 文件：`core/face_ai.py`
  - 从旧 `scripts/face_detect/app.py` 抽出检测复用逻辑：`ALIGN_UP`、`_draw_color`、`FaceDetectionApp`。
  - 保留 K230 draw 颜色 `(A,B,G,R)` 转换；保留 `draw_result(..., recognition_results=None)` 供后续 Phase 3 彩框/ID 复用。
- 文件：`scripts/face_detect/app.py`
  - 改为 `_template` 单线程 `run(runtime)+on_frame(img)`：`snapshot chn0 → on_frame(chn2 AI detect + 画白框) → Display.show_image(OSD1) → lv.task_handler()`。
  - 删除旧裸跑 self-init media/LVGL、`_thread`、face_db、face_reg、id_registry；Phase 1 只做白框检测。
  - anchors/kmodel 在首次 `lv.task_handler()` 前加载，on_frame 内零文件 I/O。
- 文件：`main.py`
  - 删除 `face_detect` 跳过 `runtime.init_app`/`cleanup` 的特殊分支；face_detect 统一走 reset 框架 init/cleanup。
- 文件：`core/app_runtime.py`
  - face_detect 通道保持 chn0 预览 + chn2 NPU 输入，不分配旧 chn1。
- 测试：
  - `tests/test_face_ai.py`
  - `tests/test_face_detect_template.py`
  - `tests/test_framework.py`
  - 旧 `tests/test_face_detect.py` 双线程契约已退役。
- host 验证：
  - `python tests/test_face_ai.py` → ALL PASS
  - `python tests/test_face_detect_template.py` → ALL PASS
  - `python tests/test_framework.py` → ALL PASS
  - `python tests/test_camera.py` → ALL PASS
  - `python tests/test_camera_gallery.py` → ALL PASS
  - changed files AST → OK
- 提交：`7094d60`（face_ai 抽取）、`0544edd`（模板契约测试）、`ec811b1`（app.py 单线程模板）、`f1b8261`（main 统一 init/cleanup）、`e338193`（旧测试退役）。
- 板端待验收：启动 face_detect → 顶栏/底栏/预览正常 → 对脸白框 → fc 2-5 分钟持续增长不卡 → UI 不消失 → 返回菜单 → 反复进出 3 次稳定。

## 2026-06-23 face_detect Phase 1 板端卡死定位修复（systematic-debugging + TDD）

板端首验两个问题，逐层定位根因后修复：

### 问题 A：主菜单启动 fatal `no module named 'ui.back_bar'`
- 现象：`run_menu` 中 `from core.app_runtime import AppRuntime` 触发 `core/__init__.py` eager-import 旧架构 `ScriptRunner`/`PluginLoader` → `core.script_runner` → `ui.back_bar`，板端缺该模块 fatal。
- 根因：`core/__init__.py` 包初始化有副作用导入，`import core.app_runtime` 会先执行它。
- 修复（TDD）：加 `test_core_init_has_no_legacy_side_effect_imports`（RED）→ `core/__init__.py` 改为无副作用注释包（GREEN）。提交 `5c962ba`。

### 问题 B：face_detect fc~47 卡死、预览黑屏
- 现象：顶栏/底栏/返回钮正常，预览区黑屏，跑到约 fc=47 卡死无 traceback。
- 系统化定位（分段诊断日志，逐层缩小边界）：
  1. init_app 全程日志 → 完整到 `sensor.run done`，排除 init_app/硬件 init。
  2. 主循环/on_frame 边界日志 → 每帧 chn0 snapshot / chn2 snapshot / to_numpy_ref / face_det.run / draw_result / show_image / task_handler / sleep 全部正常返回，最后停在 frame47 `after to_numpy_ref`，无 `after face_det.run`。
  3. 卡点精确锁定：`_face_det.run(img_np)`（NPU 推理）在第 47 帧永久阻塞。
- 根因：K230 坑#16 —— NPU/AI2D/native buffer 对 MicroPython GC 不可见，on_frame 不做每帧 `gc.collect()` 会逐帧累积，几十帧后 `kpu.run`/`face_det.run` 永久阻塞。Phase 1 初版为规避坑#10（运行期 gc 触发 LVGL 终结器）而没加每帧 gc，证据证明此处必须加；on_frame 不创建/删除 LVGL 对象，故每帧 gc 安全。
- 修复（TDD）：加 `test_on_frame_collects_gc_after_npu_run`（RED）→ `on_frame` 在 `face_det.run`+`draw_result` 后加 `gc.collect()`（GREEN）。同时清理临时诊断日志、恢复干净 app.py。提交 `e930a80`。
- 预览黑屏：因 fc~47 卡死前 show_image 已调多次仍黑，怀疑与卡死同因（NPU 累积影响 OSD1）；先验证 gc 修复后黑屏是否同时消失，若仍黑再单独定位。

host 回归：`test_face_ai` / `test_face_detect_template`（含新 gc 测试）/ `test_framework`（含新 core init 测试）全绿。
- 板端待验收：部署 `core/__init__.py` + `scripts/face_detect/app.py` → 进 face_detect → fc 持续涨超 fc=47 不卡 → 预览出画面 → 对脸白框 → 返回/反复进出稳定。

### 板端验收通过（2026-06-23）
- 部署 `core/__init__.py` + `scripts/face_detect/app.py` 后：进 face_detect 正常、预览出画面、对脸白框、fc 持续涨超 fc=47 不卡、UI 不消失、返回/反复进出稳定。Phase 1 板端验收通过。
- 关键结论：坑#10（运行期 gc 触发 LVGL 终结器）与坑#16（NPU 每帧须 gc）的权衡——AI `on_frame` 必须每帧 `gc.collect()`，安全条件是 on_frame 内不创建/删除 LVGL 对象（UI 对象只在 `_build_ui`/`_destroy_ui` 建/删）。后续 AI 脚本 on_frame 遵此约束。
- 记忆：新增 `camerai-face-detect-phase1-accepted.md`，MEMORY.md 索引更新。
- Phase 1 收尾。Phase 2-4（ID 注册 / 特征匹配 / 清除记录）待后续各开设计（运行期写盘/删盘触发坑#2 风险，须单独设计写盘策略）。

## 2026-06-23 人脸识别业务设计（注册 + 识别 + 清除）

- 设计文档：`docs/superpowers/specs/2026-06-23-face-recognition-design.md`
- 背景：在 Phase 1 单线程模板白框检测（板端验收通过）基础上，叠加识别业务。注册需持久化特征到 `/data/fac_db`，运行期写盘撞坑#2（SD I/O 与 display DMA flush 死锁），故写盘/删盘全部 deferred 到退出阶段。
- 用户确认：
  - 注册触发 = K2 物理按键（复用 id_registry，单线程化）。
  - 写盘策略 = 内存注册 + 退出写盘（运行期零 SD I/O，完全避坑#2）。
  - 范围 = 全做（注册 + 识别 + 清除）。
  - 清除触发 = 底栏左下 `list.png` 图标 → 弹出底栏大小浮层 → 清除/保存两按钮；清除=全清（内存+删盘）；保存=空操作（退出自动持久化）。
- 设计要点：
  - `FaceRegistrationApp`（mobile kmodel 2.65MB 512维 + umeyama+affine）+ `database_search` 从旧 app.py git 历史搬入 `core/face_ai.py` / `core/face_db.py`。
  - `face_db.register` 改内存-only + `_dirty`；`face_db.clear` 改内存-only + `_clear_dirty`；`flush_to_disk` 退出阶段按 dirty/clear_dirty 决定写盘或删盘。
  - `IdRegistry` 单线程化（poll_k2 主循环 + try_register on_frame），加 `has_pending()`。
  - `on_frame` 每帧识别最大脸（database_search）+ K2 pending 时复用 feature 注册（零额外 NPU）；每帧 `gc.collect()` 保留（坑#16）。
  - 浮层清除/保存按钮回调不直接删盘/删浮层（坑#2 + use-after-free），deferred 到主循环 `_process_overlay_close`（对齐 camera 删照片 deferred 模式）。
- 当前状态：仅完成并提交设计文档；尚未实施。下一步按 `writing-plans` 生成 TDD 实施计划。

## 2026-06-23 人脸识别业务实现（host 验证）

- spec：`docs/superpowers/specs/2026-06-23-face-recognition-design.md`
- plan：`docs/superpowers/plans/2026-06-23-face-recognition.md`
- 文件：`core/face_ai.py` 加 `FaceRegistrationApp`（mobile kmodel 512维 + umeyama+affine）。提交 `9cd04c7`。
- 文件：`core/face_db.py` 加 `database_search`；`register`/`clear` 改内存-only + `_dirty`/`_clear_dirty`；`flush_to_disk` 退出阶段按标志写盘或删盘；删旧 `clear_disk`。提交 `7b569b6`。
- 文件：`core/id_registry.py` 加 `has_pending()`，单线程化文档（poll_k2 主循环 + has_pending/try_register on_frame）。提交 `30fe356`。
- 文件：`scripts/face_detect/app.py` 叠加识别业务：`_init_ai` 加载 det kmodel + db_features；`run()` 主线程循环前构造 `FaceRegistrationApp`（mobile kmodel）；`on_frame` 每帧识别最大脸（database_search 彩框 ID）+ K2 pending 时复用 feature 注册（零额外 NPU）；底栏 list 图标浮层清除/保存，deferred 关浮层 `_process_overlay_close`；`run()` 退出 finally `face_db.flush_to_disk`；标题改"人脸识别"。提交 `3a55ce0`。
- 文件：`tests/test_face_ai.py`（reg kmodel 契约）、`tests/test_face_db.py`（内存-only + dirty + database_search + id_registry has_pending）、`tests/test_face_detect_template.py`（识别契约，反转 Phase 1 排除 + 标题改"人脸识别"）。
- plan 偏差修正（子 agent 正确根因处理）：
  - plan 的 register/clear docstring 字面含测试禁止 token（`flush_to_disk`/`os.remove`），测试用 ast.get_source_segment 含 docstring 子串匹配 → 只改 docstring 措辞，方法体逻辑不变。
  - plan 把 reg kmodel 构造/flush 放 `_init_ai`/`_deinit_ai`，但契约测试用 `ast.get_source_segment(run_fn)` 只看 run 函数体 → 把 reg kmodel 构造移到 `run()` 循环前、flush 移到 `run()` finally（still 主线程循环前构造、still 退出阶段执行，设计意图不变，契约不削弱）。
- 运行期零 SD I/O（注册/清除只改内存，写盘/删盘 deferred 到退出，避坑#2）。每帧 `gc.collect()` 保留（坑#16）。
- host 验证：`test_face_ai`(6) / `test_face_db`(7) / `test_face_detect_template`(19) / `test_framework`(12) / `test_camera`(14) / `test_camera_gallery`(11) 全绿 + AST OK。
- 板端待验收：注册→彩框 ID→退出→重进仍识别（持久化）；K2 短按注册；list 浮层清除→退出→重进空库；2-5 分钟不卡；返回/反复进出稳定。

## 2026-06-25 上位机通讯协议接入（握手 + 4组ID推送，板端验收通过）

- spec：`docs/superpowers/specs/2026-06-25-host-protocol-integration-design.md`
- plan：`docs/superpowers/plans/2026-06-25-host-protocol-integration.md`
- 目标：把 `comm/host_api.py` 已实现的二进制协议接入 reset 框架主循环，覆盖主菜单 + 全部脚本（_template/camera/settings/face_detect + 未来扩展）。face_detect 识全部脸按 DB 槽位填4组数据。

### 协议语义（参考 `E:\LBS-Project\CanMV\CamerAPP\DurUI.py` + 主机 `dataAgreeAnalys`/`_camer_cam_data` 源码最终敲定）

- 帧格式：`5A(头) + 源A7 + 目97 + length + type + payload + chk + A5(尾)`。
- **length 只算 payload，不含 type**（主机 `data[3]=length`、`data[4]=type`、`memcpy(data+5, data[3])`）。
- **主机超时检测的是"有没有收到数据帧"**，不是握手应答。摄像头持续发数据帧 → 主机不重发握手；仅进程重启/断连间隙主机才重发握手请求。
- 握手请求帧（主机发）：`5A 97 98 0B 09 "Please Link" A5 A5`（帧尾两个 A5）。摄像头按 0x5A 对齐匹配**完整帧**，不用子串。
- 握手应答帧（摄像头发）：payload `"Play Aplication"`（15字符，**文档拼写缺p**，主机硬匹配）。
- **100ms 起步延迟**：握手应答后等 100ms 才开始发数据帧（静默期跳过 `send_id_data`），之后每帧发。未握手前不发数据。
- **不短路**：`poll_handshake` 入口不 `_connected` 短路——主机超时后重发握手，摄像头新进程要能重新应答（切换脚本重连的关键）。
- **x/y/w/h 大端打包**（主机 `_camer_cam_data` 大端解析 `(data[off+1]<<8)|data[off+2]`）。id/conf 单字节不受影响。
- 类型码：main_menu/settings=`0x01`、camera=`0x02`、face_detect=`0x03`、_template=`0x01`（`CATEGORY_TYPE` 映射）。
- 4组数据语义：4组固定对应 DB 槽位1-4。匹配到的槽位填 `(id,x,y,w,h,conf)`，其余全0。多人同画面各自填槽。

### 实现

- `comm/host_api.py`：加 `CATEGORY_TYPE` / `send_id_data`（泛化4组发送，大端）/ `tick`（poll_handshake + 100ms cooldown + 按 category 发）。`send_face_data` 改薄封装。`HANDSHAKE_REQUEST_FRAME` 完整帧常量。`_last_handshake_ms` 时间戳。
- `core/app_runtime.py`：`init_menu`/`init_app` 存 `self.category_id`；加 `host_tick(slots=None)`。
- `main.py:run_menu` + `scripts/{_template,camera,settings}/app.py:run` 主循环每帧加 `runtime.host_tick()`。
- `scripts/face_detect/app.py:on_frame` 改**识全部脸**（每检测框跑 reg + database_search，匹配填对应 DB 槽位）+ 构建4槽位 + `host_tick(slots)`。K2 注册仍取最大脸。
- `core/face_db.py:database_search` 返回 `(slot_id, score)`（原只返回 slot_id 丢 score）。on_frame `conf = int(score*100)`（置信度=识别匹配度，非检测框 det[4]）。
- `core/face_db.py` 持久化禁用为 no-op（用户决定先不保存配置文件，路径待定），`flush_to_disk` 调用作为预留接口保留在 `run()` finally。
- 测试：`tests/test_host_api.py`（新）、`tests/test_host_tick_wiring.py`（新）+ 扩展 `test_framework.py`/`test_face_detect_template.py`/`test_face_db.py`。host 侧 AST 契约 6/6 全绿。

### 板端调试历程（systematic-debugging 逐个定位根因）

1. 握手一直重发 → `poll_handshake` 未检查 `_connected`（先误判为"握手一次后不再应答"，加短路）。
2. 主机不识别设备 → length 含 type 多1字节（主机 `dataAgreeAnalys` length 只算 payload）+ payload 拼写 "Play Application" 应为 "Play Aplication"。修 length + 拼写。
3. 切换脚本断开 → 主机超时检测的是数据帧，短路阻止了重连。推翻短路，改完整帧匹配 + 100ms cooldown + 不短路。
4. 坐标值很大 → 小端打包，主机大端解析。改大端。
5. 置信度一直0 → `database_search` 丢 score，on_frame 误用 `det[4]`。改返回 (slot_id, score)，conf=score*100。

### 提交链

`d145e15`(host_api) → `0dcb234`(app_runtime) → `7a1467d`(wire scripts) → `d3b0c96`(face_detect all-face) → `da8c9ab`(face_db no-op) → `b91e364`(length+payload) → `3d22419`(full-frame+cooldown) → `dd1f447`(big-endian) → `484dd80`(confidence=score)。

### 板端验收

主菜单/settings(`0x01`)、camera(`0x02`)、face_detect(`0x03`) 握手 + 数据帧全通；切换脚本不再断开（重连机制生效）；face_detect 4组数据 id/x/y/w/h/conf 全部正确（注册后人脸匹配槽位填值，置信度=匹配度）。

### 待办

- face_db 持久化路径待用户决定（当前 no-op，接口预留）。
- face_detect 多脸 reg 长时 NPU 稳定性（坑#16）需长时观察；如卡死走降频降级（spec 已记，另开任务）。

## 2026-06-26 稳定版本 0d5af6e（P1/P2/P3 + rfind 修复，板端稳定）

基于 6.25 协议验收版（4cfa745）追加 4 个修复，板端验证 face_detect / camera / settings / 主菜单均稳定运行（主菜单挂机 mem 泄漏卡死问题暂搁置，不影响脚本模式）。

### 追加修复

| commit | 内容 |
|---|---|
| `2471694` | P3 相机图库日期 `年/月/日` → `-` 分隔 |
| `12668fa` | P2 face_detect 顶栏返回钮改用 `face_detect_icon/back.png` + 标题 i18n |
| `4e9c4b9` | P1 恢复开机 LOGO（BootSplash.show）+ 主菜单四角白点（scr_act 显式设纯黑 bg/radius0/border0） |
| `4d9861a` | rfind 修复：`buf.rfind(self.FRAME_HEAD)` 传 int 崩 MicroPython，改 `bytes([self.FRAME_HEAD])` |
| `0d5af6e` | 补 `face_detect_icon/back.png` 图标资源（P2 引用但仓库缺文件，板端 ENOENT） |

### 关键 bug：rfind 传 int 崩 MicroPython

- 现象：face_detect / 主菜单连上位机后 UART 收到数据即崩，`can't convert 'int' object to str implicitly`，traceback 指向 `host_api.py:poll_handshake` 的 `buf.rfind(self.FRAME_HEAD)`。
- 根因：`FRAME_HEAD=0x5A` 是 int，MicroPython `bytearray.rfind()` 不接受 int 参数。CPython 接受 int，host AST 测试测不出来。
- 修复：`buf.rfind(bytes([self.FRAME_HEAD]))`。TDD 守护 `test_poll_handshake_rfind_does_not_pass_int`。

### 部署清单（板端 /sdcard/CamerAi/）

代码：`comm/host_api.py` `core/app_runtime.py` `main.py` `ui/main_menu.py` `scripts/{face_detect,camera,settings,_template}/app.py`
图标：`resource/icons/face_detect_icon/{back,list}.png`

### 已知未解决：主菜单挂机 mem 泄漏卡死

- 现象：主菜单不操作挂机 1-2 分钟，`gc.mem_free` 线性下降到 0 后硬死机。脚本模式（face_detect/camera）不卡。
- 已排查排除：host_tick（撤回预分配改动）、DIRECT render mode（黑屏）、flush_cb 清零/sleep、`_on_scroll->_update_snap`、显式 gc.collect（与 LVGL 互锁必卡）、gc.threshold（更早卡）。
- 边界诊断结论：`lv.task_handler` 每帧约 256B + `host_tick` 每帧约 1216~1472B（host_tick 预分配可降到 0~256，但该改动已撤回未保留）。
- DurUI 对照：DurUI 同样 mem 下降，但自动 gc 在 ~400K 触发回收跳回 3.3M 不卡；本项目自动 gc 触发时机不安全，回收后即死。
- 当前处置：暂搁置。主菜单实际使用不会挂机那么久（用户很快点进脚本），脚本模式稳定。后续如需深查，方向是 LVGL 回调/style_t 对象保活（GC 后悬空指针）或对齐 DurUI 显示管线（DIRECT + 不指定 layer + scr_load）。

### 待办

- face_db 持久化路径待用户决定（当前 no-op，接口预留）。
- 主菜单挂机 mem 泄漏卡死（见上，暂搁置）。
- face_detect 多脸 reg 长时 NPU 稳定性（坑#16）需长时观察。

---

## 2026-06-26 标签识别（tag_detect）脚本 + face_detect 双语修复 + 居中十字

### 新增：tag_detect 脚本（AprilTag + 二维码双功能）

- **设计/计划**：`docs/superpowers/specs/2026-06-26-tag-detect-design.md`、`docs/superpowers/plans/2026-06-26-tag-detect.md`
- **TDD 9 Task 全程 Red→Green**，11 个测试文件全量回归 ALL PASS。
- **架构**：复用 `_template` 单线程主循环；chn0 VGA RGB888 显示，**chn1 QVGA RGB565 专做检测**（官方 AprilTag/QR demo 同款，VGA 下 AprilTag 极慢），rect ×2 整数缩放映射显示。
- **双功能切换**：底栏 list 图标（绑定清除/保存浮层，对齐 face_detect）+ AprilTag/QR 两卡片（选中置绿 0x2E7D32）+ N/4 计数。
- **ID 注册**：检测到码 → 按 KEY2 → 存入下一槽（1-4 轮转覆盖）。`core/tag_db.py`（纯 Python 可真单测）镜像 face_db 内存-only + flush_to_disk 预留（no-op）。`IdRegistry.try_register` 加可选 `registrar` 参数（None→face_db 向后兼容，tag_detect 传 tag_db.register）。
- **协议**：`host_api.CATEGORY_TYPE` 加 `tag_detect→0x04`，两功能都用 0x04 上传 4 槽位（命中 conf=100）。
- **AprilTag 家族**：仅 TAG36H11（官方默认，误检率最低）。
- **画框**：对齐 face_detect——未注册白框（BOX_UNKNOWN=0xFFFFFF），注册按 slot 彩色（1绿/2蓝/3橙/4紫），`_draw_color` 转 RGB888 (A,B,G,R)，`ID%d` 文字同色。
- **持久化**：预留 `flush_to_disk` 接口，当前 no-op，路径待定（同 face_db）。

### 修复：face_detect 双语切换

- 根因：非标题文本（"已注册"/"清除"/"保存"）硬编码中文未走 `lang.t()`，切英语只有标题变英文。
- 修复：三处改 `t()`；`face_detect.registered` i18n 值改带 `%d` 格式（zh "已注册 %d/4" / en "Registered %d/4"）。

### 新增：居中绿色十字（face_detect + tag_detect）

- 屏幕居中画小绿十字（VGA 640×480 中心 320,240，size=20，thickness=2），作对准参考。颜色 RGB888 (A,B,G,R) 绿色，对齐 face_ai 画框色序。

### 提交（本地领先 origin，待 push）

| commit | 内容 |
|--------|------|
| `00b437c` | docs: tag_detect 设计 spec |
| `f9da61d` | docs: tag_detect TDD 实施计划 |
| `ec60771` | fix: face_detect 硬编码中文→t() 双语修复 |
| `57daec7` | feat: TagDB 纯 Python 内存库（可真单测） |
| `eb1cc55` | feat: IdRegistry 可选 registrar 参数 |
| `7987eb4` | feat: host_api tag_detect→0x04 |
| `8080ae8` | feat: icon_cache tag 图标 + 复制 back/list.png |
| `0e98e4c` | feat: app_runtime chn1 QVGA RGB565 + 图标预读 |
| `98fd7b8` | feat: i18n tag_detect 段 |
| `c543b24` | feat: tag_detect AprilTag+QR 双功能脚本 |
| `177a78c` | feat: 画框对齐 face_detect + list 绑浮层 |
| `2807d01` | feat: 居中绿色十字 |

### 部署清单（板端 /sdcard/CamerAi/）

- 代码：`core/tag_db.py`（新）、`core/id_registry.py`、`core/icon_cache.py`、`core/app_runtime.py`、`comm/host_api.py`、`scripts/tag_detect/app.py`（新）、`scripts/face_detect/app.py`
- i18n：`resource/i18n/{zh_CN,en_US}.json`
- 图标：`resource/icons/tag_detect_icon/{back,list}.png`（新，复制自 face_detect_icon）

### 板端验收（2026-06-26 全过）

7 点全过：进标签识别 → 顶栏标题+back → 底栏 list/AprilTag(默认绿)/QR 卡片 → 对准 AprilTag 红框→K2 注册绿框+id1+计数 → 切 QR 同流程 → 上位机 0x04 数据帧 4 组正确 → 中英双语切换 → 退出不卡死 → 居中绿十字。后续修订（画框白/彩色 + list 浮层 + 十字）待板端复验。

---

## 2026-06-26 物体识别脚本（object_detect，YOLOv8n COCO80）

### 背景

扩展第四个 stream 脚本：物体识别。UI 布局 / 串口协议 / ID 设置与 tag_detect 一致；底栏仅左侧 list 图标（清除/保存浮层）；加居中绿十字。检测用 `yolov8n_320.kmodel`（COCO 80 类，demo 实验15 同款）。注册语义=**按类别注册**（KEY2 注册当前帧最大框的类别到槽 1-4，最多 4 类），注册框显示 `ID%d english_class_name`（槽号+英文类名，类名不双语），未注册白框。

### 设计要点

- **检测通道**：`app_runtime._channels_for` 加 `object_detect → (CAM_CHN_ID_2, Sensor.XGA, Sensor.RGBP888)`，与 face_detect **完全相同**的 AI 通道（XLA=笔误，实际枚举名 `Sensor.XGA`=1024×768，板端已验证可用）。chn0 VGA RGB888 显示，检测框 rgb888p→display 整数缩放。
- **`core/object_ai.py`**（新）：`ObjectDetectionApp(AIBase)`，镜像 face_ai 封装 + 移植 demo 实验15 后处理。AI2D 仅 resize（不做 letterbox）。`postprocess`：YOLOv8 [N,84] → argmax 取类 → conf 阈值 0.2 → **纯 Python NMS**（无 aidemo C 加速，移植 demo.nms）→ 返回 `[[l,t,r,b,score,class_id], ...]`（rgb888p 坐标）。`COCO_LABELS` 80 类英文标签（内置）。不内置 draw_result（画框在 app 按注册槽上色）。
- **`core/object_db.py`**（新，纯 Python 可真单测）：`ObjectDB` 镜像 tag_db，存 int class_id。**同类不重复占槽**（同 class_id 再注册返回原槽，不推进轮转指针）—— 区别于 tag_db 纯轮转。register round-robin 1-4，match 精确（score=1.0），clear，flush_to_disk no-op 预留。
- **on_frame**：每帧 chn2 推理 → 每类别取**面积最大实例**画框+填槽（协议每槽一坐标）→ 注册类彩色框+`ID%d 类名`+填槽，未注册白框无文字 → 居中绿十字 (320,240) → KEY2 取最大框类别 `try_register(cid, buzzer, registrar=db.register)` → `host_tick(slots)` 类型 0x05 → gc.collect。
- **画框**：对齐 face_detect/tag_detect——未注册白框 BOX_UNKNOWN=0xFFFFFF，注册按 slot 彩色（1绿/2蓝/3橙/4紫），`_draw_color` 转 RGB888 (A,B,G,R)。
- **UI**：镜像 face_detect/tag_detect，顶栏 back+标题 + 透明预览 + 底栏（list 图标绑清除/保存浮层 + 计数标签）。**无双功能卡**（object_detect 单一功能，区别于 tag_detect 的 AprilTag/QR 卡）。浮层 clear/save + deferred 关闭（LVGL use-after-free 防护），整套与 face_detect 一致。
- **协议**：`host_api.CATEGORY_TYPE` 加 `object_detect→TYPE_OBJECT_DETECT(0x05)`（常量原已预留）。每注册类别填一槽（最大实例），未注册类别不填。
- **i18n**：新增 `object_detect` 段（registered `%d/4` / clear / save 双语）。category.object_detect 预置。

### 提交（本地领先 origin，待 push）

| commit | 内容 |
|--------|------|
| `8d26a88` | docs: object_detect 设计 spec |
| `a52c514` | docs: object_detect TDD 实施计划（9 任务） |
| `23bf73f` | feat: host_api object_detect→0x05 |
| `ea98c0a` | feat: ObjectDB 类别→槽内存库（纯 Python，同类不重复占槽） |
| `6f6637e` | feat: object_ai YOLOv8n COCO80 封装（AIBase 子类 + 纯 Python NMS） |
| `8db472d` | feat: icon_cache object 图标 preload/get |
| `5a02091` | feat: app_runtime object_detect chn2 XGA RGBP888 + 图标预读 |
| `d0ff3b7` | feat: i18n object_detect 段（双语） |
| `1e3fc6e` | feat: object_detect 主 app 循环（YOLOv8 检测/类别注册/十字） |
| `60990e0` | feat: object_detect back/list 图标（复制自 face_detect_icon） |

### 部署清单（板端 /sdcard/CamerAi/）

- 代码：`core/object_ai.py`（新）、`core/object_db.py`（新）、`core/icon_cache.py`、`core/app_runtime.py`、`comm/host_api.py`、`scripts/object_detect/app.py`（新）
- i18n：`resource/i18n/{zh_CN,en_US}.json`
- 图标：`resource/icons/object_detect_icon/{back,list}.png`（新，复制自 face_detect_icon）
- kmodel：`yolov8n_320.kmodel` 板端自带（`/sdcard/examples/kmodel/`），无需部署

### 测试

- 新增 6 个测试文件全 PASS：`test_object_db.py`（8 真单测）、`test_object_ai.py`（9 AST 契约）、`test_object_detect_app.py`（13 AST 契约）、`test_app_runtime_object.py`（2 契约）、`test_i18n_object.py`（3 契约）；改 `test_host_api.py`/`test_icon_cache.py` 各加 object 用例。
- 全量回归 25 测试文件：23 PASS / 2 FAIL。2 个失败（test_face_register、test_template）经 baseline `a52c514` 对比确认是 **object_detect 工作之前就存在的预存失败**（main.py face_detect 特殊处理分支、_template 通道、PARTIAL 渲染模式相关，与本次改动无关）。

### 板端验收

待板端复验（7 点）：进物体识别 → 顶栏标题+back → 对准物体白框 → K2 注册彩色框+`ID1 类名`+计数 → 多类注册轮转 → 同类不重复占槽 → list 清除浮层 → 上位机 0x05 数据帧 4 组正确 → 居中绿十字。

### 风险

- YOLOv8 后处理纯 Python NMS（无 aidemo C 加速），板端帧率可能低于 face_detect。降级：调小 max_boxes_num（50→20）、每帧 gc.collect。卡顿记录坑并降级。

---

## 2026-06-26 物体识别板端验收 + 卡顿/泄漏诊断 + 主菜单改进

### object_detect 板端验收（7 点全过）

物体识别板端验收通过：进物体识别 → 顶栏标题+back → 对准物体白框 → K2 注册彩色框+`ID1 类名`+计数 → 多类注册轮转 → 同类不重复占槽 → list 清除浮层 → 上位机 0x05 数据帧 4 组正确 → 居中绿十字。

### 卡顿诊断与优化（object_detect）

板端 debug_mode=1 计时定位：`kpu run` 46.3ms/帧（占 69%，模型本身硬底线，纯 Python 改不动）+ `postprocess` 16.7ms + preprocess/get_output 小项。理论 ~14fps，K230 上 yolov8n_320 固有限制。

优化（保留）：`core/object_ai.py` postprocess 向量化预算 lbrt（8400 行一次性算，循环只过阈取值）；`scripts/object_detect/app.py` confidence 0.2→0.5、max_boxes 50→20、debug_mode 关。⚠️ ulab ndarray 无 `.astype()`（CPython numpy 才有），int 转换在循环用 `int()`。板端卡顿"好一点点"后接受现状（通用物体识别在 K230 即此水平，换 person_detect_yolov5n+aicube 能更快但只识人）。

### 3 个 BUG 修复

1. **每次进脚本 KEY2 自动按一次**：根因 `IdRegistry.__init__` 硬初始化 `_prev_pressed=False`，启动瞬间引脚电平未稳定/上拉未建立时首次 poll_k2 误判按下边沿。修复：构造时 sleep 5ms 等上拉 + 读当前真实电平作 `_prev_pressed` 初值。
2. **返回主菜单重显开机 LOGO**：根因 `run_script→reset→run_menu` 无条件显示 BootSplash，软件无法区分 reset 回菜单 vs 首次上电。修复：新增 `.warm_boot` 标记文件，run_script 末尾写、run_menu 开头检测到就跳过 LOGO（仅上电/断电重启无标记才显 LOGO）。
3. **主菜单吸附僵硬/无节奏**：滚动中逐帧硬切几何（set_size/set_pos 无过渡）→ 僵硬；慢速停交界 nearest_idx 反复跳 → 蜂鸣嘀嘀嘀 + 同卡弹出缩回重复。修复：(a) 几何用 ease_out 动画过渡（280ms）+ 版本号 token 防多动画闭包叠加（快速滚动卡片没缩回）；(b) `_update_snap` 加迟滞（越过半步进+1/4 才切）防边界抖动；(c) 滚动中不蜂鸣，松手吸附响一声确认；(d) 非居中卡点击无反应（只能滚动选中），居中卡点击启动。

### 主菜单内存泄漏诊断（systematic-debugging，暂搁置）

用户线索"滚动时掉更多，滚越快越多"是关键。三轮实验定位：

| 假设 | 实验 | 结论 |
|---|---|---|
| 图标解码缓存泄漏 | 色块占位（无 PNG/lv.img）两轮下降完全一样（109056B/3s 固定） | ❌ 排除 |
| host_tick 临时分配 | send_frame/send_id_data 预分配 `_tx`/`_id_payload` 复用，每帧零分配 | ✅ 主因，泄漏 109056→28544B/3s（**减 74%**，保留）|
| 几何动画 set_size/pos | 禁用动画滚动仍漏一样多 | ❌ 排除 |
| FULL render mode 整屏重绘 | 静止 28k/3s + 滚动额外，低频 gc 回收不到 | ✅ 剩余根因（C 层，GC 够不着）|

**根因定论**：剩余泄漏（静止 28k/3s + 滚动额外）全部来自 `lv.task_handler` 的 **FULL render mode** 整屏重绘 C 层分配，Python/GC 都够不着。`import time` 已提模块级。停 host_tick 会卡死（uart.write 参与 DMA 让步，不能停）。**唯一解**：DIRECT render mode（对齐 DurUI：`show_image(draw_buf)` 不指定 layer + `lv.scr_load`），但之前试 DIRECT 黑屏死锁，暂搁置。memory `camerai-main-menu-freeze-unresolved` 已更新全部发现。

**保留改进**：host_tick 预分配（泄漏减 74%）、bytes→bytearray（img_dsc_t.data 鲁棒性）。主菜单实际使用不会挂机那么久（用户很快点进脚本），脚本模式稳定。

### 其他

- `config/categories.json`：`_template` 类目 `enabled: true→false`（不进主菜单，开发模板仅参考）。`tests/test_template.py::test_categories_json_has_template` 同步放宽（不再强制 enabled=True）。
- 全量 25 测试文件：23 PASS / 2 FAIL（test_face_register、test_template 的 _channels_for/PARTIAL/main.py face_detect 三项为 object_detect 工作前预存失败，与本次无关）。

## 2026-06-26 颜色识别(color_detect)设计 brainstorming

- 触发:用户"扩展新脚本-颜色识别"。需求:UI 同前脚本;底栏 list 图标 + 6 个 LAB 阈值格(Lmin/Lmax/Amin/Amax/Bmin/Bmax)+ 共享滑块(选中置绿,标签上数值下);左侧 4×3 表格(首行 L/A/B,2-4 行采色历史,底色=采样色,LAB 值);屏幕点击取色循环填 3 槽;KEY2 注册当前色为 ID。
- 探查:color_detect 条目已在 categories.json(order6,enabled);TYPE_COLOR_DETECT=0x06 已定义但未进 CATEGORY_TYPE;i18n 键已存在;tag_detect 范式可复用(chn1 QVGA RGB565 检测 + chn0 VGA 显示,DET_SCALE=2);LVGL slider 项目未用过但 demo 有用法。
- 三轮 AskUserQuestion 澄清交互语义:
  1. 取色后 LAB 用法 → **采样即套用阈值**(点哪个色就用哪个色检测,滑块可微调)
  2. KEY2 注册"当前颜色" → **当前滑块检测色**
  3. ID 槽位数 → **4 槽(对齐协议 send_id_data 4 组)**,左表 3 槽仅采色历史参考,与 ID 独立
- 用户补充:容差 L/A/B 统一 **±10**;左表位置放**顶栏左下方**;**保留居中绿色十字**。
- 设计文档:`docs/superpowers/specs/2026-06-26-color-detect-design.md`(架构/通道/检测/on_frame/UI布局/取色阈值/RGB→LAB/ID注册协议/ColorDB/i18n/文件清单/风险/测试策略)。
- 架构定稿:复用 _template 单线程循环 + tag_detect 双通道;chn0 取色显示 + chn1 find_blobs;采样即套用 ±10 容差生成 6 阈值;KEY2 轮转注册 4 槽;每帧当前色白框 + 注册色彩色 ID 框;协议 0x06。
- 下一步:用户审阅 spec → writing-plans 出 TDD 实施计划。

## 2026-06-26 颜色识别(color_detect)实施计划 writing-plans

- 设计 spec `docs/superpowers/specs/2026-06-26-color-detect-design.md` 用户已确认。
- 实施计划 `docs/superpowers/plans/2026-06-26-color-detect.md`,7 个 TDD Task:
  1. ColorDB 纯 Python 内存库(6阈值+LAB+RGB,同阈值不重复占槽,轮转1-4)
  2. host_api CATEGORY_TYPE 加 color_detect->0x06 + zh/en i18n 6阈值键
  3. app_runtime _channels_for 加 color_detect(chn1 QVGA RGB565)+ icon_cache preload/get_color_icon
  4. RGB→LAB(sRGB→XYZ→Lab D65)+_make_threshold ±10 容差6阈值裁剪
  5. UI:顶栏/左表4×3(顶栏左下方)/透明预览可取色/底栏 list+6格+共享滑块+计数
  6. on_frame:pending_click取色套阈值+当前色白框+注册色彩色ID框+KEY2注册4槽+十字+host_tick + run/_destroy_ui
  7. 全量回归 + 板端18项验收清单
- 接口核对:TagDB.register 轮转空槽优先范式、IdRegistry(fpioa,pin)、_channels_for 分支、icon_cache preload/get 范式、i18n 顶层脚本段结构。
- 下一步:用户选执行方式(subagent-driven 推荐 / inline)。

## 2026-06-26 color_detect Task1 ColorDB 纯 Python 内存库(TDD)

- 文件：`core/color_db.py`(新增)
  - `ColorDB` 类:存 6 阈值 tuple `(Lmin,Lmax,Amin,Amax,Bmin,Bmax)` + 中心 LAB + RGB。
  - `register(threshold, rgb)`:同阈值(完整 2-tuple 相等)不重复占槽返回已有 slot;空槽优先(1-4),满 4 轮转覆盖(`_next_slot` 循环);设 `_dirty`。
  - `match(threshold)`:精确匹配(完整 threshold 相等)→`(slot,1.0)`,否则`(None,0.0)`。
  - `clear()`/`get_slot()`/`iter_slots()`/`flush_to_disk()`(no-op 预留,仅复位 dirty)/`count` 属性。
  - 镜像 tag_db 内存-only + flush 预留;K230 坑#2 运行时不写盘。
- 文件：`tests/test_color_db.py`(新增,8 用例,无 MicroPython 依赖)
  - register 返回 1→2、同阈值去重、轮转回 slot1、match 命中/未命中、clear、get_slot、iter_slots。
- **TDD 修复(根因)**:计划给定实现把 `entry['threshold']` 存成 6-tuple `th`(unpack 后的首段),但测试契约要求存完整 2-tuple `((...),(...))`。导致 `test_register_same_threshold_returns_existing_slot`(去重比 6-tuple vs 2-tuple 永不命中)与 `test_get_slot_returns_threshold_and_meta`(断言期望完整 2-tuple)2 项 FAIL。修复:register 存 `threshold`(完整),match 直接比 `entry['threshold']==threshold`;`lab` 仍单独存供 print/取用。Red→Green 确认。
- 验证：`python tests/test_color_db.py` → 8/8 PASS(ALL PASS)。
- 下一步：Task2 host_api CATEGORY_TYPE color_detect->0x06 + i18n 6 阈值键。

## 2026-06-29 颜色识别(color_detect)实现完成(待板端验收)

7 个 TDD Task 全部完成(subagent-driven,逐 Task 审查):

| commit | Task | 内容 |
|---|---|---|
| f82b886 | 1 | ColorDB 纯 Python 内存库(6阈值+LAB+RGB,同阈值不重复占槽,轮转1-4)。实现时修了存储契约:存完整 threshold 2-元组而非拆开的6-元组。8 PASS |
| dd7ae7f | 2 | host_api CATEGORY_TYPE 加 color_detect->0x06 + zh/en i18n 6阈值键+registered/clear/save。13 PASS |
| 94ff5c1 | 3 | app_runtime _channels_for 加 color_detect(chn1 QVGA RGB565,同tag_detect)+ init_app 预读 + icon_cache preload/get_color_icon。4 PASS |
| 5a6d9ea | 4 | RGB→LAB(sRGB→XYZ→Lab D65 纯Python,取色时调一次)+ _make_threshold ±10容差6阈值裁剪。函数体用字面量便于AST exec测试。9 PASS,数值标准无需调阈值 |
| 54d3b09 | 5 | UI:顶栏(返回+标题)+左表4×3(顶栏左下方,采色历史)+透明预览(可点击取色)+底栏(list图标+6阈值格点选置绿+共享滑块range随选中格切换+计数)+清除/保存浮层。13 PASS |
| 5f936a5 | 6 | on_frame:pending_click取色套阈值+当前色find_blobs白框+注册色彩色ID框+KEY2注册4槽+居中绿色十字+host_tick协议0x06 + run()单线程主循环 + _destroy_ui。21 PASS |
| 39238bb | - | 复用 tag_detect back/list 图标到 color_detect_icon(目录原空) |

全量回归:test_color_db(8)/test_host_api(13)/test_color_detect_ast(21) 全 PASS;test_template 3 FAIL 为预存基线(与本次无关)。app.py py_compile OK,工作区干净。

关键设计点:
- 双通道:chn0 VGA RGB888 显示+取色(get_pixel直接VGA空间无需换算),chn1 QVGA RGB565 find_blobs(blob rect ×2 映射显示)
- 采样即套用:点屏→get_pixel RGB→_rgb_to_lab→_make_threshold ±10→更新6格+滑块→压入左表3槽循环(0→1→2→0)
- KEY2 注册当前6阈值到4槽(同阈值不重复),每帧注册色find_blobs取最大blob画彩色ID框,slots填(id,x*2,y*2,w*2,h*2,100),协议0x06大端
- 滑块首次使用:VALUE_CHANGED回调刷新格数值,range随选中格类型切换(L格0-100,A/B格-128~127)
- 左表3槽采色历史与ID 4槽独立

待板端验收(18项清单),通过后建 memory camerai-color-detect.md。

## 2026-06-29 颜色识别(color_detect)板端验收通过

板端验收通过(用户确认)。板端调试踩的 6 个 K230 坑(均已修):
1. `lv.table` 无 set_rows/set_cols → LVGL v8 MP 绑定用 set_row_cnt/set_col_cnt;且 table cell 白底不可控、无法按行设底色 → 左表改自建 obj 4×3 网格(行底色=采样RGB)。
2. FontManager 无 small 档 → 只有 title/body/caption,阈值格标签用 caption。
3. `indev.get_point()` 无参不返回 point → 需传入预分配 lv.point_t() 填充;原无参调用抛异常被 try/except 静默吞 → 取不了色。
4. `img.get_pixel(x,y)` 对 chn0 RGB888 snapshot 返回 None(合法坐标也返回 None,绑定未实现)→ 改用 chn1 RGB565 img_det 取色,坐标÷DET_SCALE(2),RGB565 打包值解包到8位。
5. `find_blobs([th])` 的 th 必须是 list 不能是 tuple(报 can't convert tuple to int)→ th_list=[int(v) for v in th]; find_blobs([th_list])。
6. ColorDB `entry['threshold']` 必须存 6-元组,不能存完整 2-元组 ((6阈值),(L,A,B)) → 否则注册色检测 _find_largest_blob 收到 2-元组报错;register去重/match 同步比较 threshold[0]。

布局调整(板端反馈):
- 左表一片白 → 自建 obj 网格(非 lv.table)。
- 底栏标签改短英文 Lmin/Lmax/...(不走 i18n)。
- 滑块移预览右侧竖向(20×300,x=612),预览宽改 600。
- 底栏 6 格铺满(list到计数均分,每格~82px)。
- 英文 registered 缩为 "Reg %d/4"(中文不变)。

设计语义:KEY2 注册不依赖当前帧 find_blobs 命中(颜色阈值本身就是特征,取色/调滑块设好即注册)。

部署:color_detect 跨 10 文件(app.py/__init__/color_db/app_runtime/icon_cache/host_api/2图标/2i18n),只部署 app.py 不够(color_db 没更新会复现坑#6)。memory camerai-color-detect.md 已建。

## 2026-08-03 tag_detect 改全屏扫描+动态排序上报

- **需求**：取消按键学习 ID（KEY2 注册），改为每帧全屏扫描所有可识别 AprilTag/QR 码，
  按屏幕从左到右排序（x 最小 = 目标1），动态数量上报；id 直接用实际码值。
- **改动**：
  - 新增 `core/tag_scan.py`（纯 Python）：`build_slots(detected, qr_mode, scale=2)`
    排序（x,y 升序）→ 截断 25 → id 编码 → 坐标 ×2。host 可真单测。
  - `comm/host_api.py`：`send_id_data` 支持动态 N 槽（N≤25，N×10B 载荷，
    slots=None/空 → 0B）；`send_frame` 加可选 `length` 参数（零分配）；
    `_id_payload` 250B、`_tx` 257B。帧格式不变，face/object 等 4 槽调用兼容。
  - `scripts/tag_detect/app.py`：移除 KEY2/TagDB/持久化/清空保存浮层/注册计数；
    on_frame 走 `tag_scan.build_slots`，画框统一白色；list 图标纯占位。
  - id 语义：AprilTag = 实际码值（TAG36H11 0~586，>255 固定 255）；QR = 排序序号。
- **验证**：`tests/test_tag_scan.py`（真单测）、`test_tag_detect_app.py`（AST 契约反转）、
  `test_host_api.py`（动态载荷 AST）全 PASS；host_api_switch/host_tick_wiring/framework/
  tag_db_persist/id_registry/i18n_tag/app_runtime_tag 回归全 PASS。
  （注：`test_tag_db.py` 直 import 失败为既有基线问题——`from core import db_store` 需包
  上下文，非本轮改动引入，与本次 tag_detect 无关系。）
- **主机侧注意**：0x04 帧载荷由固定 40B 变为动态 N×10B（N≤25），主机须按 length 解析；
  id 语义由板端当前功能决定（AprilTag=码值 / QR=序号）。
- **坑**：AST 契约断言扫源码文本，注释中不得含 `try_register`/`TagDB` 等被断言关键词
  （重写 app.py 时注释已规避）；计划 Task5 回归清单漏了 `test_i18n_tag.py` 的键断言，
  执行时同步更新为只要求 `april_tag`/`qr_code` 且断言学习键已删除。
### 2026-08-03 补充：框标签加 ID: 前缀

- **现象**：用户要求框标签显示 `ID:码值`（AprilTag 显示 `ID:42` 等实际码值，QR 显示 `ID:序号`）。
- **改动**：[scripts/tag_detect/app.py](scripts/tag_detect/app.py) on_frame 标签由 `str(detected[i][0])` 改为 `"ID:" + str(detected[i][0])`；`tests/test_tag_detect_app.py` 新增 `test_on_frame_label_has_id_prefix` 契约断言。
- **验证**：`python tests/test_tag_detect_app.py` 全 PASS，`py_compile` 通过。

### 2026-08-03 补充：多标签显示 ID 错位修复（码值与框一一对应）

- **现象**：用户报告标签识别 ID 显示错位——单目标正常；先让标签 3 进画面、再让标签 4 进画面后，屏幕显示标签 3 框上变 `ID:4`、标签 4 框上变 `ID:3`；主机侧 0x04 上报数据排序正常。
- **根因**：`scripts/tag_detect/app.py` on_frame 画框用 `slots[i]`（`build_slots` 已按 (x,y) 排序），显示码值却取**未排序**的 `detected[i][0]`。`find_apriltags` 返回顺序与按 x 排序后顺序不一致时（多目标常见），ID 与框错位；主机上报用排序后的 `slots` id_val，故数据正常。
- **改动**：`core/tag_scan.py` `build_slots` 加 `return_codes=True` 可选参数，返回 `(slots, codes)`——codes 为与 slots 同序的原始 code_id（排序后未截断，AprilTag=码值 / QR=payload），默认 False 保持原 6 元组列表返回兼容；`scripts/tag_detect/app.py` 改用 `codes[i]` 显示，与框一一对应。
- **验证**：`tests/test_tag_scan.py` 新增 4 个 return_codes 用例（同序/未截断/QR payload/默认兼容）全 PASS；`py_compile` 通过；`test_app_runtime_tag`/`test_host_api_switch`/`test_host_tick_wiring`/`test_framework` 回归全 PASS。

### 2026-08-03 补充：所有模式统一动态 N 组协议（槽位按屏幕位置排序）

- **需求**：所有 stream 模式统一采用标签式的动态载荷（N×10B，主机按 length 解析）；其它模式注册 ID 槽上限保持 4；槽位排序与标签一致（按 (x,y) 升序，最左=目标1）；**id 值保持各模式原语义**（人脸/颜色等注册槽号不变，槽位只按位置排列，如 ID3 在最左则目标1 的 id=3）。
- **背景缺陷**：各脚本 slots 固定 `[None]*4` 且未命中留 None，`send_id_data` 解包 None 直接崩 → **目标不满 4 个时整帧 abort、上位机收不到数据**（仅 tag_detect 动态列表天然安全）。统一协议一并修复。
- **改动**：`comm/host_api.py` 加模块级 `order_slots(filled, max_slots=4)`（过滤 None → (x,y) 升序 → 截断）与 `MAX_REG_SLOTS=4`；`tick()` 对 slots 非 None 统一 `order_slots`（tag_detect 上限 25 / 其它 4）后发送；`send_id_data` 加 None 防御（跳过空槽）。各脚本零改动。
- **文档**：`通讯协议.txt` 改为"统一数据载荷说明"，含类型码/槽位上限/id 语义表。
- **验证**：新 `tests/test_host_api_order.py`（order_slots 3 用例 + tick 动态发送 4 用例）全 PASS；host_api_switch/host_tick_wiring/tag_scan/framework/app_runtime_tag 回归全 PASS。
- **主机侧注意**：破坏性变更——所有模式须按 length 解析 N 组（原其它模式固定 40B 解析不再成立）。

### 2026-08-03 补充：人脸识别优化（精度阈值 + 多人性能 + 注册质量）

- **现象**：① 画面人脸太多时卡死（体验差）；② 学习一个人脸后换下一个人脸，仍识别为上一个人（精度差）。
- **根因**：① on_frame 每帧对每张脸串行 reg NPU 推理 + 全库余弦，多人时单帧耗时爆炸且无帧率保护；② `database_search` 阈值 0.75（cos≥0.5）对 MobileFaceNet 过松，不同人脸也命中 → 换人误识别为已注册人。
- **改动**：
  - `core/face_db.py`：默认阈值 `FACE_MATCH_THRESHOLD` 0.75→**0.82**（cos≥0.64）；新增 `FACE_MATCH_MARGIN=0.06` 次佳区分——注册≥2 人时 best 与 second 分数差 < margin 判"不确定"返回 None（防已注册人混淆）；单注册人不受影响。
  - `scripts/face_detect/app.py`：`REG_MAX_FACES=4` 每帧最多识别 4 张（与协议 4 槽对齐，超出只画框）；多人跳帧（2~3 人每 2 帧、≥4 人每 3 帧，检测仍每帧跑；K2 注册时强制识别）；非识别帧复用上轮 slots 保持主机数据连续；`MIN_REG_AREA=1600`（VGA 40×40）以下拒绝注册 + 300ms 长音提示（脸太小特征质量差）。
- **验证**：新 `tests/test_face_db_search.py`（fake ulab exec 真单测：阈值 ≥0.82 / score0.8 拒绝 / 高分命中 / margin 拒绝 / 单人不生效 / 空库）6 用例全 PASS；`test_face_detect_template.py` 的"识全部脸"契约同步更新为"上限保护遍历"，其余 face 回归全 PASS（`test_face_db` 1 FAIL、`test_face_detect_template` 2 FAIL 为既有基线失败，stash 验证与本轮无关）。
- **参数可调**：阈值/margin/上限/间隔均为常量，板端实测后可再调（如阈值 0.82 仍误报可提 0.85；卡顿仍明显可降 REG_MAX_FACES）。

### 2026-08-03 补充：运动多人脸甩动即卡死 — on_frame 缺帧内 GC（坑#16 不完整修复）

- **现象**：① 摄像头静止、无人脸，mem_free 平稳，跑一段时间死机（排除 FULL C 层泄漏——泄漏会 mem_free 下降）；② 关键复现：扫多人脸时甩动两下**立马卡死**（无 traceback）。
- **根因**：坑#16（NPU/AI2D 原生缓冲对 MicroPython GC 不可见，须靠 Python 对象回收触发释放）历史修复只在**主循环帧尾** `gc.collect()`，on_frame 内连续 N 次 reg 推理产生的原生缓冲要等整帧结束才释放——运动 + 多人时每帧峰值叠加 → NPU 内存瞬时耗尽 → `kpu.run` 永久阻塞（无 traceback 卡死）。契约测试 `test_on_frame_collects_gc_after_npu_run` 一直基线 FAIL，正是设计者要求 on_frame 帧内 gc 的证据。
- **改动**：`scripts/face_detect/app.py` on_frame 帧内加 3 处 `gc.collect()`——① `_face_det.run` 后立即；② 每张脸 `_face_reg.run` 后立即（识别循环内）；③ K2 注册 `_face_reg.run` 后。坑#10 安全性成立（on_frame 不创建/删除 LVGL 对象，历史结论）。另加无人脸时 det 跳帧（`DET_IDLE_INTERVAL=2`，有脸仍每帧，降 NPU 分配频率）。
- **验证**：`py_compile` 通过；`test_on_frame_collects_gc_after_npu_run` 由基线 FAIL → **PASS**；face_db_search/face_ai 回归 PASS（`test_on_frame_no_runtime_disk_io` 为基线既有失败）。
- **板端复验**：① 无人脸长时间挂机；② 多人脸快速甩动 30 秒以上；③ 多人脸进进出出。观察 fc 持续增长、无死机。

### 2026-08-03 补充：静止死机根因确认 — chn2 大分辨率 DMA 竞争修复

- **证据**：插桩定位 → 死机在 `pre-show` 之后（显示侧 `show_image`/`task_handler`），fc 不固定（430/840）累积型；`DIAG_SKIP_AI`（跳过 chn2 取流+AI）不死机；chn2 只在检测帧取流后跑 9000+ 帧不死机——**三轮二分确认**。
- **根因**：face_detect 是唯一双通道且 AI 通道最大的脚本（chn2 1024×768 RGB888P = 2.25MB/帧）。持续大分辨率 DMA 取流与显示 DMA（OSD1 show_image + OSD2 LVGL FULL flush）长期竞争累积 → 显示侧永久阻塞。已知历史坑（478 行：sensor DMA 与 LVGL flush DMA 争用卡死）。
- **修复**：`scripts/face_detect/app.py` `chn2 snapshot` 移入 `do_det` 块——非检测帧（无人脸跳帧时）不再取 chn2 帧，chn2 DMA 取流减半；有脸时每帧检测每帧取流保实时。
- **验证**：板端无人脸静止跑 9000+ 帧不死机（原 430/840 帧死机）；`py_compile` 通过；`test_face_detect_template` 仅 1 基线既有 FAIL。
- **诊断清理**：`DIAG_SKIP_AI` 常量、`pre-show`/`post-task` 插桩打印已删除。

### 2026-08-03 补充：清除人脸重启回魂修复 + 识别降频提帧率

- **问题 1（清除回魂）**：点"清除"后 `[FaceDB] cleared (memory, clear_dirty)` 只清内存，写盘 deferred 到退出 `finally`——用户清除后**断电/异常重启**（未走正常退出）时磁盘旧文件未被覆盖 → 重启加载旧 3 个。
  - 修复：`_on_clear_clicked` 置 `_pending_clear_flush` 标志，主循环 `task_handler` 前安全窗口（同注册即写时机，坑#2 不冲突）立即 `flush_to_disk()` 写空库；退出 `finally` 兜底保留。
- **问题 2（识别灵敏度→帧率）**：多人识别帧 ~10 次 `gc.collect()`（全堆扫描 5~20ms/次）吞帧时；识别更新每帧/每 3 帧一次过于频繁。用户要求降低识别灵敏度换高帧率。
  - 修复：检测统一每 2 帧一次（有脸/无脸，框滞后≤1帧）；识别降频 1 人每 2 帧 / 2~3 人每 4 帧 / ≥4 人每 6 帧（ID 更新延迟≤200ms），非识别帧复用上轮结果，主机数据连续。K2 注册强制本轮识别。
  - 效果：平均 NPU 推理 + GC 工作量大幅下降 → 帧率显著提升。
- **验证**：`py_compile` 通过；`test_face_detect_template` 仅 1 基线既有 FAIL。板端复验：① 清除后立即断电重启 → 0 人；② 单人/多人场景帧率对比。

### 2026-08-03 补充：全脚本审查与同构修复（卡死/回魂/误匹配/死锁）

- **审查**：对 face_detect 已修复的 4 类问题全面排查其余脚本，发现 5 类同构问题。
- **修复**：
  1. **卡死（4 脚本）**：`body_detect`/`gesture_detect`/`object_detect`/`object_classify` 移植降频模式——`DET_INTERVAL=2` 检测/识别一体降频（非检测帧用缓存 det/rec 结果画框，ID 与框同源无窜脸）、chn2 只在检测帧取流（减半大分辨率 DMA 与显示 DMA 竞争）、run 后立即 `gc.collect()`（坑#16 帧内回收）、帧内 ID 去重、注册面积门槛（body/gesture）。
  2. **回魂（5 脚本）**：`body`/`gesture`/`object_detect`/`object_classify`/`color_detect` 加 `_pending_clear_flush`——清除置标志、主循环 task_handler 前安全窗口写空库（防断电重启旧数据回魂）。
  3. **误匹配（2 模块）**：`body_db`/`object_classify_db` 阈值 0.75→**0.82** + `*_MATCH_MARGIN=0.06` 次佳区分；`object_classify_lock.select_lock_index` 同步加 margin 防相似物体锁错。
  4. **死锁（road_detect）**：clear/save 回调内直接 `flush_to_disk`（坑#2 死锁风险）改 `_pending_flush` 主循环安全窗口写盘。
  5. **object_classify 索引错位：核实为误报**——`run()` 内 `det_res`/`feat_res` 同步过滤（过小框跳过时同步），返回值等长同序，无错位。
- **教训**：gesture/object_detect/color 三脚本首次移植因 multi_edit 部分失败后只补零碎、旧 AST 测试不覆盖新逻辑而**误判完成**；code-simplifier 审查暴露后补齐，并给 5 个脚本全部补了契约测试（`DET_INTERVAL`/帧内 gc/`_pending_clear_flush`/`flush_to_disk`/回调禁 flush）。另发现全量回归运行器对**不存在的测试文件误判 PASS**（如 test_color_detect_app/test_road_detect_app），已改用 Test-Path 预检 + 真实清单。
- **验证**：全量 29/29 PASS（真实文件清单，含新契约）。

## 2026-08-03 分支工作流规则（长期有效）

**规则：日常修改/维护一律在 `main-work` 分支进行并 push；功能验证通过后合并到 `main` 再 push；main 不直接开发。**

1. **开发**：新任务先 `git checkout main-work`（不存在则 `git checkout -b main-work main` 并 `git push -u origin main-work`），所有提交推送到 `origin/main-work`。
2. **合并**：功能完成并验证后 `git checkout main && git merge --no-ff main-work && git push`。用 `--no-ff` 保留合并历史。
3. **main 保护**：main 只收验收过的合入，不做直接开发提交；紧急修复也走 main-work → 合并。
4. **仓库内容**：main 只含板端运行代码（main.py / comm / config / core / hw / resource / scripts / ui / 通讯协议.txt / 项目记录.md / .gitignore）。AI 工具配置（.agents/.claude/.superpowers/.reasonix）、项目工具/文档/测试（demo/tools/docs/tests/CLAUDE.md/根目录散落脚本）一律不入库——.gitignore 已兜底，历史误入库的文件用 `git rm --cached` 解除跟踪（本地磁盘保留）。
5. **合并注意**：若 main 与 main-work 对已删工具文件（如 tests/ 下文件）有 modify/delete 冲突，以 main 的删除为准（工具文件不入库）。

## 2026-08-06 AI 脚本运行几分钟死机再修 — chn2 大帧存活期过长（原生缓冲与显示 DMA 长期共存）

- **现象**：用户板端复测，除颜色识别/标签识别/未开发完全脚本外，face_detect / object_detect / body_detect / gesture_detect / object_classify 运行几分钟仍死机（无 traceback）。
- **对照**：不死机脚本（color/tag/road/image_classify）共同点 = 无 chn2 AI 通道、无 kmodel 推理；死机脚本共同点 = chn2 XGA 1024×768 RGBP888（2.25MB/帧）+ kpu 推理 + LVGL FULL 显示。官方 demo 全部单通道，本项目多通道 chn2 是独有风险（官方 AIBase.run 每帧仅新建 ai2d 输入包装 + kpu 输出 to_numpy 数组，其余复用——本地与其差异主要在 chn2 大帧 + LVGL 组合）。
- **根因分析**：历史修复（chn2 只在检测帧取流）仅把死机从 430/840 帧推迟到 9000+ 帧（≈5 分钟@30fps，与"运行几分钟"吻合）——**未根治**。剩余累积源：每检测帧 `snapshot(chn=2)` 分配 2.25MB 原生缓冲，`img_ai`/`img_np` 存活整个 on_frame（含画框/发送/显示阶段），与显示 DMA（OSD1 show_image + OSD2 LVGL FULL flush）长期共存竞争累积；且 K230 sensor 通道 DMA 持续运行（snapshot 只是从缓冲池取帧），chn2 大帧带宽压力始终存在。
- **改动**：5 个 AI 脚本 on_frame 推理完成后立即 `del img_np` / `del img_ai`（引用计数清零 → 帧内 gc 及时回收 2.25MB 原生缓冲），把大帧存活期从"整个 on_frame"缩短到"仅推理期"：
  - face_detect：识别/注册循环全部完成后（检测块末尾，img_np 最后用于 K2 注册）
  - object_detect / body_detect / gesture_detect / object_classify：run 之后、帧内 gc.collect() 之前
  - 顺序约束：先 `del img_np`（numpy 视图持有 image 缓冲引用）再 `del img_ai`。
- **验证**：新增 5 个 AST 契约测试 `test_on_frame_releases_chn2_frame_after_inference`（on_frame 必须 del img_np + del img_ai 且位于 chn2 snapshot 之后，注释行不计）——Red → Green 全 PASS；`py_compile` 通过；全量回归 exit-code 判定 55/59（4 个 FAIL 为基线既有：test_face_register/test_object_db/test_tag_db/test_template，stash 对比确认与本轮无关）；code-simplifier 审查无简化项（del 顺序与根因注释有保留价值）。
- **板端复验清单**：各 AI 脚本有人/无人场景各跑 15+ 分钟，观察 fc 持续增长 + mem_free 趋势（脚本已有 fc%30 打印 + fc%300 mem_free 打印）；关注 mem_free 是否单调下降（Python 堆泄漏）还是平稳后死机（原生缓冲/DMA 累积）。
- **若仍死机（下一步方向，需板端插桩二分）**：① DET_INTERVAL 2→3/4（继续降低 chn2 取流频率，牺牲实时性）；② chn2 XGA → VGA 降分辨率（RGBP888 2.25MB→0.9MB，DMA 带宽约 -60%；模型输入 320/512 够用，body 640×640 输入需板端重新验证精度）；③ 探索暂停 chn2 通道（K230 sensor 是否支持 per-channel 暂停）——从根上消除大帧 DMA 与显示 DMA 竞争。

### 2026-08-06 补充：chn2 降分辨率实施（用户选定根治方向）

- **调查确认**（官方 `k230_canmv/.../builtin_py/media/sensor.py` 源码核实）：① `sensor.run()` 后 chn2 通道硬件 DMA **持续**以 30fps 搬运 2.25MB/帧（与是否 snapshot 无关，snapshot 只是从 VB 池零拷贝取帧）；② 缓冲池固定数量（k230d 默认 4~6 块），满池丢帧/超时；③ **无 per-channel 暂停 API**（`stop()` 停整设备且需 reset 重建，运行期无法改 chn2）；④ 官方 demo 全部单通道，本项目"单设备 chn0+chn2 双通道 + LVGL FULL"是独有组合。→ 之前 DET_INTERVAL 降频只减 CPU 侧取帧/持有时间，**不减硬件 DMA**，只能推迟死机（与"9000+ 帧才死机"证据吻合）。
- **实施**（chn2 降分辨率，按各模型输入尺寸）：
  - `face_detect` / `object_detect` / `object_classify`：chn2 XGA 1024×768 → **VGA 640×480**（2.25MB→0.9MB/帧，DMA 约 -60%；det 输入 320×320 足够）
  - `gesture_detect`：chn2 XGA → **SVGA 800×600**（1.44MB/帧，DMA 约 -36%；hand_det 输入 512×512，800 高足够）
  - `body_detect`：**保持 XGA**（person_detect 模型输入 640×640 需 ≥640 高度源，降分辨率不可行——板端需单独验证长稳）
  - 改动点：`core/app_runtime.py` `_channels_for` 5 个分支 + `core/face_ai.py`/`object_ai.py`/`object_classify_ai.py`/`gesture_ai.py` 的 `RGB888P_SIZE` + `scripts/object_detect/app.py` 的 `RGB888P_W/H`（该脚本用自有常量，勿漏）+ `core/body_ai.py` 注释说明保持原因。
- **验证**：4 个 `_channels_for` 契约断言 XGA→`Sensor.VGA`/`Sensor.SVGA`（Red→Green）；object 测试改为 `Sensor.VGA` 切片断言（防被 chn0 VGA / 后续分支注释的 "VGA" 误兜底）；注释精简防断言窗口被推出切片（教训：AST 切片断言对注释长度敏感）；全量回归 55/59（4 个基线失败不变）；py_compile 通过；code-simplifier 审查无简化项。
- **注意**：① `Sensor.SVGA` 若板端固件 framesize 枚举缺失，gesture 需回退 XGA（板端确认）；② chn2 降分辨率后小目标检出可能略降（源分辨率降低），板端需复验精度；③ `hw/lcd.py` 旧架构仍有 XGA 配置（弃用代码，import 不执行方法体，未改）。
- **板端复验清单**：各 AI 脚本有人/无人场景跑 15+ 分钟长稳（fc 持续增长 + mem_free 趋势）；face/object 小目标检出对比；gesture 手部检测正常；body_detect 单独长稳（未降分辨率）。

## 2026-08-07 object_detect 单通道化根治死机 + 识别恢复 + 切换提速（字体按需 + 耗时插桩）

### 现象（用户板端反馈）
1. 8-06 降 chn2 分辨率后 object_detect 仍**死机**，且**识别不了东西**；
2. 模式切换时间太久。

### 根因
1. **死机未根治**：历史确认根因是 chn2 硬件 DMA 持续 30fps 搬运大帧与显示 DMA（OSD1+OSD2 LVGL FULL）竞争累积。降分辨率只把带宽 2.25MB→0.9MB/帧（-60%），**没清零**，推迟而非根治，与"仍死机"吻合。
2. **识别不了**：① 降 VGA 后小目标 conf 低于 0.5 阈值漏检；② chn2 缓冲池满/竞争时 `snapshot(chn=2)` 超时/异常被 except 吞掉 → 空结果；③ 预存 bug（code-simplifier 发现）：`on_frame` 的 `global` 声明缺 `_last_names` → 非检测帧读 `_last_names` 抛 **UnboundLocalError**，on_frame 中断、host_tick 不发、每 2 帧报错（`object_classify` 同构，见后续）。
3. **切换慢**：reset 架构每次全量 init；其中 `fonts.load_all()` 每次加载 3 个字体 + 3 次 Phase2 探测（脚本 UI 实际只用 body），且 init 各阶段耗时从未量过。

### 修复（TDD Red→Green，commit 全绿）
1. **object_detect 单通道化**：`_channels_for` 该分支不再配 chn2（仅 chn0 VGA RGB888）；`on_frame` 直接用传入的 chn0 显示帧 `img.to_numpy_ref()` 推理（官方 ai_lvgl 同构），去掉 `snapshot(chn=2)`/`img_ai`/`del img_ai`。硬件层面消除 chn2 DMA → 死机根治方向。
2. **阈值 0.5→0.3**：补回小目标检出（若板端误检多回调 0.35/0.4）。
3. **预存 bug 修复**：on_frame `global` 补 `_last_names`。
4. **字体按需加载**：`font_manager` 新增 `load(*levels)`（`_probe_phase2` 探测缓存 + `_load_level`），`load_all` 委托 `load("title","body","caption")` 保留给主菜单；`init_app` 按 category 只 `fonts.load("body")`（color_detect/road_detect 补 caption）。
5. **init_app 耗时插桩**：Display/sensor/MediaManager/lvgl/fonts/services/total 各阶段 `ticks_ms` 打点打印，板端看切换耗时分布再精准优化。

### 验证
- 契约测试 Red→Green：`test_app_runtime_object` 单通道断言（无 chn2/RGBP888）、`test_object_detect_app` 单通道 on_frame + 阈值 0.3 + `global _last_names`、新增 `test_app_runtime_perf`（字体按需 + 插桩）、`test_face_register` fonts 断言改按需（范围限定 init_app 函数体，init_menu 全量加载合法）。
- 全量回归 56/60（4 历史基线失败不变：test_face_register 过时断言 / test_object_db、test_tag_db import 路径 / test_template 过时断言）；py_compile 通过。
- code-simplifier 审查：发现并修 `_last_names` bug + 应用安全简化（O1 拆分笔误 `_id_registry = None_object_det`、O2 `_rect_area` helper、O3 冗余条件、O4 过时注释、O5 冗余 `import gc as _gc`、F1 未用变量、F2 `load_all` 冗余早退）。内置 review 两轮：warn 无阻塞。
- 教训：AST 文本断言会扫到注释（CLAUDE.md 已知）——本轮注释里写 "load_all" 被 `test_face_register` 扫到 FAIL，改措辞为"全量加载"规避；断言范围收窄到 init_app 函数体而非整个文件。

### 板端复验清单（下一步）
1. object_detect 有人/无人 15+ 分钟长稳（fc 持续增长 + mem_free 趋势）——**死机是否根治**；
2. **chn0 RGB888 喂 AIBase 兼容性**（最大不确定性）：正常出框即 OK；若 run 报错，回退方案 = object_ai 里 ai2d 改 RGB888 输入或恢复 chn2（git revert 单文件）；
3. 识别对比：小目标检出是否恢复；误检率（阈值 0.3）是否可接受，多则回调 0.35/0.4；
4. 切换耗时分布：串口看 init_app 各阶段 ms，决定下一步优化重点（kmodel 加载 / MediaManager 等大头）；
5. 若单通道方案稳定 → 推广 face_detect/object_classify（object_classify 顺带修 `_last_names` 同构 bug）；gesture/body 因模型输入需 ≥512/640 高源保持 chn2，单独长稳验证。

### 遗留（非阻塞）
- font_manager `load()` 为一次性加载（`_loaded` 门闩）：每进程只调一次，当前无触发路径；若未来需追加加载 level 再改。
- 插桩打印为累计值标签（相对 _t0），板端读分布时可自行换算分段耗时。

### 板端反馈（2026-08-07，用户真机验证）
- **现象**：单通道化后能出框（chn0 RGB888 喂 AIBase 兼容性 OK，死机问题待长稳复验），但**框比物体大一圈/包背景**。
- **根因**：阈值 0.3 放出的低置信度松框（YOLO 低 conf 框边界估算差）+ 同帧推理后框严格贴显示画面（以前 chn2 XGA 推理+chn0 显示视野不一致时框偏小，视觉反差）。
- **修复**：阈值 **0.3 → 0.4**（折中：滤低质量松框，仍低于原始 0.5 保留部分小目标）。契约 `test_confidence_threshold_040` Red→Green。
- **若 0.4 仍松/漏**：松→0.45~0.5，漏小目标→0.35；阈值是单常量，板端可反复微调。

### 板端反馈（2026-08-07 第二轮）：全屏假框根因定位 — RGB888 packed vs RGBP888 planar 布局
- **现象**：阈值 0.4 后仍是"框很大/框键盘一半/垂直超界"。板端 DBG 日志：`img=640x480 np.shape=(480, 640, 3)`（帧格式正常），但 `det0=[1.0, 1.0, 630.0, 478.0, 0.52, 0]` —— **全屏框**（几乎覆盖全图、置信度仅 0.52）。坐标映射/postprocess 全正常 → 模型输入的 320×320 图本身异常，YOLO 把整张错乱图当一个大目标。
- **根因**（用户拷出板上 `libs/AIBase.py` + `libs/AI2D.py` 源码确认）：`AIBase.preprocess` 原样 `self.ai2d.run(input_np)`、`Ai2d.run` 原样 `nn.from_numpy(input_np)` —— **输入 numpy 必须与 `ai2d.build([1,3,H,W])` 的 NCHW 语义匹配**。而 `Sensor.RGBP888` = **planar CHW**（hw/lcd.py:69 明注：PIXEL_FORMAT_RGB_888_PLANAR），chn2 帧 to_numpy_ref 天然 planar 零转置；chn0 的 `Sensor.RGB888` 是 **packed（RGBRGBRGB 交错）**。单通道化后直接喂 packed → ai2d 按平面错读 → 错乱图 → 全屏假框。lcd.py:72 早有记录："早期误用 RGB888 interleaved + 软件转置 CHW 喂 NPU 卡死"——本项目 AI 从未吃过 chn0 RGB888，官方 ai_lvgl 标杆也是 chn2 RGBP888。
- **修复**：on_frame 把 chn0 packed RGB888 逐通道重排为 **planar (3,H,W)** 再喂 `_object_det.run()`：
  ```python
  _planar = np.zeros((3, img.height(), img.width()), dtype=np.uint8)
  _planar[0] = img_np[:, :, 0]; _planar[1] = img_np[:, :, 1]; _planar[2] = img_np[:, :, 2]
  img_np = _planar
  ```
  ai2d build 声明 [1,3,H,W]，喂省略 batch=1 的 (3,H,W)（板端实测通过）。`del _planar` + `del img_np`（同对象双名，防坑#16 回收失效）。ulab 切片赋值先例：demo 实验13 `draw_img_np[...] = self.shang_argb`。
- **契约**：`test_on_frame_converts_packed_rgb888_to_planar` Red→Green（断言 np.zeros + 三通道切片，注释行不计）。
- **验证**：全量回归 56/60（4 历史基线失败不变）；py_compile 通过；code-simplifier 审查：修复正确、del 顺序必要、根因注释与源码事实一致；DBG print 暂留（板端验证修复后删）。
- **板端复验**：① det0 应从全屏框变成紧贴键盘的合理框（DBG 日志可见）；② 画框应正常包住物体；③ 长稳 15 分钟（单通道 + planar 转换性能）。
- **后续**：验证通过后删 DBG 插桩；face/object_classify 单通道化时复用同一 packed→planar 转换（建议抽成 core 公共 helper）；若板端 `dir(nn.ai2d_format)` 有 packed RGB888 枚举，可评估 ai2d 输入声明零拷贝方案。

### 板端反馈（2026-08-07 第三轮）：全屏假框修复确认 + FPS 优化
- **验证**：packed→planar 重排修复生效，框正常包住键盘（det0 从全屏 [1,1,630,478] 变为合理框）。**chn0 RGB888 单通道链路闭环**。
- **新问题：FPS 偏卡**。瓶颈构成：① 纯 Python postprocess（YOLOv8 8400 候选遍历+NMS，无 aidemo C 加速，demo 同款）；② 每检测帧 np.zeros 921KB 分配+清零+3 通道拷贝；③ gc.collect()；④ DBG print（UART 拖帧）。
- **优化（已实施）**：① 删 DBG 插桩；② `DET_INTERVAL` 2→3（检测频率降 1/3，框滞后 ≤3 帧≈100ms 可接受；契约 `test_det_interval_is_3_for_fps` Red→Green）。若仍卡：DET_INTERVAL→4，或评估 ai2d 输入声明 packed 格式零拷贝（需板端 `dir(nn.ai2d_format)` 确认枚举）。
- **验证**：全量回归 56/60（4 历史基线失败不变）；py_compile 通过；code-simplifier 无简化项。
- **板端复验**：看 fc%30 打印算帧率对比；确认画框实时性（滞后≤3 帧）。

### 板端反馈（2026-08-07 第四轮）：死机根治确认 + 零拷贝 packed 输入
- **死机根治确认**：fc 跑到 10000+ 无死机（历史 430/840 → 9000+ → 10000+）。单通道化从硬件层面消除 chn2 DMA 与显示 DMA 竞争，**根治达成**。
- **帧率优化（选 B）**：`core/object_ai.py` ai2d 输入格式探测——`hasattr(nn.ai2d_format, "RGB888p_FMT")`（K230 ai2d 中 p=packed，与 Sensor.RGBP888 的 P=planar 命名相反，坑）；有则 `set_ai2d_dtype(RGB888p_FMT, NCHW_FMT)` 声明 packed 输入 → chn0 帧零拷贝直接喂（免每检测帧 np.zeros 921KB + 3 次通道重排），无则退回 NCHW + 软件重排（现状保底）。`ObjectDetectionApp.input_is_packed` 标志，app.py on_frame 按标志分支。
- **契约**：`test_init_tries_packed_rgb888_input_format`（object_ai）+ `test_on_frame_branches_on_input_is_packed`（app）Red→Green；code-simplifier 合并 set_ai2d_dtype 双分支为 getattr 默认值（8 行瘦身，行为不变）。
- **验证**：全量回归 56/60（4 历史基线失败不变）；py_compile 通过；code-simplifier 通过。
- **板端复验**：① 框仍正常（RGB888p_FMT 生效零拷贝 or 自动退化 planar）；② fc%30 帧率对比（零拷贝路径应明显提升）；③ 若假框复发 → 说明 RGB888p_FMT 语义非 packed，报告后改回强制 NCHW+重排。

### 板端反馈（2026-08-07 第五轮）：零拷贝验证通过，object_detect 闭环
- **验证**：`RGB888p_FMT` 存在且为 packed 语义，零拷贝路径生效（无全屏大框）；fc 速率 30（满帧率，卡顿消除——之前卡 = DBG print + 每帧 921KB planar 分配 + DET_INTERVAL=2）。
- **object_detect 最终状态**：死机根治（fc 10000+）+ 检测精准（packed→planar 布局正确）+ 满 30fps（零拷贝）。三项目标全部达成。
- **推广待办**：face_detect / object_classify 单通道化复用同一套方案（_channels_for 去 chn2 + 布局转换/零拷贝探测 + DET_INTERVAL 调优）；object_classify 顺带修 `_last_names` global 同构 bug。

## 2026-08-07 object_classify 单通道化推广（死机根治 + 布局转换 + _last_names 修复）

### 改动（复用 object_detect 已验证方案）
1. `core/app_runtime.py` `_channels_for`：object_classify 分支去 chn2（单通道仅 chn0 VGA RGB888）。
2. `core/object_ai.py`：抽公共 helper `configure_ai2d_input(ai2d)`（RGB888p_FMT 探测 → 返回 input_is_packed），ObjectDetectionApp 复用。
3. `core/object_classify_ai.py`：FeatureExtractionApp 改用 `configure_ai2d_input`（det/feature 共用同一输入格式，packed 路径必须同步否则错读）；`ObjectClassifyRecognition` 暴露 `input_is_packed`（转发 detector）；顶部过时 chn2 注释清理。
4. `scripts/object_classify/app.py`：on_frame 去 chn2 snapshot，AI 直接吃 chn0 帧（`img.to_numpy_ref()`），按 `input_is_packed` 分支零拷贝 or planar 重排；**顺带修预存 `_last_names` global bug**（与 object_detect 同构，非检测帧 UnboundLocalError）。
5. DET_INTERVAL 保持 2（object_detect 卡是因 DBG+planar 分配+间隔，classify 板端验证 fps 后再调）。

### 验证
- 契约 Red→Green：`test_object_classify_detect_ast` 单通道/planar/global/ai 探测 5 处更新+新增；`test_object_ai` helper 断言同步。
- 全量回归 56/60（4 历史基线失败不变）；py_compile 通过；code-simplifier 审查通过（无简化项，1 注释清理已做）。

### 板端复验清单
1. object_classify 有人/物体场景 15+ 分钟长稳（fc 持续增长 + mem_free 平稳）——死机根治；
2. 框精准（无全屏假框）、锁定/触摸/K2 注册正常；
3. fps（fc%30）对比，卡则 DET_INTERVAL 2→3 或调 rec 提特征频率；
4. 若假框复发 → RGB888p_FMT 语义异常，回退强制 NCHW+planar 重排（configure_ai2d_input 返回 False）。

## 2026-08-07 object_classify 显示框与学习反馈修复（用户反馈第二轮）

### 现象（用户反馈）
1. 识别模式全屏框只能看到左右两边，上下看不到；
2. 按下 K2 学习后，画面上没有标记对应槽位的 ID。

### 根因
1. **上下边被栏盖住**：`on_frame` 里两处 `draw_rectangle(0,0,639,479)`（识别全局框 + 学习确认闪烁框）画整屏框，但顶栏（y 0..51）与底栏（y 428..479）是不透明 LVGL 对象（BAR_BG，bg_opa 255），盖住 OSD1 上下两条边，只剩左右可见。
2. **学习无槽位标记**：`_learn_center` 注册成功只置 `_record_flash`（全屏黄框闪若干帧），未记录槽位与位置，闪烁帧只画框不画 ID。

### 解法
1. 框改画预览区可见边界：新增模块级常量 `PREVIEW_X1 = DISPLAY_SIZE[0]-1`、`PREVIEW_Y1 = PREVIEW_Y + PREVIEW_H - 1`，识别全局框与闪烁框统一 `draw_rectangle(0, PREVIEW_Y, PREVIEW_X1, PREVIEW_Y1, ...)`（两处重复魔法表达式提取为常量）。
2. 学习反馈标槽位：新增 `_record_slot`/`_record_pos`（模块级），`_learn_center` 注册成功保存槽位与物体中心显示坐标；闪烁分支（`_record_flash > 0`）在物体中心上方画 `"ID%d" % _record_slot`（draw_string_advanced，24 号，黄色 BOX_LOCK）。

### 验证
- 契约 Red→Green：`test_object_classify_detect_ast` 新增 `test_global_boxes_clipped_to_preview_area`（断言边界常量定义 + on_frame 引用 + 不再出现整屏高度 DISPLAY_SIZE[1]）、`test_record_flash_marks_slot_id`（断言 _record_slot 保存 + 闪烁帧画 ID 标签）；先跑 FAIL 确认 Red，实现后全 PASS。
- 全量回归：除 4 个历史基线失败（face_register/object_db/tag_db/template，已 stash 对比验证与本次改动无关）外全绿；py_compile 通过；code-simplifier 优化（边界魔法表达式提取常量）后复测仍全绿。
- 板端复验清单：① 识别模式四边框上下边可见；② K2 学习成功后闪烁帧在物体上方显示 ID%d（对应选中槽）；③ 学习与识别功能不回归。

### 板端反馈修正（2026-08-07）：识别框颜色与左上角 ID 可见性（用户澄清视觉预期）
- **用户澄清识别逻辑**：命中已学物体的预览区四边框应为**白色**（非金色），左上角标记对应槽位的 **ID 且按槽位颜色**（1~4 历史绿/蓝/橙/紫 + 5~25 色环，BOX_COLORS）。
- **修正**：① 新增 `BOX_ACTIVE = 0xFFFFFF` 白色常量，识别全局框改用它（学习确认闪烁框仍用 BOX_LOCK 金黄，两种状态视觉区分）；② 左上角 ID 由 `" ".join` 统一金色改为**每个命中槽位独立用 `BOX_COLORS.get(slot, BOX_UNKNOWN)` 颜色**水平排列绘制。
- **隐藏坑（重要）**：原左上角 ID 画在 `y=6`，位于顶栏（0..51，BAR_BG 不透明）**之内被盖住**——这就是"看不到槽位标记"的真正根因；改为 `PREVIEW_Y + 4`（预览区左上角内侧）才可见。同类问题：识别框/闪烁框原画整屏 (0,0)~(639,479)，上下边同样被顶/底栏盖住（前轮已修）。
- **契约更新**：`test_recognition_id_at_frame_corner` 改为断言 BOX_ACTIVE（白框）+ BOX_COLORS（槽位色）+ 标签位于 PREVIEW_Y 之下；Red→Green 后全量回归除 4 历史基线失败外全绿；code-simplifier 优化（`"ID%d" % slot` 提局部变量 `tag`）后复测仍全绿。

### 板端反馈修正（2026-08-07）：框/ID 统一槽位色 + 学习反馈 ID 移左上角（用户确认视觉方案）
- **用户确认（第三轮）**：识别逻辑已对；但① 框的颜色也要和 ID 一样按槽位显示 5 个不同颜色（不再用白色/黄色固定色）；② 学习（K2 确认闪烁）时中央十字架附近不显示 ID 内容，左上角显示即可。
- **修正**：删除 `BOX_ACTIVE`（白）与 `BOX_LOCK`（黄）常量，全部改按槽位取色 `BOX_COLORS.get(slot, BOX_UNKNOWN)`：识别框 = 最佳命中槽位色，闪烁框与左上角 ID = 学习槽位色（框与 ID 同色，`flash_color` 局部变量一次取色）；闪烁帧 ID 从物体中心（十字架附近，曾用 `_record_pos` 锚点）移到预览区左上角 `(8, PREVIEW_Y+4)`，`_record_pos` 变量删除。
- **契约更新**：`test_recognition_id_at_frame_corner` 断言删除 BOX_ACTIVE/BOX_LOCK（全文件不得出现）；`test_record_flash_marks_slot_id` 断言 `_record_pos` 删除 + 闪烁 ID 在 `(8, PREVIEW_Y+4)`；Red→Green 后全量回归除 4 历史基线失败外全绿；code-simplifier 优化（闪烁分支框/ID 同色提 `flash_color`）后复测仍全绿。
- **板端复验**：命中已学物体 → 预览区框与左上角 ID 同槽位色（槽 1 绿/2 蓝/3 橙/4 紫/5 红）；K2 学习成功 → 预览区闪烁框 + 左上角 ID 同为所学槽位色，中央十字区无 ID 文字。

### 板端反馈修正（2026-08-07）：识别/闪烁框底边线被底栏盖住（线宽外扩）
- **现象（用户反馈）**：框的上下左右线都能看到，唯独**底部那条线看不到**。
- **根因**：`draw_rectangle` 线宽向边界外扩展。底部线画在 `PREVIEW_Y1=427`（预览区最后一行），线宽外扩部分落在 bottom bar（y 428 起，不透明）内被盖住，只剩 1px 几乎不可见。
- **解法**：抽公共 helper `_draw_preview_box(img, color, thickness)`，y1 用 `PREVIEW_Y1 - thickness` 内收一个线宽，保证底边整条线（含线宽）落在预览区可见范围内；识别框（thickness=4）与学习闪烁框（thickness=6）两处重复的 `draw_rectangle` 调用统一走 helper（code-simplifier 风格：合并重复 + 坑因注释集中在 helper）。
- **契约更新**：`test_global_boxes_clipped_to_preview_area` 改为断言 helper 定义与 on_frame 调用；新增 `test_global_box_bottom_inset_by_thickness` 断言 `PREVIEW_Y1 - thickness` 内收；Red→Green 后全量回归除 4 历史基线失败外全绿。

### 板端反馈修正（2026-08-07）：全屏框贴满可视区 + 任意物体可学可识别（YOLO 退化中心区域）
- **用户反馈**：① 强调"全屏框"= 顶栏高度与底栏高度之间的可视区全屏框（框须贴满 52..427）；② 现在只能学几种物体（COCO80 类内），很多物体按 K2 触发蜂鸣警告。
- **根因**：① 上轮底部内收 thickness 后底边距底栏留空隙，框未贴满可视区；② 学习依赖 YOLOv8n（COCO80 类）检测中心框，`pick_box_at_point` 无命中 → 蜂鸣；非 COCO 类物体检测不到无法学习；识别同样依赖 YOLO 框，学完也识别不到。
- **解法**：① `_draw_preview_box` 底部 y1 改 `PREVIEW_Y1 - thickness + 1`（线宽向边界外扩，内收 thickness 再 +1 贴底栏上边缘，四边完整可见且框填满可视区）；② 新增 `CENTER_BOX = [208, 128, 432, 352]`（VGA 画面中心 224×224，rec 输入尺寸）：`_learn_center` 在 YOLO 无中心框时退化用中心区域提特征注册（任意物体对准中央即可学，蜂鸣仅保留给提特征失败）；on_frame 识别模式在 `_ocr.run` 无框时同样退化中心区域提特征匹配（任意已学物体对准中央可识别），det_boxes 用 `[CENTER_BOX + score/class]` 补位供画框/上报。
- **契约更新**：`test_global_box_bottom_inset_by_thickness` 断言改 `PREVIEW_Y1 - thickness + 1`；新增 `test_center_fallback_for_any_object`（CENTER_BOX 常量 + 学习/识别两处退化引用）；Red→Green 后全量回归除 4 历史基线失败外全绿。
- **板端复验**：① 识别/学习确认框四边完整可见且顶贴顶栏、底贴底栏；② 任意物体（含非 COCO80 类）对准中央按 K2 可学习成功（闪烁 + 左上角 ID），识别时对准中央可命中；③ COCO80 类物体原有学习/识别不回归。

### 板端反馈（2026-08-07）：单通道版随机硬卡死（画面卡住+串口静默）——死机二分定位
- **现象（用户反馈）**：最新版（含 CENTER_BOX 学习/识别退化）随机**硬卡死**：画面完全卡住、串口不再打印；用户实测**前两三次修复（纯 YOLO 路径）不会卡死**。不是内存耗尽重启（无重启、无 MemoryError），是 NPU/驱动级挂死（非 Python 异常，try/except 无法捕获）。
- **根因定位（二分）**：前几版（框位置/颜色/五色/底边内收）纯 YOLO 匹配路径板端验证不死；第五轮唯一改变 NPU 调用模式的改动是 **CENTER_BOX 退化**——识别模式每 DET_INTERVAL 帧在 YOLO 无框时 `det.run` 后紧接 `extract_feature → feature.run`（双 kmodel det 320 + rec 224 交替），偶发 NPU 挂死。`AIBase.run` 的 `self.cur_img = input_np` 会让两个 AIBase 实例跨帧持有输入（planar 921KB 峰值 ×2），叠加交替推理，随机触发驱动死锁。
- **本轮处理（二分验证版）**：① 移除**识别模式**持续退化（恢复纯 YOLO 匹配——回到验证过不死的路径）；② 保留**学习退化**（用户核心需求"任意物体可学"，K2 低频触发）；③ 关键点插桩 `[object_classify] DET n=N` / `LEARN det n=N` / `[objcls] run det/feat done` / `extract start/done`——若仍死机，串口最后打印可精确定位挂死点（det.run / feature.run / config_preprocess / extract）。
- **契约更新**：`test_center_fallback_learn_only`（原 test_center_fallback_for_any_object）改为断言学习退化保留 + **on_frame 不再出现 CENTER_BOX**（识别退化移除的契约）；Red→Green 后全量回归除 4 历史基线失败外全绿。
- **验证结论预期**：若此版不再死机 → 确认识别退化（双 kmodel 持续交替）是死机源；下一轮再设计低风险识别方案（降频+保护 or 单一 kmodel 复用）。若仍死机 → 插桩日志定位挂死点后根治。**插桩为临时诊断，定位后必删**。
- **潜在根治方向（下一轮）**：双 kmodel 每框 `config_preprocess`（ai2d.build）重建 + 交替 run 的 NPU 状态冲突；可尝试 feature 侧 ai2d build 一次复用、或 det 无框时不走 feature.run、或串行化 NPU 调用。

### 板端反馈修正（2026-08-07）：全屏框底部线根因——draw_rectangle 实为 (x,y,w,h) 语义
- **现象（用户反馈）**：底部那条线始终看不到（顶/左/右可见）；同时用户收敛需求范围：只要"学习任意物体 + 识别时根据已学物体框出正确 ID 槽"。
- **根因（关键）**：K230 media `Image.draw_rectangle(x, y, w, h, color, thickness)` 参数是 **(x, y, 宽, 高)** 语义（OpenMV 风格，项目 body_detect 等所有画框都传 x,y,w,h），此前误按对角坐标 (x0,y0,x1,y1) 传参：`draw_rectangle(0, 52, 639, 424)` 实际是**宽 639、高 424** → 底部线画在 y=52+424=476，完全落在底栏（428 起不透明）内 → 永远不可见。此前多轮"内收 thickness/+1"都是在对角坐标错误假设上打补丁。
- **解法**：`_draw_preview_box` 改标准 `(x, y, w, h)`：`draw_rectangle(0, PREVIEW_Y, DISPLAY_SIZE[0], PREVIEW_H, ...)` → 宽=640、高=376，线宽向内扩展：顶线 52..55、左线 0..3、右线 636..639、底线 424..427，四边完整可见且贴满可视区（52..427）；删除废弃的 PREVIEW_X1/PREVIEW_Y1 常量。
- **契约更新**：`test_global_boxes_clipped_to_preview_area` 改断言 helper 用 PREVIEW_Y + DISPLAY_SIZE[0] + PREVIEW_H（不再有 PREVIEW_X1/Y1）；`test_global_box_bottom_inset_by_thickness` 更名 `test_global_box_bottom_edge_visible` 断言 `DISPLAY_SIZE[0], PREVIEW_H`（宽高语义）；Red→Green 后全量回归除 4 历史基线失败外全绿。
- **功能范围确认**：学习任意物体（CENTER_BOX 退化保留，K2 低频）；识别按 YOLO 检到物体匹配已学特征 → 预览区槽位色框 + 左上角槽位色 ID（v2 交互，用户确认）。识别持续退化（双 kmodel 交替死机嫌疑）保持移除，插桩保留待用户确认死机二分结果后删除。

### 板端反馈（2026-08-07）：死机定位第二轮——draw_rectangle 右边界越界 1px（fc=4200 累积型挂死）
- **插桩日志（用户提供）**：死机前最后打印 `DET n=1`（在 `_ocr.run()` 返回之后），fc=4200（≈140 秒），mem_free=3.6MB 充足，无异常——**时间累积型硬挂死，发生在 run 返回后的主循环阶段**。
- **排除**：① UART1 发送阻塞——`test_host_api_order` 背书"未握手也发数据"是设计，且 TTL 无流控下无对端不阻塞 TX；② 内存耗尽——mem_free 3.6MB；③ cosine 匹配慢——fc≈30fps 正常。
- **新根因（绘制越界）**：上轮改 `draw_rectangle(0, PREVIEW_Y, DISPLAY_SIZE[0], PREVIEW_H)`（x,y,w,h 语义）后，w=640 使右边界 x=640 **越界 1px**（OSD1 为 640 宽，x 0..639）；前几版 w=639 完全不越界所以不死。K230 绘制驱动对越界坐标处理不稳 → 偶发驱动挂死（时间累积型吻合）。日志无 `[objcls]` 前缀说明板端 object_classify_ai.py 未更新（用户只部署了 app.py），死机点定位在 run 之后仍成立。
- **修复**：`_draw_preview_box` 改 `(0, PREVIEW_Y, DISPLAY_SIZE[0]-1, PREVIEW_H-1)`——四边全在 OSD1 内（x≤639, y≤427），底部线 423..426 仍可见（满足用户"看到底部线"诉求）；细分插桩 MATCH/DRAW/TICKED（do_det 帧打）——若仍死机，最后打印精确定位：DET→run 后到匹配前 / MATCH→匹配后到绘制前 / DRAW→绘制后到 host_tick / TICKED→host_tick 后到下一帧 snapshot。
- **契约更新**：`test_global_box_bottom_edge_visible` 改断言 `DISPLAY_SIZE[0] - 1, PREVIEW_H - 1`（不越界 + 底部可见）；Red→Green 后全量回归除 4 历史基线失败外全绿。
- **待用户验证**：部署新版观察是否仍死机；若仍死机发最后打印（DET/MATCH/DRAW/TICKED 定位）；定位后删除全部插桩。

### 板端反馈（2026-08-07）：非 COCO 物体学完移动后回来匹配不上——恢复识别退化
- **现象（用户反馈）**：学习任意物体（非 COCO 类，走中心区域退化）后，移动到其它地方再回来**不能匹配**；只有 YOLO（COCO80）支持的物体能匹配上。死机问题用户暂缓（静止跑测试中）。
- **根因**：识别模式依赖 YOLO 检测框提特征匹配；非 COCO 类物体 YOLO 检不到 → det_boxes 空 → 无特征 → 匹配不到（上一轮死机二分移除了识别退化所致）。
- **解法**：恢复 on_frame 识别退化（与学习退化对称）：`_ocr.run` 返回空时用 `extract_feature(CENTER_BOX, img_np)` 中心区域提特征匹配，det_boxes 补 `[CENTER_BOX + score/class]` 供画框/上报。学习与识别特征同源（CENTER_BOX）→ 学完移动后物体回中央即可命中。
- **契约更新**：`test_center_fallback_for_any_object`（恢复原名）改回断言 on_frame 含 CENTER_BOX（识别退化恢复）；Red→Green 后全量回归除 4 历史基线失败外全绿。
- **注意**：识别退化恢复 = 每 do_det 帧在 YOLO 无框时多 1 次 rec 推理（死机候选路径之一，死机根因越界修复 3e3e401 已另行处理，用户静止测试中）；插桩（DET/MATCH/DRAW/TICKED + [objcls]）保留定位。

### 架构简化（2026-08-07 用户确认）：object_classify 去掉 YOLO，单 recognition 模型
- **用户决策**：只要 recognition 模型，认物体长什么样并正确匹配即可。去掉 YOLO 后：学习与识别统一对画面中心固定区域（CENTER_BOX 224×224）提特征，任意物体对准中央可学可识别，不受 COCO80 类别限制；**同时消除双 kmodel（det 320 + rec 224）交替推理——死机最大嫌疑路径，可能顺带根治死机**（待板端静止测试确认）。
- **改动**：
  - `core/object_classify_ai.py`：`ObjectClassifyRecognition` 加 `use_det` 参数（默认 True 兼容；False 不建 ObjectDetectionApp，`self.detector=None`，`input_is_packed` 取 feature 的）；`run()` 单模型模式防御返回空；`deinit` 判空。
  - `scripts/object_classify/app.py`：`_init_ai` 传 `use_det=False` 只加载 rec kmodel；`_learn_center` 直接 `extract_feature(CENTER_BOX)`（删 YOLO 检测 + pick_box_at_point）；`on_frame` 识别模式每 DET_INTERVAL 帧中心区域提特征 + database_search 匹配；删除 `pick_box_at_point`/`OBJ_DET_KMPATH` 未使用 import；文件头/函数 docstring 更新。
- **能力变化**：只能识别画面中央一个物体（中心区域固定）；不再支持多物体/任意位置识别（YOLO 时代的能力，用户接受）。
- **契约更新**：`test_learn_center_picks_center_box`→`test_learn_center_center_only_no_yolo`（禁 pick_box_at_point/detector）；`test_center_fallback_for_any_object`→`test_center_only_for_any_object`（学习+识别 CENTER_BOX 唯一来源）；新增 `test_no_yolo_detector_in_app`（use_det=False + 禁 detector.run/_ocr.run(）与 `test_classify_ai_optional_detector`（ai 层 use_det）；`test_inference_releases_numpy_ref_after_run` 改 extract_feature 入口。Red→Green 后 31 项全 PASS，全量回归除 4 历史基线失败外全绿。
- **待板端验证**：① 任意物体学习+识别正确匹配；② 死机是否随单模型消失（静止测试）；③ 帧率（单 rec 比双 kmodel 每 do_det 帧少 1 次 det 推理，应更快）；④ 插桩（DET/LEARN/MATCH/DRAW/TICKED + [objcls]）定位后删除。

## 2026-08-07 object_classify 坑清单与重要信息汇总（备忘）

### 一、板端坑（按发现顺序，均已在代码修复并验证/待验证）

1. **K230 `Image.draw_rectangle(x, y, w, h)` 是 (x, y, 宽, 高) 语义，不是对角坐标**（最坑）
   - 误按 (x0,y0,x1,y1) 传 `draw_rectangle(0, 52, 639, 424)` → 实为宽 639、高 424 → 底部线画在 y=52+424=476，被底栏（428 起不透明）完全盖住，永远看不到。
   - 正确：`draw_rectangle(0, PREVIEW_Y, DISPLAY_SIZE[0]-1, PREVIEW_H-1, ...)`。项目里 body_detect 等所有画框都是 (x,y,w,h)。
2. **draw_rectangle 越界 1px 偶发驱动硬挂死**（fc≈4200 累积型，画面冻结 + 串口静默）
   - w=640 时右边界 x=640 超出 OSD1（640 宽，x 0..639）→ K230 绘制驱动对越界坐标处理不稳 → 随机挂死。
   - 修复：宽高各减 1。挂死不是 Python 异常，try/except 救不了；串口无输出 + 无重启 = 驱动级死锁（非内存耗尽）。
3. **顶栏/底栏是不透明 LVGL 对象，盖住 OSD1**：顶栏 y 0..51、底栏 y 428..479；画在顶栏内（如 y=6 的左上角 ID）或整屏框上下边全被盖 → 内容必须落在预览区 52..427。
4. **YOLOv8n 只认 COCO80 类**：学习/识别依赖 YOLO 检测框 → 非 COCO 物体检不到 → 无法学习（蜂鸣）或无法匹配。
5. **双 kmodel（det 320 + rec 224）交替推理是死机最大嫌疑路径**：单通道化（去 chn2 DMA）后死机更快；换单模型（use_det=False，只留 recognition）消除交替，可能根治死机（待板端静止测试确认）。
6. **AIBase.run 的 `self.cur_img = input_np` 持有输入引用直到下次 run**：planar 重排 921KB 缓冲可能被多个 AIBase 实例跨帧持有 → 峰值内存上升；packed 零拷贝路径（RGB888p_FMT）无此问题。
7. **`test_host_api_order` 背书"未握手也发数据"**：tick 未握手时仍 send_id_data 是设计（TTL 无流控下无对端不阻塞 TX），排查死机勿误改此行为。

### 二、重要信息（当前架构）

- **单 recognition 模型**（2026-08-07 用户确认）：`core/object_classify_ai.py` `ObjectClassifyRecognition(use_det=False)` 只加载 recognition.kmodel，不建 YOLO detector。
- **学习/识别统一 CENTER_BOX 中心区域**：`CENTER_BOX = [208, 128, 432, 352]`（VGA 画面中心 224×224，rec 输入尺寸）；任意物体对准中央按 K2 可学，识别时回中央可匹配；**非 COCO 物体只能在画面中央识别**（中心区域固定，一次一个物体）。
- **recognition.kmodel 是通用特征提取器**（不限制类别）——这是"任意物体可学"的根基。
- **匹配**：`database_search` 余弦相似度映射 score=cos/2+0.5，阈值 0.82（勿降 0.5：CNN 特征几乎总正相关 → 全部命中同一 ID），best-second 差 < 0.06 判不确定。
- **学习 ID 上限 5**（MAX_SLOTS=5，底栏 ID1~5 选项卡，选中绿高亮）；DB 存 plain list（`_to_list` 拷贝，避免 ulab/NPU 视图引用）。
- **颜色**：框与 ID 均按槽位取色 `BOX_COLORS`（1 绿/2 蓝/3 橙/4 紫/5 红），学习确认闪烁框 + 左上角 ID 同色；识别命中画预览区槽位色框（thickness 4）+ 左上角槽位色 ID（`PREVIEW_Y+4`）。
- **帧率**：DET_INTERVAL=3（每 3 帧一次中心区域提特征，其余帧用缓存匹配）；主循环每帧 gc.collect（show_image 后）。
- **部署文件**：`scripts/object_classify/app.py` + `core/object_classify_ai.py`（单模型后这两个即可；host_api/db/box_colors 未变）。
- **调试插桩**：DET/LEARN/MATCH/DRAW/TICKED（app）+ [objcls]（ai）——死机定位用，确认后删除。

### 板端验证通过（2026-08-07）：死机消除 + 删除全部插桩
- **用户确认**：单 recognition 模型版运行**很流畅、不再死机**——双 kmodel（det 320 + rec 224）交替推理确认为死机根因，越界 1px 为次要嫌疑，单模型方案同时消除两者。
- **收尾**：删除全部临时插桩——app.py 5 处（DET/LEARN/MATCH/DRAW/TICKED）+ object_classify_ai.py 4 处（[objcls] run det/feat done/extract start/done）；Select-String 确认无残留；py_compile 通过；契约测试 31 项全 PASS，全量回归仅剩 4 个历史基线失败（与本次无关）。
- **最终部署文件**：`scripts/object_classify/app.py` + `core/object_classify_ai.py`（拷到 /sdcard/CamerAi 对应路径，重启生效）。

## 2026-08-07 body_detect 重构：人体姿态识别（yolov8n-pose）+ 单通道化根治死机（用户确认）

### 现象
1. **死机**：body_detect 是最后仍用 chn2 XGA(1024×768) RGBP888 双通道的 AI 脚本，
   板端复测运行几分钟后卡死无响应（同 face/object 此前死机模式，无 traceback）。
2. **模型用错**：业务实际需要"人体姿态/关键点"，但脚本用的是 person_detect_yolov5n
   （实验5 人体检测）+ recognition.kmodel（实验20 通用特征提取做"识别是谁"）——
   recognition.kmodel 非人体专用模型，且功能形态与业务需求不符。

### 根因
1. **死机**：chn2 XGA 大帧(2.25MB/帧)硬件 DMA 与显示 DMA 竞争累积 → 几分钟后
   显示侧永久阻塞（object_detect/object_classify 已单通道化根治，body 因
   person_detect 模型输入 640×640 需 ≥640 高度源而一直保留 XGA）。
2. **模型**：通用特征提取模型（实验20 recognition）被误用于"人体身份区分"，
   并非人体姿态模型；业务要的是姿态识别。

### 解法（用户决策 2026-08-07）
1. **换模型**：yolov8n-pose.kmodel（实验6 人体关键点/姿态，输入 320×320，
   aidemo.person_kp_postprocess C 后处理）。
2. **单通道化**（照搬 object_detect 已验证方案）：_channels_for body_detect 分支
   去 chn2，AI 直接吃 chn0 VGA RGB888 显示帧（input_is_packed 探测：packed 零拷贝 /
   planar 重排）；模型 320 输入 VGA 足够。
3. **功能形态改姿态显示**：画人体框 + 17 关键点圆 + 19 骨骼线（SKELETON/LIMB_COLORS/
   KPS_COLORS 移植实验6），移除 K2 注册/body_db 注册链路/清除保存浮层/IdRegistry；
   坐标 clamp 到可视区防 draw 越界挂死（坑：draw_rectangle 越界 1px 死机）。
4. **协议 0x11**：数据格式不变（统一动态 N×10B），id = 按 (x,y) 升序后的目标序号
   （最左=1），learned 恒 0，conf = 检测置信度；通讯协议.txt 同步更新。
5. **降频**：DET_INTERVAL=2，非检测帧用缓存骨架画 + 帧内 gc.collect()（坑#16 防护）。

### 改动文件
- core/body_ai.py：重写为 PersonKeyPointApp（yolov8n-pose 单模型 + configure_ai2d_input）
- core/app_runtime.py：_channels_for body_detect 单通道化
- scripts/body_detect/app.py：重写（单通道主循环 + 骨架绘制 + host 上报 id=排序序号）
- 通讯协议.txt：0x11 语义更新（人体识别→人体姿态）
- resource/i18n/zh_CN.json + en_US.json：标题改"人体姿态/Body Pose"，删无用 body_detect 段
- tests/test_body_ai_ast.py + tests/test_body_detect_ast.py：契约重写（TDD Red→Green）

### 验证
- 契约 Red→Green：test_body_ai_ast 6 项 / test_body_detect_ast 8 项全 PASS；
  全量回归 56/60（4 历史基线失败不变：face_register/object_db/tag_db/template）；
  py_compile 通过；code-simplifier 优化（槽位构建去 0 占位）后复测仍全绿。
- 板端复验清单：① body_detect 进入显示人体骨架（17 点 + 19 线 + 白框），
  有人/无人场景跑 15+ 分钟不死机（fc 持续增长 + mem_free 稳定）；② 主机 0x11
  每帧 N×10B，id = 排序序号（最左=1），conf = 置信度；③ 无人场景 0 载荷。
- 部署文件：core/body_ai.py + core/app_runtime.py + scripts/body_detect/app.py
  （拷贝到 /sdcard/CamerAi 对应路径，重启生效）。

### 板端反馈修复（2026-08-07）：只见人体框不见骨骼——关键点防护误用 19 点
- **现象（用户反馈）**：body_detect 重构版部署后画面"还是画框框"，没画骨架。
- **根因**：`aidemo.person_kp_postprocess` 返回每人 **COCO 17 个关键点**
  （(x,y,score)）；demo 实验6 的 `for k in range(17+2)` 是循环 19 次画 **19 条
  骨骼线**，其访问的关键点索引最大 16，从未取第 18/19 个点。移植时 `_draw_skeleton`
  误写 `if len(kps) < 19: continue` → 板端 17 < 19 恒成立 → **整人跳过骨架绘制，
  只剩白框**。
- **解法**：防护改为 `len(kps) < 17`（只需 17 个关键点即可画圆 + 19 条线），
  注释说明 17 点 vs 19 线循环的区别，防再误改。
- **契约更新**：`test_body_detect_ast` 新增 `test_draw_skeleton_min_keypoints_17`
  （断言 `len(kps) < 17`），Red→Green；全量回归 56/60（4 历史基线失败不变）；
  py_compile 通过。
- **板端复验**：重新部署 scripts/body_detect/app.py 后，应显示骨架
  （17 点圆 + 19 骨骼线 + 白框）；若仍无骨架，打印 kpses 形状/score 定位。

### 板端反馈修复（2026-08-07 第二轮）：去白框 + 主机无数据 + 骨骼延迟/卡顿
- **用户反馈**：① 不要白框,只画骨骼;② 识别到骨骼时主机 config 一直为空;
  ③ 骨骼不贴人、有延迟;④ 整体画面卡顿。
- **① 去白框**：`_draw_skeleton` 删除人体框绘制,只画 17 点 + 19 线;
  boxes 仅作 kpses 索引(契约:test_no_draw_rectangle_body_skeleton 断言
  app 全文无该绘制调用)。
- **② 主机无数据根因**：`aidemo.person_kp_postprocess` 返回的人体框元素
  可能只有 4 元 `[l,t,r,b]` 无 score(demo 实验6 只用了 len(res[0]),从未解析
  元素内容,无法佐证第 5 个值是 score);on_frame 原按 5 元硬解 →
  板端 ValueError → except 吞掉 → slots 恒空 → 发 0 载荷 → 主机 config 空。
  修复:取 `boxes[i][:4]`,`len(boxes[i]) >= 5` 时有 score 才用(×100),
  否则缺省 conf=100(契约:test_box_parse_tolerates_missing_score)。
- **③④ 延迟/卡顿**：DET_INTERVAL 2→3(同 object_detect 提平均帧率经验,
  契约:test_det_interval_is_3);骨骼不贴人主要来自降频缓存滞后,
  提帧率后复测;若静止仍不贴再查坐标空间。
- **验证**：契约 Red→Green(body_detect 12 项 / body_ai 6 项全 PASS);
  全量回归 56/60(4 历史基线失败不变);py_compile 通过。
- **板端复验**：重新部署 scripts/body_detect/app.py 后:① 画面只有骨架
  无白框;② 主机 config 应有 N×10B 数据(id=排序序号);③ 流畅度/贴合度
  复测;若主机仍空,查 UART 握手日志;若静止时骨骼仍偏移,打印 kps 范围。

### 板端验收通过（2026-08-07 第三轮）：body_detect 姿态识别闭环
- **用户确认**："可以了"——画面只有骨架（17 关键点 + 19 骨骼线，无白框）、
  主机 config 有数据（0x11 N×10B，id=排序序号）、流畅度可接受。

### body_detect 坑清单（防再踩，同 object_classify 汇总风格）
1. **aidemo.person_kp_postprocess 返回 COCO 17 关键点**：demo 实验6 的
   `for k in range(17+2)` 是循环 19 次画 19 条骨骼线（访问关键点索引最大 16），
   不是 19 个点——移植防护误写 `len(kps) < 19` → 整人跳过骨架绘制 →
   板端只见白框不见骨骼（已改 `< 17`）。
2. **该接口人体框元素可能只有 4 元 [l,t,r,b] 无 score**：demo 只用
   `len(res[0])` 从未解析元素内容；按 5 元硬解 → ValueError 被 except 吞 →
   slots 恒空 → 发 0 载荷 → **主机 config 静默为空（无任何报错）**。
   解析须 `boxes[i][:4]` + `len(boxes[i]) >= 5` 才取 score（×100），缺省
   conf=100（已改）。
3. **死机根因闭环**：chn2 XGA 大帧 DMA 与显示 DMA 竞争 → 换 320 输入模型
   （yolov8n-pose）后单通道化根治（同 object_detect 方案）。
4. **模型选择**：recognition.kmodel 是通用特征提取（实验20），非人体专用；
   "人体识别"业务若指姿态，用 yolov8n-pose（实验6，320 输入）/ falldown
   （实验7，640 输入）。
5. **延迟/贴合**：降频缓存（DET_INTERVAL=3）致骨骼滞后于人体是降频的正常
   表现，提帧率后改善；静止仍偏移才是坐标空间问题（本板端未出现）。

### 最终部署文件
- core/body_ai.py（PersonKeyPointApp：yolov8n-pose 单模型 + input_is_packed）
- core/app_runtime.py（body_detect 单通道分支）
- scripts/body_detect/app.py（单通道主循环 + 骨架绘制 + 0x11 id=排序序号）
- 协议 0x11 语义：人体姿态，id = 按 (x,y) 排序后的目标序号（最左=1），
  learned 恒 0，conf = 检测置信度（无 score 时缺省 100）。

## 2026-08-07 手势识别重构：单通道 + 任意手势 ID 学习（参考 object/body 单通道方案）
### 现象（用户报告）
- gesture_detect 运行报错：`NameError: local variable referenced before assignment`（on_frame line 201）
- 用户询问当前手势识别模型数量 → 2 个（hand_det.kmodel 手掌检测 + hand_reco.kmodel 4 类手势分类）
- 需求：改成单通道推理+显示（参考 object_detect/body_detect）；只识别任意手势，配合 ID 学习记录
### 根因
- NameError：on_frame 的 global 声明漏了 `_last_names`（函数内有赋值 → 被当局部变量；第三次循环（非检测帧）读未初始化局部变量）。原 `_last_slots` 已声明但 `_last_names` 漏了
- 双通道死机隐患：chn2 SVGA 800x600 大帧 DMA 与显示 DMA 竞争（同 object/body 已根治的 chn2 XGA 模式）
- 4 类分类语义（gun/other/yeah/five）不符"任意手势"需求；官方无单模型手势识别 kmodel（hand_det 只输出"手掌"单类），手势识别必须双模型串联
### 方案（用户确认）
- 单通道（同 object/body）：AI 直吃 chn0 VGA 显示帧（img.to_numpy_ref()），移除 chn2
- hand_reco 语义变更：4 维 softmax 分布 = 手势形态特征（不再作 4 类分类），任意手势均可学习
- 任意手势 ID 学习：K2 注册当前最大手掌特征 → gesture_db 余弦匹配 → ID（相似特征注册不重复占槽）
- 保留双模型（官方无替代），但 hand_reco 角色从"4 类分类"升级为"特征提取"
### 关键实现
- core/gesture_db.py：register(feature)/match(feature) 余弦匹配（score=cos/2+0.5，阈值 0.85 + margin 0.10 防已注册手势间混淆）；旧 label_idx 数据加载时 one-hot 迁移（兼容旧库）
- core/gesture_ai.py：RGB888P_SIZE=[640,480]（chn0 VGA）、configure_ai2d_input/input_is_packed（packed 零拷贝直喂）、HandRecognitionApp.postprocess 返回 4 维特征 list、HandRecognition.run 返回 (det_boxes, feats)
- scripts/gesture_detect/app.py：on_frame 直吃 chn0 帧（非 packed 时 planar 重排）；gesture_db.match(feat) 填槽（未学习上报 id=0 + 检测置信度）；K2 注册 reg_feat；名称帧（0x0E）固定名 "GESTURE"；画框只显示 ID 不再显示类别名
- core/app_runtime.py：_channels_for 的 gesture_detect 分支改单通道（移除 chn2 SVGA RGBP888）
### 验证
- 测试 Red→Green：test_gesture_db（11 项特征语义）、test_gesture_db_persist（4 项）、test_gesture_ai_ast（6 项）、test_gesture_detect_ast（13 项单通道+特征断言）
- 全量回归：除 4 个历史基线失败（face_register/template/object_db/tag_db，git stash 验证与本次改动无关）外全 PASS；py_compile 通过
- 板端需真机验证：手势检测框、K2 学习分配 ID、同手势再识别显示 ID、未学习白框、流畅度（DET_INTERVAL=2，卡顿可参考 object/body 调 3）
